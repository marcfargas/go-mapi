---
milestone: v2.0.0
audited: 2026-04-10T23:55:00.000Z
status: tech_debt
scores:
  requirements: 42/43 satisfied, 1 deferred (SIGN-01)
  phases: 4/4 complete
  integration: 4/4 wirings verified
  flows: 2/2 user flows specified (E2E-03 happy path, E2E-04 install UX) — runtime validation pending first CI run
gaps:
  requirements: []
  integration: []
  flows:
    - flow: "Happy path draft creation"
      status: "source complete, runtime-pending"
      pending_on: "first CI run of .github/workflows/e2e.yml on windows-latest"
    - flow: "Install UX (MISSING → READY transition)"
      status: "source complete, runtime-pending"
      pending_on: "first CI run of .github/workflows/e2e.yml on windows-latest"
    - flow: "Installer end-to-end (install → verify registry → uninstall → verify clean)"
      status: "source complete, runtime-pending"
      pending_on: "first CI run of .github/workflows/installer-smoke.yml on windows-latest"
    - flow: "go test -race on amd64 (FOUND-01 race fix regression guard)"
      status: "source complete, runtime-pending"
      pending_on: "first run of .github/workflows/go-race-nightly.yml (manual dispatch or nightly cron)"
tech_debt:
  - phase: "01-foundation-signpath-application"
    items:
      - "SIGN-01 filing deferred by Marc — draft complete at SIGNPATH-APPLICATION.md, file whenever ready"
      - "Pre-existing staticcheck suggestions in src/native-host/gmail.go (~10), main.go, watcher.go — explicitly out of scope for Phase 1/4"
  - phase: "04-test-suite-completeness-e2e"
    items:
      - "PHASE-4-FINDING-02 (low, pre-existing): -Wcomment warning in src/interceptor/json_writer.h:36 — trailing backslash in a // comment. Bundle with next interceptor edit."
      - "PHASE-4-FINDING-03 (informational): go test -race is not available on windows/arm64 — affects Marc's main dev box. Documented in FINDINGS.md; nightly CI targets windows-latest (amd64) so the race guard is still in place."
deferred:
  - "SignPath Foundation application filing — Marc to file on own schedule (SIGN-01)"
  - "OUTDATED host-version branch activation — dead code in v2.0.0, activates in v3.0.0 by bumping MIN_SUPPORTED_HOST_VERSION"
  - "blegal.dev download mirror, Edge Add-ons Store publishing, nightly real-Gmail smoke — v2.1.0 candidates"
fixes_applied_during_audit:
  - "EXT-07 (closed as no-op): Phase 2's placeholder INSTALLER_DOWNLOAD_URL already matched Phase 3's final URL. Removed the TODO comment and marked EXT-07 complete in REQUIREMENTS.md."
  - "PHASE-4-FINDING-01 (fixed): HOST_INSTALLED_TOAST on MISSING → PROBING → READY reconnect was silently broken because the guard checked prev === 'MISSING' and the reachable sequence always transits through PROBING. Fixed by latching a sticky wasMissingThisSession flag on entering MISSING and checking that at the READY edge. The two Phase 4 tests that locked the buggy behavior were flipped to assert 1 toast. 87/87 extension tests still green."
---

# v2.0.0 Milestone Audit — 2026-04-10

## Summary

**Status:** `tech_debt` — all requirements shipped or explicitly deferred, no critical gaps, but the installer + E2E + race-nightly pipelines await their first CI run on `windows-latest` to convert source-complete into runtime-verified. One item (SIGN-01) is user-owned and deferred indefinitely.

**Score:** 42/43 requirements complete + 1 deferred (SIGN-01 by Marc).

This audit ran inline as the closing step of an autonomous execution of Phases 2–4 in parallel git worktrees (`/gsd:autonomous --from 2`), after Phase 1 was delivered in a prior session. All three worktree agents merged cleanly into `develop` with no conflicts.

## Phase Verification Summary

| Phase | Status | Plans | Verification Source |
|-------|--------|-------|---------------------|
| 1. Foundation & SignPath Application | ✓ passed | 8/8 | `01-VERIFICATION.md` — FOUND-01..06 green, SIGN-01 draft ready (filing deferred) |
| 2. Extension Install UX | ✓ passed | 4/4 | `02-VERIFICATION.md` — Go unchanged & green, tsc/lint/tests clean (43/43 at phase close, 87/87 after Phase 4 tests landed) |
| 3. Inno Setup Installer + Signing + Distribution | ⚡ human_needed | 4/4 | `03-VERIFICATION.md` — source complete, CI-pending (no Windows runner / Inno Setup / SignPath in executor sandbox) |
| 4. Test-Suite Completeness + E2E | ⚡ human_needed | 4/4 | `04-VERIFICATION.md` — Go/C++/TS tests all green locally (87 extension tests, 23 doctest cases/76 asserts, Go build/vet/test clean). E2E and `-race` nightly pending first CI run on `windows-latest` |

### Why two phases are `human_needed`, not `passed`

The executor sandbox does not have:
- **Inno Setup 6 (`iscc.exe`)** for the `.iss` build (Phase 3)
- **Admin rights + Windows MAPI registry** for the Pester install → verify → uninstall round-trip (Phase 3)
- **Chromium** for Playwright `launchPersistentContext` runs (Phase 4)
- **SignPath project + API secrets** (Phase 3 — also blocked by Marc's deferred SIGN-01 filing)
- **`-race` runtime** on `windows/arm64` (Phase 4, FOUND-01 regression guard)

All four are exercised by the first CI run of the relevant workflows on `windows-latest` (which is `amd64` and has the full toolchain). There is no code that is expected to fail — just code that has not yet been runtime-validated on a real Windows runner.

## Requirements Coverage (3-source cross-reference)

43 milestone requirements checked across REQUIREMENTS.md traceability, phase SUMMARY frontmatter, and phase VERIFICATION tables:

| Category | Count | Status |
|----------|-------|--------|
| FOUND-01..06 | 6/6 | Complete (Phase 1) |
| EXT-01..07 | 7/7 | Complete (Phase 2 + Phase 2/3 merge close-out for EXT-07) |
| INST-01..07 | 7/7 | Source complete (Phase 3) — CI-pending |
| SIGN-01 | 0/1 | **Draft, filing deferred by Marc** |
| SIGN-02..05 | 4/4 | Source complete (Phase 3) — CI-pending / SignPath-pending |
| GOTEST-01..04 | 4/4 | Complete (Phase 4) — nightly `-race` job pending first run |
| CPPTEST-01..03 | 3/3 | Source complete (Phase 4) — ctest step pending first CI run |
| TSTEST-01..05 | 5/5 | Complete (Phase 4) — 87/87 extension tests green locally |
| E2E-01..06 | 6/6 | Source complete (Phase 4) — Playwright E2E pending first CI run |
| **Total** | **42/43 + 1 deferred** | |

No orphans. Every REQ-ID in REQUIREMENTS.md's traceability table maps to a phase SUMMARY and a plan in that phase.

## Cross-Phase Integration Verification

Verified manually during the merge sequence (Phase 2 + Phase 3 merged first, Phase 4 merged last):

1. **Phase 1 → Phase 2 (FOUND-02 `hostVersion` → EXT-03)** ✓
   - `src/extension/src/types/messages.ts` line 68 has `hostVersion?: string` (FOUND-02)
   - `src/extension/src/background/service-worker.ts` line 17 reads `hostVersion` from `NativeReadyMessage`
   - `hostVersion.ts` consumes the compatible version semantics

2. **Phase 1 → Phase 3 (FOUND-06 `.tmpl` manifests → INST-03)** ✓
   - `src/installer/go-mapi.iss` reads the same `.tmpl` files produced by Phase 1 for the five browser registry trees
   - `scripts/install.ps1` (Phase 1 refactor) and `go-mapi.iss` (Phase 3) share the single template source per FOUND-06

3. **Phase 2 ↔ Phase 3 parallel coordination (EXT-07)** ✓
   - Phase 2 seeded the `INSTALLER_DOWNLOAD_URL` constant in `InstallPrompt.tsx` with the exact final URL Phase 3 locks in `installer-release.yml` output filename
   - Post-merge verification: `grep -c "releases/latest/download/go-mapi-setup.exe" src/extension/src/popup/InstallPrompt.tsx` → 1
   - EXT-07 swap was a no-op — closed during audit, TODO comment removed

4. **Phase 2 → Phase 4 (hostDetector, hostVersion, InstallPrompt, service-worker → TSTEST-02/03/04/05)** ✓
   - All four test files import from their Phase 2 production modules with zero Chrome API mocking required for the pure-lib modules
   - Phase 4 surfaced PHASE-4-FINDING-01 (EXT-06 toast silently broken on reconnect) — **fixed during audit**
   - 87/87 extension tests green after the fix

5. **Phase 1 → Phase 4 (FOUND-03 baseURL → GOTEST-01, FOUND-04 flag/env → E2E-02)** ✓
   - `gmail_test.go` uses `NewGmailClientWithBase` + `httptest.Server` per FOUND-03's dedicated constructor decision
   - Mock Gmail server at `tests/e2e/mock-gmail/` injected via `--gmail-api-base` from FOUND-04

6. **Phase 1 → Phase 4 (FOUND-05 message_converter extraction → CPPTEST-02)** ✓
   - `src/interceptor/tests/message_converter_tests.cpp` links the existing OBJECT library without rebuilding the source (per FOUND-05's CMake `OBJECT` library decision)
   - 23 doctest cases / 76 assertions green locally

7. **Phase 3 → Phase 4 (installer artifacts → E2E-04 install-UX flow)** ✓
   - `tests/e2e/install-ux.spec.ts` launches Chromium without a registered native-messaging manifest, writes one pointing at `tests/e2e/mock-host/`, and asserts the MISSING → READY transition
   - With the PHASE-4-FINDING-01 fix, the spec can now also assert the success toast (it currently does not assert the toast — a small follow-up, documented below)

## Tech Debt Summary

- **SIGN-01 filing** — Marc to file the SignPath Foundation application whenever ready. Draft is complete and reviewer-safe. Does not block the v2.0.0 release (SIGN-03 unsigned fallback is in place).
- **Pre-existing staticcheck warnings in `gmail.go`, `main.go`, `watcher.go`** — ~10 total suggestions. Explicitly out of scope for both Phase 1 and Phase 4. Address in a dedicated cleanup commit or the next time any of these files needs a real edit.
- **Pre-existing `-Wcomment` warning in `src/interceptor/json_writer.h:36`** — surfaced more visibly by CPPTEST-02's build. One-line fix (trailing backslash → forward slash). Bundle with next interceptor edit.
- **`go test -race` unavailable on windows/arm64** — affects local repro on Marc's main dev box. Nightly CI (`go-race-nightly.yml`) targets `windows-latest` amd64 where `-race` works, so the regression guard is still in place. Document in `CONTRIBUTING.md` when written.
- **E2E-04 does not currently assert the HOST_INSTALLED_TOAST** — intentionally omitted by the Phase 4 agent because PHASE-4-FINDING-01 had not yet been fixed at the time the spec was written. With the fix now in place, a one-line `expect(toast).toBeVisible()` could be added on the next Phase 4 touch. Non-blocking — the unit test `service-worker.test.ts` now locks the toast invariant.

## Fixes Applied During This Audit

1. **EXT-07 close-out** (commit `2f1abcc`): placeholder URL matched final Phase 3 URL, TODO comment removed, REQUIREMENTS.md updated.
2. **PHASE-4-FINDING-01 fix** (commit `3fa2272`): EXT-06 success toast now fires on MISSING → PROBING → READY reconnect. 5-line change in `service-worker.ts` plus flipping the two Phase 4 tests that had locked the buggy zero-toast behavior.

Both fixes were within the spirit of "close milestone cleanly" — the toast is part of the advertised EXT-06 contract and was trivially fixable before audit sign-off.

## Nyquist Compliance

Skipped — `workflow.nyquist_validation` is `false` in `.planning/config.json`.

## Routing

Status is `tech_debt` because the runtime-pending CI validations and deferred SIGN-01 filing are acceptable debt, not blockers. No unsatisfied requirements. Recommended next step: `/gsd-complete-milestone v2.0.0` to archive, then trigger the three CI workflows (`installer-smoke.yml`, `e2e.yml`, `go-race-nightly.yml`) manually via `gh workflow run` to lock in the runtime-verified half.
