---
phase: 11-autoupdate-release
plan: 05
type: execute
wave: 3
depends_on: [11-03]
files_modified:
  - tests/sandbox/phase11/go-mapi-phase11.wsb
  - tests/sandbox/phase11/Run-Phase11Smoke.ps1
  - tests/sandbox/phase11/Collect-Phase11Evidence.ps1
  - .planning/phases/11-autoupdate-release/11-SMOKE-CHECKLIST.md
  - .planning/phases/11-autoupdate-release/11-SMOKE-EVIDENCE.md
autonomous: false
requirements:
  - REL-06
tags:
  - sandbox
  - smoke-test
  - release-verification
  - evidence
must_haves:
  truths:
    - "A reproducible clean-machine harness exists outside hosted CI for the full v3.0 user journey."
    - "Automated portions capture screenshots/video where practical and the manual tail is explicit and short."
    - "The final smoke evidence proves one successful path: install, sign in, MAPI trigger, queue row, Gmail draft, uninstall clean."
  artifacts:
    - path: tests/sandbox/phase11/go-mapi-phase11.wsb
      provides: "Disposable Windows Sandbox config for Phase 11 smoke"
      min_lines: 20
    - path: tests/sandbox/phase11/Run-Phase11Smoke.ps1
      provides: "Automation harness for install/uninstall and evidence staging"
      min_lines: 120
    - path: .planning/phases/11-autoupdate-release/11-SMOKE-EVIDENCE.md
      provides: "Evidence record with screenshot/video/checklist references and final verdict"
      min_lines: 80
  key_links:
    - from: "tests/sandbox/phase11/Run-Phase11Smoke.ps1"
      to: "stable latest-download installer URL"
      via: "clean-machine install path"
      pattern: "latest/download/go-mapi-setup.exe"
    - from: ".planning/phases/11-autoupdate-release/11-SMOKE-EVIDENCE.md"
      to: "manual checklist"
      via: "explicit record of OAuth + MAPI shell steps"
      pattern: "install|sign in|MAPI|queue|draft|uninstall"
---

<objective>
Create and run the pre-GA clean-machine Phase 11 smoke path outside hosted CI.

Purpose: REL-06 explicitly requires the full user journey on a reproducible clean Windows environment, and the phase decisions prohibit trying to overload hosted GitHub runners with OAuth/UI/MAPI shell flow. This plan produces the sandbox harness, evidence scripts, and the blocking human verification step.

Output: sandbox configuration, automation scripts, smoke checklist, and an evidence file with screenshots/video/manual-tail proof that acts as the pre-GA gate for public release.
</objective>

<execution_context>
@C:/dev/go-mapi/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/go-mapi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-autoupdate-release/11-CONTEXT.md
@.planning/phases/11-autoupdate-release/11-RESEARCH.md
@.planning/phases/09-queue-automode-toasts/09-CONTEXT.md
@.planning/phases/10-installer-migration/10-05-PLAN.md
@.planning/phases/10-installer-migration/10-06-PLAN.md

<interfaces>
Locked decisions this plan must implement:
- D-13: Windows Sandbox or equivalent clean reproducible Windows environment is acceptable.
- D-14: automate as much as practical; short manual tail allowed.
- D-15: automated portions should capture screenshots/video where feasible.
- D-16: the success bar is one fully working clean-machine journey.

This plan must NOT move the full OAuth/UI flow into hosted CI. Keep the sandbox harness separate from `.github/workflows/installer-smoke.yml`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the sandbox harness and evidence collection scripts</name>
  <files>
    tests/sandbox/phase11/go-mapi-phase11.wsb
    tests/sandbox/phase11/Run-Phase11Smoke.ps1
    tests/sandbox/phase11/Collect-Phase11Evidence.ps1
    .planning/phases/11-autoupdate-release/11-SMOKE-CHECKLIST.md
    .planning/phases/11-autoupdate-release/11-SMOKE-EVIDENCE.md
  </files>
  <action>
    Build a reproducible clean-machine harness rooted under `tests/sandbox/phase11/`, not in GitHub Actions. The harness should:
    - download or stage the installer under test, using either the Phase 10 `workflow_dispatch` dry-run artifact or the stable `latest/download/go-mapi-setup.exe` asset when a release candidate already exists;
    - automate installer launch, app launch, and uninstall where shell scripting is reliable;
    - prepare an evidence directory for screenshots/video/log snippets;
    - leave a short explicit manual tail for the OAuth consent and MAPI-trigger verification steps that are not worth hardening further in this phase.

    Create `11-SMOKE-CHECKLIST.md` with a numbered human-readable sequence and expected outcomes. Create `11-SMOKE-EVIDENCE.md` as the artifact ledger for screenshot names, video path, timestamps, and pass/fail notes for each step.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$files=@('tests/sandbox/phase11/go-mapi-phase11.wsb','tests/sandbox/phase11/Run-Phase11Smoke.ps1','tests/sandbox/phase11/Collect-Phase11Evidence.ps1','.planning/phases/11-autoupdate-release/11-SMOKE-CHECKLIST.md','.planning/phases/11-autoupdate-release/11-SMOKE-EVIDENCE.md'); foreach($f in $files){ if(-not (Test-Path $f)){ throw \"missing $f\" } }; [xml](Get-Content 'tests/sandbox/phase11/go-mapi-phase11.wsb' -Raw) | Out-Null; $err=$null; [void][System.Management.Automation.PSParser]::Tokenize((Get-Content 'tests/sandbox/phase11/Run-Phase11Smoke.ps1' -Raw), [ref]$err); if($err.Count -gt 0){ throw 'Run-Phase11Smoke.ps1 parse errors' }; $err=$null; [void][System.Management.Automation.PSParser]::Tokenize((Get-Content 'tests/sandbox/phase11/Collect-Phase11Evidence.ps1' -Raw), [ref]$err); if($err.Count -gt 0){ throw 'Collect-Phase11Evidence.ps1 parse errors' }; $run=Get-Content 'tests/sandbox/phase11/Run-Phase11Smoke.ps1' -Raw; if(($run -notmatch 'workflow_dispatch') -and ($run -notmatch 'releases/latest/download/go-mapi-setup.exe')){ throw 'installer source missing from harness' }; $ev=Get-Content '.planning/phases/11-autoupdate-release/11-SMOKE-EVIDENCE.md' -Raw; foreach($s in @('install','sign in','MAPI','queue','draft','uninstall')){ if($ev -notmatch [regex]::Escape($s)){ throw \"missing evidence step $s\" } }"</automated>
  </verify>
  <done>
    A reproducible sandbox harness exists and the manual tail is explicitly documented rather than implied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the clean-machine smoke pass and record the evidence bundle</name>
  <what-built>
    The sandbox harness, checklist, and evidence ledger now exist under `tests/sandbox/phase11/` and `.planning/phases/11-autoupdate-release/`, and this rehearsal is the gate before the public GA tag push.
  </what-built>
  <how-to-verify>
    1. Launch the clean environment using `tests/sandbox/phase11/go-mapi-phase11.wsb` or the equivalent reproducible VM.
    2. Run `Run-Phase11Smoke.ps1` to execute the automated install/evidence setup steps against the release-candidate artifact or stable installer under test.
    3. Complete the manual tail from `11-SMOKE-CHECKLIST.md`: sign in, trigger a MAPI send, confirm queue row appears, create a Gmail draft, then uninstall clean.
    4. Run `Collect-Phase11Evidence.ps1` if needed to gather screenshots/video/log files into the evidence folder.
    5. Fill in `11-SMOKE-EVIDENCE.md` with screenshot/video filenames, timestamps, and pass/fail notes for every checklist step.
  </how-to-verify>
  <resume-signal>Type `approved` once `11-SMOKE-EVIDENCE.md` records a full passing clean-machine journey, or describe the failed step and captured evidence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stable release asset → clean sandbox | Public installer is executed in a fresh Windows environment |
| Sandbox automation → manual OAuth/MAPI steps | Human interaction completes the browser and shell edges that are intentionally not over-automated |
| Evidence artifacts → planning docs | Screenshots/video/logs are summarized back into the repo as closure proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Denial of service | Hosted CI limitations | mitigate | Keep the full REL-06 flow out of hosted CI and run it in Windows Sandbox or equivalent per D-13. |
| T-11-05-02 | Repudiation | Smoke verification | mitigate | Require screenshot/video/checklist references in `11-SMOKE-EVIDENCE.md`. |
| T-11-05-03 | Information disclosure | Evidence bundle | mitigate | Capture app behavior/status only; do not include OAuth secrets or sensitive email body content in screenshots/video. |
</threat_model>

<verification>
The deliverable is not complete until `11-SMOKE-EVIDENCE.md` contains a full pass record from a clean-machine run and the user checkpoint is approved. Public GA release work in `11-04` stays blocked on that result.
</verification>

<success_criteria>
- Clean-machine harness is reproducible.
- Hosted CI is not overloaded with OAuth/UI flow.
- Evidence includes screenshots/video where practical plus a short explicit manual checklist.
- One full install→sign-in→MAPI→queue→draft→uninstall journey passes.
- The rehearsal result is captured before the public GA release is cut.
</success_criteria>

<output>
After completion, create `.planning/phases/11-autoupdate-release/11-05-SUMMARY.md`.
</output>
