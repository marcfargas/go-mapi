---
phase: 11-autoupdate-release
plan: 04
type: execute
wave: 4
depends_on: [11-02, 11-03, 11-05]
files_modified:
  - src/app/wails.json
  - README.md
  - .github/release-template.md
  - .planning/phases/11-autoupdate-release/11-RELEASE-EVIDENCE.md
autonomous: false
requirements:
  - REL-01
  - REL-02
  - REL-07
tags:
  - release
  - docs
  - github-releases
  - store-retirement
must_haves:
  truths:
    - "README is fully v3-only and no longer describes Phase 11 as future work."
    - "Release metadata is cut over to the shipping version and supports the stable `latest/download/go-mapi-setup.exe` URL."
    - "Browser-store retirement/deprecation work has proof captured in-repo per REL-01 as aligned to D-09 and D-12."
    - "Release notes use strong cutover language: uninstall v2, install v3, v2 is retired."
  artifacts:
    - path: src/app/wails.json
      provides: "shipping `info.productVersion` value for the final v3.0 release"
      contains: "productVersion"
    - path: README.md
      provides: "v3-only install/setup/update flow and no Phase-11-planned language"
      min_lines: 80
    - path: .planning/phases/11-autoupdate-release/11-RELEASE-EVIDENCE.md
      provides: "release and store-retirement proof bundle with links/screenshots/checklist"
      min_lines: 60
  key_links:
    - from: "src/app/wails.json"
      to: ".github/workflows/installer-release.yml"
      via: "version authority validated against pushed tag"
      pattern: "productVersion"
    - from: ".planning/phases/11-autoupdate-release/11-RELEASE-EVIDENCE.md"
      to: "Chrome Web Store and Edge Add-ons listings"
      via: "captured screenshots/URLs and submitted deprecation action proof"
      pattern: "Chrome Web Store|Edge Add-ons|screenshot|proof"
---

<objective>
Cut the v3.0 release surface and record store-retirement evidence.

Purpose: REL-01, REL-02, and REL-07 define the public ship boundary. For this phase, REL-01 is satisfied by frozen/deprecated Chrome Web Store and Edge Add-ons listings with strong retirement messaging plus captured proof of the store-side action, matching D-09 and D-12.

Output: final release/docs cutover, shipping version bump, release evidence file, and a blocking store-proof checkpoint that stays ahead of the actual GA release action and follows the clean-machine rehearsal.
</objective>

<execution_context>
@C:/dev/go-mapi/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/go-mapi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-autoupdate-release/11-CONTEXT.md
@.planning/phases/11-autoupdate-release/11-RESEARCH.md
@.planning/phases/10-installer-migration/10-06-PLAN.md
@.planning/phases/10-installer-migration/10-06-SUMMARY.md
@README.md
@.github/release-template.md
@src/app/wails.json

<interfaces>
Locked decisions this plan must implement:
- D-09: keep Chrome Web Store and Edge listings published but frozen with strong deprecation messaging.
- D-10: README becomes fully v3-only; no maintained legacy doc tree.
- D-11: release notes use strong cutover language.
- D-12: proof of initiated retirement/deprecation action is sufficient for closure.

Alignment note:
- REL-01 is implemented in this phase as store retirement via frozen/deprecated listings plus captured proof, matching D-09 and D-12.
- The evidence file and summary must say that explicitly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize shipping metadata and v3-only docs</name>
  <files>
    src/app/wails.json
    README.md
    .github/release-template.md
    .planning/phases/11-autoupdate-release/11-RELEASE-EVIDENCE.md
  </files>
  <action>
    Update `src/app/wails.json` from the placeholder release version to the actual shipping version for the Phase 11 cut. Rewrite `README.md` so it describes the shipped v3 installer/update path, not planned work, and make it fully v3-only per D-10. Tighten `.github/release-template.md` so the release body uses strong cutover language per D-11: uninstall v2, install v3, manual update path only, v2 retired.

    Create `11-RELEASE-EVIDENCE.md` as the durable artifact for this phase. It must contain:
    - the exact stable installer URL;
    - a pre-release checklist and a post-release verification checklist;
    - placeholders/sections for Chrome Web Store and Edge Add-ons proof, including screenshot references and final listing URLs;
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const w=JSON.parse(fs.readFileSync('src/app/wails.json','utf8')); if(!/^\\d+\\.\\d+\\.\\d+$/.test(w.info.productVersion)) throw new Error('shipping productVersion missing'); const readme=fs.readFileSync('README.md','utf8'); if(/Phase 11 planned|Planned \\| Phase 11|Notify-only autoupdate \\+ release pipeline \\| Planned/i.test(readme)) throw new Error('README still says planned'); const ev=fs.readFileSync('.planning/phases/11-autoupdate-release/11-RELEASE-EVIDENCE.md','utf8'); for (const s of ['latest/download/go-mapi-setup.exe','Chrome Web Store','Edge Add-ons','REL-01','D-09','D-12']) { if(!ev.includes(s)) throw new Error('missing '+s); }"</automated>
  </verify>
  <done>
    Shipping version metadata, README, release template, and release/store evidence scaffolding are ready for the final human-proof checkpoint and GA action.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture browser-store retirement proof per D-09 and D-12</name>
  <what-built>
    The repo now contains `11-RELEASE-EVIDENCE.md` with exact sections for Chrome Web Store and Edge Add-ons proof, required screenshot names, listing URLs, and the REL-01 / D-09 / D-12 alignment note.
  </what-built>
  <how-to-verify>
    1. Update the Chrome Web Store listing with strong deprecation/frozen messaging that directs users to the desktop app.
    2. Update the Edge Add-ons listing with matching deprecation/frozen messaging.
    3. Capture screenshots or submitted-form evidence for both stores.
    4. Add the screenshot filenames/URLs and the final listing URLs into `11-RELEASE-EVIDENCE.md`.
    5. Confirm whether you want the GA release/tag pushed immediately after this checkpoint.
  </how-to-verify>
  <resume-signal>Type `approved` once the store proof is recorded in `11-RELEASE-EVIDENCE.md`, or respond with the missing store/evidence item.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the GA release through the existing installer workflow and record stable-URL proof</name>
  <files>
    .planning/phases/11-autoupdate-release/11-RELEASE-EVIDENCE.md
  </files>
  <action>
    After the human checkpoint is approved and the Phase 11 clean-machine rehearsal has passed, use the Phase 10 release pipeline rather than inventing a manual publish path. Create/push the final release tag that matches the updated `wails.json` version, wait for `.github/workflows/installer-release.yml` to finish, verify that the GitHub Release contains `go-mapi-setup.exe`, and verify the stable latest-download URL resolves to the new asset.

    Append the release evidence file with:
    - the final tag and GitHub Release URL;
    - workflow run URL and outcome;
    - proof that `https://github.com/marcfargas/go-mapi/releases/latest/download/go-mapi-setup.exe` resolves successfully;
    - any SignPath fallback note if the release shipped unsigned.
  </action>
  <verify>
    <automated>gh release view "v$(node -e \"const fs=require('fs');process.stdout.write(JSON.parse(fs.readFileSync('src/app/wails.json','utf8')).info.productVersion)\")" --json url,assets && curl -I -L https://github.com/marcfargas/go-mapi/releases/latest/download/go-mapi-setup.exe</automated>
  </verify>
  <done>
    The release is published through the existing workflow, the stable installer URL works, and the proof bundle records both release and store-retirement evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo metadata → GitHub release workflow | Shipping version and release notes drive the public release output |
| Human browser-store actions → evidence file | External dashboard changes must be recorded back into the repo as proof |
| Final tag push → public GitHub Release | Release action exposes the installer publicly at the stable URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | Shipping version/tag mismatch | mitigate | Use `wails.json` as the only version authority and let the existing release workflow validate the tag match. |
| T-11-04-02 | Repudiation | Store-retirement proof | mitigate | Require screenshots/URLs in `11-RELEASE-EVIDENCE.md` before GA. |
| T-11-04-03 | Information disclosure | Release notes/docs | mitigate | Keep release docs v3-only and avoid publishing secrets, internal tokens, or debug-only links. |
| T-11-04-04 | Denial of service | Missing store proof blocks release | accept | This is an intentional blocking checkpoint because D-12 requires evidence. |
</threat_model>

<verification>
Use the automated command in Task 3 only after release approval and after `11-05` records a passing pre-GA clean-machine rehearsal; then manually inspect `11-RELEASE-EVIDENCE.md` for both store proof entries.
</verification>

<success_criteria>
- README is fully v3-only.
- Release notes use strong v2-retired cutover language.
- `wails.json` contains the shipping version.
- Stable installer URL works after the GA release.
- GA release occurs only after the pre-GA clean-machine rehearsal passes.
- Browser-store deprecation/freeze proof is captured in-repo.
</success_criteria>

<output>
After completion, create `.planning/phases/11-autoupdate-release/11-04-SUMMARY.md`.
</output>
