---
phase: 06-changesets-monorepo-scaffold
plan: 01
subsystem: versioning
tags: [changesets, npm-workspaces, monorepo, versioning]
dependency_graph:
  requires: []
  provides: [changesets-config, native-host-workspace, workspace-versions]
  affects: [package.json, src/extension/package.json, src/native-host/package.json, package-lock.json]
tech_stack:
  added: ["@changesets/cli@^2.30.0"]
  patterns: [npm-workspaces, changesets-private-packages]
key_files:
  created:
    - .changeset/config.json
    - .changeset/README.md
    - src/native-host/package.json
  modified:
    - package.json
    - src/extension/package.json
    - package-lock.json
decisions:
  - "privatePackages.tag=true enables go-mapi-extension@X.Y.Z and go-mapi-host@X.Y.Z git tags without npm publish"
  - "commit=false in changeset config — GitHub Action owns commits to avoid double-commit"
  - "Root version field removed entirely (D-02) — per-package package.json is now the sole version authority"
  - "Both packages start at 2.1.0 (D-01) — clean start for v2.1.0 milestone"
metrics:
  duration: "3 minutes"
  completed: "2026-04-12"
  tasks_completed: 2
  files_created: 3
  files_modified: 3
requirements: [CS-01, CS-02, CS-03]
---

# Phase 6 Plan 1: Changesets Monorepo Scaffold Summary

**One-liner:** npm workspace with two private packages (extension + host) at 2.1.0, changesets configured with privatePackages git tagging enabled.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Install @changesets/cli and configure changesets | 58bfabe | .changeset/config.json, .changeset/README.md, package.json, package-lock.json |
| 2 | Create native-host workspace stub and update workspaces | 9c83596 | src/native-host/package.json, src/extension/package.json, package.json, package-lock.json |

## What Was Built

- **`.changeset/config.json`** — Changesets configuration with `privatePackages.tag: true` (creates `go-mapi-extension@X.Y.Z` / `go-mapi-host@X.Y.Z` git tags), `baseBranch: main`, `commit: false`. Without `privatePackages.tag: true` no git tags would be created for private packages (verified pitfall from research).
- **`.changeset/README.md`** — Auto-generated by `changeset init`; explains developer workflow.
- **`src/native-host/package.json`** — Minimal private workspace stub: `name: go-mapi-host`, `version: 2.1.0`, `private: true`, no scripts or dependencies (Go modules handle everything).
- **Root `package.json`** — `version` field removed (D-02); `src/native-host` added to `workspaces` array alongside `src/extension`.
- **`src/extension/package.json`** — Version bumped from 2.0.1 to 2.1.0 (D-01).
- **`package-lock.json`** — Regenerated with both workspaces resolved; `npm ci` exits 0.

## Verification Results

| Check | Result |
|-------|--------|
| `jq '.privatePackages.tag' .changeset/config.json` | `true` |
| `jq '.version' src/native-host/package.json` | `"2.1.0"` |
| `jq '.version' src/extension/package.json` | `"2.1.0"` |
| `jq '.version' package.json` | `null` (field removed) |
| `npm ci --ignore-scripts` | exits 0 |

## Deviations from Plan

None — plan executed exactly as written.

The only minor note: `src/extension/package.json` was at `2.0.1` (post-hotfix) rather than `2.0.0` as stated in the plan's context section. The action was correct regardless — the plan said bump to `2.1.0`, which was done. No deviation.

## Known Stubs

None — no placeholder text or empty data sources introduced. The `src/native-host/package.json` stub is intentional (no npm publish planned; changesets uses it for version tracking only).

## Threat Flags

None. No new network endpoints, auth paths, or file access patterns introduced. The only external boundary is npm registry install of `@changesets/cli`, which is covered by T-06-01/T-06-02 in the plan's threat model (accepted via lockfile integrity).

## Self-Check: PASSED

- `.changeset/config.json` — FOUND
- `.changeset/README.md` — FOUND
- `src/native-host/package.json` — FOUND
- Commit `58bfabe` — FOUND
- Commit `9c83596` — FOUND
