---
phase: 08.1-post-pivot-cleanup-and-test-coverage-review
plan: 07
subsystem: infra
tags: [ci, github-actions, go-1.25, race-detector, coverage, svelte-check, vitest, wails]

# Dependency graph
requires:
  - phase: 08.1
    provides: Plan 02 (Vitest + svelte-check scaffold), Plan 03 (Go test coverage), Plan 06 (deletion of e2e + extension + native-host workflows clearing CI surface)
provides:
  - Per-PR `go test -race` blocking gate on `./internal/mapi/...` and `./src/app/...` (closes QUAL-04)
  - Per-PR coverage artifacts (one per Go module, report-only, no threshold)
  - Per-PR `svelte-check` blocking gate (closes D-10)
  - Per-PR Vitest blocking gate (closes D-10)
  - Per-PR `wails build` sanity step
  - Nightly `-race` defence-in-depth on Go 1.25 + new module scope
affects: [09-feature-work, 10-installer, 11-release]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CI ordering — frontend build before any go vet/test on src/app/... (because src/app/main.go uses //go:embed all:frontend/dist)"
    - "Per-module coverage profiles uploaded as separate artifacts (coverage-internal-mapi, coverage-src-app) — no threshold gating per CONTEXT D-08"
    - "CGO_ENABLED=1 at job level for the -race detector on Windows runners"

key-files:
  created: []
  modified:
    - .github/workflows/build.yml
    - .github/workflows/go-race-nightly.yml

key-decisions:
  - "Wails CLI installed via `go install ...@latest` rather than pinned — matches PATTERNS.md guidance; can pin later if upstream breaks Go 1.25 build"
  - "wails build uses explicit `-platform windows/amd64` for CI determinism (project is Windows-only)"
  - "Frontend coverage deliberately not enforced (D-08 covers Go only); no @vitest/coverage-v8 wired"
  - "go-race-nightly.yml preamble updated to call out per-PR gate; nightly retained as defence-in-depth (rather than deleted) per CONTEXT D-07"

patterns-established:
  - "CI step ordering for embed-dependent Go packages: npm ci → frontend build → go vet → go test"
  - "Per-module coverage artifact naming: coverage-{module-slug}.out → upload-artifact name coverage-{module-slug}"

requirements-completed: [QUAL-04]

# Metrics
duration: ~5min
completed: 2026-04-18
---

# Phase 08.1 Plan 07: CI workflow rewrite — `build-wails-app` + nightly bump Summary

**Per-PR `-race` + per-module coverage + svelte-check + Vitest + `wails build` all wired into a single `build-wails-app` job; `build-interceptor` preserved verbatim; nightly bumped to Go 1.25 + new module scope.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-18T23:51:00Z (approximate, pre-commit)
- **Completed:** 2026-04-18T21:56:56Z (UTC of last commit `2fa200a`)
- **Tasks:** 2 of 3 in plan (Task 3 was "must-hold invariant + commit"; the per-task commits already happened atomically in Tasks 1 + 2; the parallel-execution context for this worktree explicitly waived running the full build pipeline because this plan edits CI YAML only)
- **Files modified:** 2

## Accomplishments

- `.github/workflows/build.yml` rewritten: deleted `build-native-host` + `build-extension`; added `build-wails-app` (windows-latest, CGO_ENABLED=1, Go 1.25, Node 20).
- `build-wails-app` runs the full per-PR gate sequence in load-bearing order: `npm ci` → frontend build → `go vet` → `go test -race -coverprofile` for both modules → upload coverage artifacts → `svelte-check` → Vitest → `wails build` → upload Wails binary artifact.
- `build-interceptor` preserved verbatim (C++ MAPI DLL job is not touched in this phase).
- `.github/workflows/go-race-nightly.yml` bumped Go 1.21 → 1.25, scope changed from `working-directory: src/native-host` + `./...` → explicit `./internal/mapi/... ./src/app/...`. Preamble updated to note nightly is now defence-in-depth alongside the per-PR gate.
- QUAL-04 effectively closed: next PR run executes `-race` + vet + build + test as blocking steps.

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite build.yml with build-interceptor + new build-wails-app** — `b3e9464` (ci)
2. **Task 2: Update go-race-nightly.yml — Go 1.25 + new module scope** — `2fa200a` (ci)

Task 3 ("must-hold invariant + commit") was effectively merged into the per-task commits above. The plan's Task 3 prescribed (a) running the full local must-hold-invariant suite and (b) producing a single combined commit. The parallel-execution context for this worktree explicitly waived (a) ("Plan 07 edits CI YAML only — no runtime code change. The must-hold invariant is automatic. You do not need to run the full build pipeline, but DO run yamllint or equivalent if available."), and the per-task atomic-commit policy from the parallel-execution context overrides (b). YAML validation was performed via `js-yaml` (yamllint not available on this runner) for both files; both parsed clean.

**Plan metadata:** SUMMARY.md commit follows this file write.

## Files Created/Modified

- `.github/workflows/build.yml` — Rewritten. Two top-level jobs: `build-interceptor` (preserved verbatim from prior version) and the new `build-wails-app` (Go 1.25 race + per-module coverage + svelte-check + Vitest + wails build, all on windows-latest with CGO_ENABLED=1). Frontend build runs before any Go work because `src/app/main.go` embeds `frontend/dist` via `//go:embed`.
- `.github/workflows/go-race-nightly.yml` — Modified. `go-version` 1.21 → 1.25; removed `working-directory: src/native-host`; scope `./...` → `./internal/mapi/... ./src/app/...`. Preamble comment block updated to reflect the new role as defence-in-depth behind the per-PR gate.

## Decisions Made

- **Wails CLI install:** used `go install github.com/wailsapp/wails/v2/cmd/wails@latest` per PATTERNS.md. Acknowledged in plan that this can be pinned later if upstream churn breaks Go 1.25 builds.
- **Wails build flag:** explicit `-platform windows/amd64` for CI determinism (project is Windows-only; the flag avoids any host-default ambiguity).
- **No frontend coverage:** intentionally omitted `@vitest/coverage-v8` and any frontend coverage upload — D-08 scopes coverage to Go only.
- **Nightly preamble rewrite:** updated comment block in `go-race-nightly.yml` to reflect that the nightly now backs up a per-PR gate (rather than being the sole `-race` runner). Single-line scope and step name updated to mirror the new module set.

## Deviations from Plan

### Auto-fixed Issues

None.

### Methodology adjustments

**1. [Parallel-execution context override] Per-task commits instead of single Task-3 combined commit**
- **Found during:** Task 1 / Task 2 execution
- **Issue:** Plan Task 3 prescribed a single combined commit at the end; the parallel-execution context for this worktree mandates per-task atomic commits and waives running the full local build pipeline.
- **Fix:** Committed `build.yml` after Task 1 (`b3e9464`) and `go-race-nightly.yml` after Task 2 (`2fa200a`). Did not run `go build` / `go test` / `npm run build` locally; performed YAML validation via `js-yaml` (yamllint not available).
- **Files affected:** None beyond the two already in scope.
- **Verification:** Both YAML files parse via `js-yaml`; `build.yml` resolves to exactly `{build-interceptor, build-wails-app}`; `go-race-nightly.yml` resolves with Go 1.25 + the new scope.
- **Committed in:** `b3e9464`, `2fa200a` (Task commits)

---

**Total deviations:** 0 auto-fixed (1 methodology adjustment driven by the worktree's parallel-execution context, not by code findings)
**Impact on plan:** None. All plan-level acceptance criteria for Task 1 and Task 2 are met; the must-hold invariant is automatic for YAML-only edits.

## Issues Encountered

- `python -c "import yaml; ..."` from the plan's `<verify>` blocks failed because PyYAML is not installed on this runner; substituted with `node -e "const yaml=require('js-yaml'); ..."` (already a transitive dep via the frontend toolchain). Both files parsed cleanly.
- `yq` not on PATH on this Git-Bash-on-Windows runner; not needed since `js-yaml` covers the same parse + key-extract checks.
- `yamllint` not available; no substitute lint was run beyond parser validation.

## Threat Flags

None. The two files modified are CI workflow YAML; the threat register in PLAN.md (T-08.1-07-01..04) covers all changes (`wails@latest` supply-chain, coverage artifact disclosure, QUAL-04 regression bypass mitigation, svelte-check regression mitigation). No new trust boundary introduced.

## Known Stubs

None. CI YAML is the final shipped form; no placeholder values, no TODO markers.

## User Setup Required

None — no external service configuration required for CI workflow changes. (The `build-wails-app` job runs exclusively on `windows-latest` GitHub-hosted runners using public actions; no secrets are referenced.)

## Next Phase Readiness

- QUAL-04 closure proven on next PR: `-race` + vet + build + test execute as blocking steps.
- D-08 coverage artifacts uploaded per workflow run; no threshold gating (deliberate per spec).
- D-10 svelte-check + Vitest blocking; frontend can no longer ship TypeScript regressions undetected.
- CI workflow surface reduced to three files total: `build.yml`, `go-race-nightly.yml`, `version-packages.yml` — matches PATTERNS.md §Wave 7 final state.
- Plans 08 (CLAUDE.md + README.md rewrite) and 09 (root config trims) can safely modify root config + docs without risking CI green, because the workflow files are now decoupled from the deleted v2.x sources.

## Self-Check: PASSED

Verified:
- `.github/workflows/build.yml` exists and contains exactly two jobs: `build-interceptor` + `build-wails-app` (validated via `js-yaml`).
- `.github/workflows/go-race-nightly.yml` exists and contains `go-version: '1.25'` + `go test -race -v ./internal/mapi/... ./src/app/...` (validated via `js-yaml`).
- Commit `b3e9464` exists in `git log` (Task 1).
- Commit `2fa200a` exists in `git log` (Task 2).
- No references to `build-native-host` or `build-extension` jobs in either workflow file.

---
*Phase: 08.1-post-pivot-cleanup-and-test-coverage-review*
*Completed: 2026-04-18*
