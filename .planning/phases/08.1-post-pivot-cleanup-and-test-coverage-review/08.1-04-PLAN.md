---
phase: 08.1-post-pivot-cleanup-and-test-coverage-review
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - src/native-host
  - go.work
  - go.work.sum
  - package.json
  - package-lock.json
  - .changeset/config.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/native-host/ directory does not exist after this plan"
    - "go.work uses only ./internal/mapi and ./src/app (no ./src/native-host entry)"
    - "root package.json workspaces array is ['src/extension','src/app'] (native-host removed; extension still present — Plan 05 removes it)"
    - "root package.json no longer contains scripts: build:native-host, build:native-host:debug, test:native-host, clean:native-host"
    - "root package.json aggregated scripts (build, test, clean) no longer chain native-host steps"
    - ".changeset/config.json $workspaces array does not contain 'src/native-host'"
    - "go build ./src/app/... ./internal/mapi/... exits 0"
    - "go test ./src/app/... ./internal/mapi/... exits 0 (including ported gmail_test and mime_golden_test from Plan 01)"
    - "npm run build:interceptor still succeeds"
    - "npm run -w @marcfargas/go-mapi-app-frontend build still succeeds"
  artifacts:
    - path: "go.work"
      provides: "Go workspace referencing only internal/mapi and src/app"
      contains: "use (\n\t./internal/mapi\n\t./src/app\n)"
  key_links:
    - from: "go.work"
      to: "src/app and internal/mapi only"
      via: "use block"
      pattern: "use.*internal/mapi.*src/app"
---

<objective>
Execute cleanup wave 1 per CONTEXT D-02: hard-delete `src/native-host/` and remove every reference to it in top-level config (`go.work`, root `package.json` workspaces + scripts, `.changeset/config.json`). The v2.1.x git tag is the archive — no copies preserved in-tree (D-01).

Purpose: The Wails pivot replaced the native messaging host with in-process Go. Keeping `src/native-host/` makes the repo misleading (CLAUDE.md still references it), inflates CI time, and risks divergence between duplicate `gmail_test.go` / `protocol_test.go` copies. Plan 01 ported the unique test cases to `internal/mapi/` so deletion is now lossless.

Output:
- `src/native-host/` gone
- `go.work` trimmed + `go work sync` applied
- `package.json` stripped of native-host workspace + 4 scripts + updated build/test/clean aggregator scripts
- `.changeset/config.json` `$workspaces` stripped of `"src/native-host"`
- Four must-hold-invariant commands all still green
</objective>

<execution_context>
@C:/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-CONTEXT.md
@.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-PATTERNS.md
@go.work
@package.json
@.changeset/config.json

<interfaces>
<!-- Concrete current-state references. Confirm at execution. -->

Current `go.work`:
```
go 1.25.0

use (
	./internal/mapi
	./src/app
	./src/native-host
)
```

Target `go.work`:
```
go 1.25.0

use (
	./internal/mapi
	./src/app
)
```

Current root `package.json` lines to remove:
- workspaces: `"src/native-host"` (line 7)
- script `build:native-host` (line 14)
- script `build:native-host:debug` (line 15)
- script `test:native-host` (line 21)
- script `clean:native-host` (line 28)
- aggregated `build` chain includes `build:native-host` (line 11) — edit to remove that step
- aggregated `test` chain includes `test:native-host` (line 20) — edit to remove that step
- aggregated `clean` chain includes `clean:native-host` (line 26) — edit to remove that step

Current `.changeset/config.json` line to remove:
- `"src/native-host"` entry in `$workspaces` array (line 17)

Plan 05 will remove the extension bits; this plan ONLY removes native-host.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight landmine checks — verify Plan 01 ported tests, no orphan consumers of src/native-host</name>
  <files></files>
  <read_first>
    - `internal/mapi/gmail_test.go` must exist (Plan 01 output).
    - `internal/mapi/mime_golden_test.go` must exist.
    - `internal/mapi/testdata/mime/` must contain 6 golden files.
    - `src/native-host/*.go` — list all files; note any non-test Go files.
    - `tests/fixtures/` and `tests/sandbox/` — PATTERNS.md confirmed NO references from src/native-host/*.go via grep; verify this is still true.
    - `.changeset/` directory — list any pending `.md` changesets.
  </read_first>
  <action>
    Run these checks. If any fails, STOP and surface the blocker:

    1. **Ported-tests check (Plan 01 output):**
       ```
       test -f internal/mapi/gmail_test.go || echo "MISSING"
       test -f internal/mapi/mime_golden_test.go || echo "MISSING"
       ls internal/mapi/testdata/mime/*.golden | wc -l   # must be 6
       ```
       If MISSING, ABORT — re-run Plan 01 first.

    2. **Ported tests pass:**
       ```
       go test -run 'TestGmailClient_CreateDraft|TestBuildFullMIME_Golden' ./internal/mapi/...
       ```
       Must exit 0.

    3. **No external Go consumers of src/native-host:**
       Grep for `marcfargas/go-mapi/src/native-host` across the repo:
       ```
       grep -r 'marcfargas/go-mapi/src/native-host' --include '*.go' --include 'go.mod'
       ```
       Must return no matches (nothing imports the native-host module).

    4. **Pending changesets don't reference native-host:**
       ```
       ls .changeset/*.md 2>/dev/null | grep -v README.md
       # For each result, grep -l 'go-mapi-native-host\|native-host' that file
       ```
       If any pending changeset file references `go-mapi-native-host`, STOP and surface to user — stale changesets will fail `changeset version` after the workspace trim.

    5. **tests/sandbox + scripts/test-drop-email.ps1 don't invoke the native-host binary:**
       ```
       grep -l 'go-mapi-host\|native-host' scripts/*.ps1 tests/sandbox/*.ps1 2>/dev/null
       ```
       Must return no matches (PATTERNS.md already confirmed; re-verify).

    6. **Record a baseline must-hold-invariant pass BEFORE mutations:**
       ```
       npm run build:interceptor
       go build ./src/app/... ./internal/mapi/...
       go test ./src/app/... ./internal/mapi/...
       npm run -w @marcfargas/go-mapi-app-frontend build
       ```
       All four must exit 0. If any fail, the baseline is broken — fix first, don't proceed with deletion.
  </action>
  <acceptance_criteria>
    - `internal/mapi/gmail_test.go` exists.
    - `internal/mapi/mime_golden_test.go` exists.
    - 6 `.golden` files at `internal/mapi/testdata/mime/`.
    - `go test -run 'TestGmailClient_CreateDraft|TestBuildFullMIME_Golden' ./internal/mapi/...` exits 0.
    - No files in the repo import `github.com/marcfargas/go-mapi/src/native-host`.
    - No pending changesets reference `go-mapi-native-host`.
    - No PowerShell scripts invoke `go-mapi-host.exe`.
    - Must-hold invariant baseline passes.
  </acceptance_criteria>
  <verify>
    <automated>go test -run 'TestGmailClient_CreateDraft|TestBuildFullMIME_Golden' ./internal/mapi/... -count=1</automated>
  </verify>
  <done>All landmines checked; ported tests exist and pass; baseline green; safe to proceed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete src/native-host/ directory and run go work sync</name>
  <files>
    src/native-host
    go.work
    go.work.sum
  </files>
  <read_first>
    - `go.work` — current `use` block contains `./src/native-host` (verified above).
    - `go.work.sum` — exists as a sibling of `go.work`.
  </read_first>
  <action>
    1. Delete the entire `src/native-host/` directory (recursive, force):
       - Bash: `rm -rf src/native-host`
       - PowerShell: `Remove-Item -Recurse -Force src/native-host`
       The directory and all contents (go.mod, go.sum, *.go, testdata/, manifests/, build/, package.json) go in one stroke. No archival copy.

    2. Edit `go.work` — remove the `./src/native-host` line from the `use (...)` block. Final file content:
       ```
       go 1.25.0

       use (
       	./internal/mapi
       	./src/app
       )
       ```
       (Tabs, not spaces — match the existing file's indentation style.)

    3. Run `go work sync` from the repo root. This regenerates `go.work.sum` to reflect only the remaining two modules.

    4. If `go work sync` errors due to a stale `go.work.sum` entry, inspect and delete obsolete sum-file entries for the native-host module; re-run `go work sync` until clean.

    5. Do NOT commit `go.work.sum` orphan lines — `git diff go.work.sum` should only remove lines, never add unexpected ones.
  </action>
  <acceptance_criteria>
    - `test -d src/native-host` returns false (directory gone).
    - `ls src/native-host 2>&1 | grep -i 'No such'` or PowerShell equivalent confirms absence.
    - `go.work` contains exactly `./internal/mapi` and `./src/app` in its `use` block, no `./src/native-host`.
    - `grep -q 'native-host' go.work` returns non-zero.
    - `go work sync` exits 0.
    - `go build ./src/app/... ./internal/mapi/...` exits 0.
    - `go test ./src/app/... ./internal/mapi/...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test ! -d src/native-host && grep -q '^use ($' go.work && ! grep -q 'native-host' go.work && go build ./src/app/... ./internal/mapi/...</automated>
  </verify>
  <done>src/native-host gone; go.work trimmed; go.work.sum synced; Go build + Go test still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Trim root package.json — remove native-host workspace, scripts, and aggregator chains</name>
  <files>
    package.json
    package-lock.json
  </files>
  <read_first>
    - Current `package.json` in full (root-level). Note the exact `workspaces` array, every script that starts with `native-host` or chains to one, and the aggregated `build`/`test`/`clean` scripts.
    - PATTERNS.md §`package.json (MODIFY — waves 1, 2, 4, 5, 9)` — the cumulative end-state after ALL waves; this plan only applies wave-1 changes.
  </read_first>
  <action>
    Edit `package.json`:

    1. **workspaces array** — remove `"src/native-host"`. Result:
       ```json
       "workspaces": [
         "src/extension",
         "src/app"
       ]
       ```
       (Plan 05 removes `"src/extension"`.)

    2. **Remove scripts** (exact entries):
       - `"build:native-host": "..."` (current line 14)
       - `"build:native-host:debug": "..."` (current line 15)
       - `"test:native-host": "..."` (current line 21)
       - `"clean:native-host": "..."` (current line 28)

    3. **Edit aggregator scripts** to drop native-host steps:
       - `"build": "npm run build:interceptor && npm run build:native-host && npm run build:extension"` → `"build": "npm run build:interceptor && npm run build:extension"` (Plan 05 removes the extension part).
       - `"test": "npm run test:native-host && npm run test:extension"` → `"test": "npm run test:extension"` (Plan 05 removes the rest and Plan 08 rebuilds this to the new Wails-era shape).
       - `"clean": "npm run clean:interceptor && npm run clean:native-host && npm run clean:extension"` → `"clean": "npm run clean:interceptor && npm run clean:extension"`.

    4. Do NOT touch:
       - `build:interceptor`, `build:interceptor:debug`, `clean:interceptor`, `test:interceptor` — all preserved (C++ DLL work).
       - Extension scripts (`build:extension`, `dev:extension`, `test:extension`, `package:extension`, `clean:extension`) — Plan 05 removes them.
       - E2E + install scripts — later plans.
       - The devDependencies block — Plan 05+ trims it.

    5. Run `npm install` from the repo root so `package-lock.json` reflects the updated workspace list (no devDeps changed, but the workspace list did). Stage both `package.json` AND `package-lock.json` in the commit per lockfile-discipline.md.

    6. Sanity check: `jq '.workspaces' package.json` returns `["src/extension","src/app"]`. `jq '.scripts' package.json | grep -c native-host` returns 0.
  </action>
  <acceptance_criteria>
    - `jq -r '.workspaces | join(",")' package.json` returns exactly `src/extension,src/app`.
    - `jq '.scripts' package.json` does NOT include any key matching `*native-host*`: `jq '.scripts | keys | map(select(contains("native-host"))) | length' package.json` returns 0.
    - `jq -r '.scripts.build' package.json` does NOT contain the string `native-host`.
    - `jq -r '.scripts.test' package.json` does NOT contain the string `native-host`.
    - `jq -r '.scripts.clean' package.json` does NOT contain the string `native-host`.
    - `npm install` exits 0.
    - `package-lock.json` staged alongside `package.json`.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.workspaces == ["src/extension","src/app"] and ([.scripts | keys[] | select(contains("native-host"))] | length == 0) and (.scripts.build | contains("native-host") | not) and (.scripts.test | contains("native-host") | not) and (.scripts.clean | contains("native-host") | not)' package.json</automated>
  </verify>
  <done>package.json has no native-host references anywhere; package-lock.json regenerated in the same commit; `npm install` succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Trim .changeset/config.json — remove "src/native-host" from $workspaces</name>
  <files>
    .changeset/config.json
  </files>
  <read_first>
    - Current `.changeset/config.json` in full — verify `$workspaces` contains `"src/native-host"` at the current line 17.
  </read_first>
  <action>
    Edit `.changeset/config.json`:

    1. Remove the string `"src/native-host"` from the `$workspaces` array. Result:
       ```json
       "$workspaces": [
         "src/extension",
         "src/app"
       ]
       ```
       (Plan 05 removes `"src/extension"`; Plan 08 finalizes to `["src/app"]` per D-20.)

    2. Do NOT change any other field (`changelog`, `commit`, `baseBranch`, `updateInternalDependencies`, `privatePackages`, etc.).
  </action>
  <acceptance_criteria>
    - `jq -r '.["$workspaces"] | join(",")' .changeset/config.json` returns exactly `src/extension,src/app`.
    - `jq -r '.baseBranch' .changeset/config.json` still returns `main` (unchanged).
    - `jq -r '.changelog' .changeset/config.json` still returns `@changesets/cli/changelog`.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.["$workspaces"] == ["src/extension","src/app"]' .changeset/config.json</automated>
  </verify>
  <done>.changeset/config.json has no `src/native-host` entry; all other fields preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Must-hold invariant re-verification + commit</name>
  <files></files>
  <read_first>
    - CONTEXT §Specifics "must-hold invariant" — the four commands that MUST still pass.
  </read_first>
  <action>
    Run the four must-hold-invariant commands after all mutations:

    1. `npm run build:interceptor` — C++ DLL builds.
    2. `go build ./src/app/... ./internal/mapi/...` — Go builds (native-host module gone from workspace).
    3. `go test ./src/app/... ./internal/mapi/...` — Go tests pass (ported gmail_test + mime_golden_test from Plan 01 run here, plus all Plan 03 additions).
    4. `npm run -w @marcfargas/go-mapi-app-frontend build` — Svelte frontend builds.

    All four must exit 0. If any fail, diagnose; do NOT commit.

    Commit with message: `chore(08.1): drop src/native-host and update workspace config` (per D-02 + CONTEXT commit-style note).

    Stage:
    - `src/native-host/` (deletions)
    - `go.work`, `go.work.sum`
    - `package.json`, `package-lock.json`
    - `.changeset/config.json`

    Do NOT stage coverage output files or any other test byproducts.
  </action>
  <acceptance_criteria>
    - All four must-hold-invariant commands exit 0.
    - `git status` shows only the expected staged changes (deletion of src/native-host, and edits to go.work + go.work.sum + package.json + package-lock.json + .changeset/config.json).
    - `git log -1 --pretty=%B` (after commit) matches `chore(08.1): drop src/native-host and update workspace config` or a close variant.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:interceptor && go build ./src/app/... ./internal/mapi/... && go test ./src/app/... ./internal/mapi/... && npm run -w @marcfargas/go-mapi-app-frontend build</automated>
  </verify>
  <done>Wave 1 cleanup committed in one atomic commit; repo still builds and tests green end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan deletes code and trims config — it does NOT introduce new network surfaces, auth paths, or data handling. The only risk is accidentally breaking the build or losing test coverage.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-04-01 | D (denial-of-service via broken CI) | `go.work` + `package.json` edits | mitigate | Task 1 runs full must-hold-invariant as a baseline BEFORE mutations; Task 5 re-runs all four after mutations. Any failure aborts the commit. |
| T-08.1-04-02 | I (lost test coverage on delete) | `src/native-host/gmail_test.go`, `mime_golden_test.go` | mitigate | Plan 01 ported both files + 6 golden fixtures to `internal/mapi/` before this plan. Task 1 verifies the ports exist and pass before the delete. |
| T-08.1-04-03 | T (stale changeset tampering) | `.changeset/*.md` | mitigate | Task 1 checks for pending changesets referencing `go-mapi-native-host`; if any exist, the plan stops and surfaces to the user (stale changeset + workspace removal = `changeset version` fails). |
</threat_model>

<verification>
- `test ! -d src/native-host` (directory removed).
- `! grep -q 'native-host' go.work` (workspace entry removed).
- `! jq -r '.scripts | keys | .[]' package.json | grep -q native-host` (scripts removed).
- `! jq -r '.["$workspaces"] | .[]' .changeset/config.json | grep -q native-host` (changeset config cleaned).
- All four must-hold-invariant commands exit 0.
- Plan-01-ported tests (`TestGmailClient_CreateDraft`, `TestBuildFullMIME_Golden`) still pass inside `internal/mapi/`.
</verification>

<success_criteria>
- `src/native-host/` is gone; the v2.1.x git tag is the archive (D-01 honored).
- Every top-level config file accurately reflects the two-module Go workspace + two-workspace npm monorepo (extension still present — Plan 05 owns it).
- Must-hold invariant preserved — Plan 05 has a clean baseline to build on.
- Test coverage preserved via Plan 01 ports — no silent loss of business-logic test cases.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-04-SUMMARY.md`.
</output>
