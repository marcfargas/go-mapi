---
phase: 08.1-post-pivot-cleanup-and-test-coverage-review
plan: 08
type: execute
wave: 6
depends_on: ["07"]
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "root package.json has a top-level `description` reflecting the Wails desktop app (not the v2.x MAPI-to-Gmail-bridge wording) OR remains factually accurate — planner audits"
    - "root package.json gains convenience aggregator scripts wiring Go + frontend test for dev ergonomics (optional but recommended per PATTERNS.md §package.json final)"
    - "root package.json has no extension/native-host/e2e residue anywhere"
    - "package-lock.json is in sync (npm install exits 0 with no changes needed)"
    - "Must-hold invariant holds"
  artifacts:
    - path: "package.json"
      provides: "Final Wails-era root manifest with accurate description, workspaces=['src/app'], devDeps={@changesets/cli}, and optional aggregator test/build scripts for dev ergonomics"
  key_links:
    - from: "package.json"
      to: "new dev ergonomics"
      via: "aggregator scripts"
      pattern: "\"test\":.*go test.*internal/mapi.*src/app.*frontend.*test:run"
---

<objective>
Finalize the root `package.json` shape for the Wails-era monorepo. This is the last config-file polish pass before the docs rewrite in Plan 09. Plans 04-06 removed all extension/native-host/e2e/installer residue; this plan rewrites the `description`, adds optional Wails-era aggregator scripts (per PATTERNS.md §package.json "New aggregate commands"), and performs a final audit that every field reflects the v3.0 reality.

Purpose: The root `package.json` description still says "MAPI to Gmail bridge for Windows - send emails via Gmail from any Windows application". Accurate, but Plan 09 will rewrite README/CLAUDE.md to emphasize the Wails desktop app — this plan aligns package.json wording so dev tooling (npm info, CI badges) describes the current architecture. Also adds dev-ergonomics aggregators so `npm run build` does something useful post-cleanup (Plan 05 reduced it to just interceptor).

Output:
- `package.json` final-shape audit: description updated if drift detected, aggregator scripts added for `build` (interceptor + wails), `test` (Go + frontend Vitest), `check` (svelte-check convenience), plus a final-form verification
- `package-lock.json` regenerated if any field change triggers lockfile touch
</objective>

<execution_context>
@C:/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-CONTEXT.md
@.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-PATTERNS.md
@package.json

<interfaces>
<!-- Expected current state after Plan 06 (confirm at execution). -->

`package.json` after Plan 06 should look approximately:
```json
{
  "name": "go-mapi",
  "description": "MAPI to Gmail bridge for Windows - send emails via Gmail from any Windows application",
  "private": true,
  "workspaces": ["src/app"],
  "scripts": {
    "build": "npm run build:interceptor",
    "build:interceptor": "powershell -ExecutionPolicy Bypass -File src/interceptor/build.ps1 -Config Release",
    "build:interceptor:debug": "powershell -ExecutionPolicy Bypass -File src/interceptor/build.ps1 -Config Debug -Tests",
    "clean": "npm run clean:interceptor",
    "clean:interceptor": "powershell -ExecutionPolicy Bypass -Command \"Remove-Item -Recurse -Force src/interceptor/build -ErrorAction SilentlyContinue\"",
    "test:interceptor": "powershell -ExecutionPolicy Bypass -Command \"src/interceptor/build/bin/go-mapi-test-harness.exe src/interceptor/build/bin/go-mapi.dll\"",
    "test:dll:sandbox": "powershell -ExecutionPolicy Bypass -File tests/sandbox/run-sandbox-test.ps1"
  },
  "repository": { ... },
  "keywords": [ ... ],
  "author": "",
  "license": "LGPL-3.0-or-later",
  "engines": { "node": ">=18.0.0" },
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  }
}
```

Target final shape:
- `description`: rewrite to describe the Wails desktop app (see Task 1 for exact wording).
- `scripts`: add Wails-era aggregator convenience scripts per PATTERNS.md.
- Everything else: keep.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit and finalize root package.json — description, aggregator scripts, repository URL sanity</name>
  <files>
    package.json
    package-lock.json
  </files>
  <read_first>
    - Current root `package.json` (post Plan 06).
    - PATTERNS.md §`package.json (MODIFY — waves 1, 2, 4, 5, 9)` — cumulative result + "New aggregate commands (optional convenience, not required)".
    - CONTEXT D-22 — "workspaces final: `['src/app']` — planner verifies which nesting npm resolves correctly". (Plan 05 set to `['src/app']`; confirm this resolves the frontend workspace name correctly.)
    - CLAUDE.md current `"three-language stack"` description — PATTERNS.md §CLAUDE.md confirms this is stale (Plan 09 rewrites).
  </read_first>
  <action>
    1. **Audit current `description`:**
       Current value: `"MAPI to Gmail bridge for Windows - send emails via Gmail from any Windows application"`.
       Evaluation: accurate but lacks Wails-era framing. Replace with:
       ```
       "MAPI to Gmail bridge for Windows — a Wails (Go + WebView2) desktop app that routes legacy 'Send to Mail recipient' calls to Gmail as drafts"
       ```
       (Unicode em-dash is fine; copy uses the same em-dash as README/PROJECT.md for consistency.)

    2. **Audit `repository.url`:**
       Current value: `"https://github.com/anthropics/go-mapi.git"`. WRONG — the project is `marcfargas/go-mapi` (verified via CLAUDE.md Project section + package-lock references). Change to:
       ```json
       "repository": {
         "type": "git",
         "url": "https://github.com/marcfargas/go-mapi.git"
       }
       ```

    3. **Add Wails-era aggregator scripts** (PATTERNS.md §New aggregate commands):
       Replace `"build"` with:
       ```
       "build": "npm run build:interceptor && cd src/app && wails build -platform windows/amd64"
       ```
       (Requires wails CLI on PATH — fine for dev machines; CI handles it explicitly in build.yml.)

       Add a new `"test"` script. The frontend build MUST run before `go test ./src/app/...` because `src/app/main.go` uses `//go:embed all:frontend/dist` — on a clean working tree `src/app/frontend/dist/` may not exist, which would make `go test ./src/app/...` fail to compile:
       ```
       "test": "npm run -w @marcfargas/go-mapi-app-frontend build && go test ./internal/mapi/... ./src/app/... && npm run -w @marcfargas/go-mapi-app-frontend test:run"
       ```

       Add a new `"check"` script:
       ```
       "check": "go vet ./internal/mapi/... ./src/app/... && npm run -w @marcfargas/go-mapi-app-frontend check"
       ```

       These mirror the CI gates from Plan 07 so developers can reproduce the per-PR gate locally with one command each.

       Preserve the existing interceptor + dll-sandbox scripts untouched.

    4. **Audit `keywords`:**
       Current: `["mapi","gmail","windows","email","google-workspace"]`. Still accurate; optionally add `"wails"` and `"desktop"` for discoverability. Low-stakes; planner's discretion.

    5. **Audit `author`:**
       Current: `""`. Per CLAUDE.md, Marc is the solo maintainer. Update to:
       ```json
       "author": "Marc Fargas <https://github.com/marcfargas>"
       ```
       (GitHub profile link rather than email — confidentiality.md rule + email privacy.)

    6. **Audit `license`:**
       Current: `"LGPL-3.0-or-later"`. Matches user global rule "Prefers LGPL-3.0 licensing" + PROJECT.md. Keep.

    7. **Audit `engines.node`:**
       Current: `">=18.0.0"`. Per Svelte 5 / Vite 6 / Vitest 2.x recommendations, a bump to `>=20.0.0` may be warranted. Check if the current CI (`actions/setup-node@v4` in `build.yml`) uses node-version 20 — if so, bump `engines.node` to `">=20.0.0"` to match.

    8. **Run `npm install`** after edits. If `package-lock.json` changes (e.g. if adding keywords/description doesn't touch lockfile, but the script changes don't either — they shouldn't touch lockfile), stage only what did change.

    9. **Sanity:** Run every aggregate script to confirm it works locally:
       - `npm run check` — go vet + svelte-check both green.
       - `npm run test` — go test + Vitest both green.
       - (Do NOT run `npm run build` — that requires Wails CLI installed; dev setup step.)

    10. **Do NOT add** `wails dev` convenience scripts — Marc uses `scripts/dev-wails.ps1` already (preserved). Plan 09's CLAUDE.md rewrite documents the dev workflow.
  </action>
  <acceptance_criteria>
    - `jq -r '.description' package.json` contains both "Wails" AND "MAPI" AND "Gmail".
    - `jq -r '.repository.url' package.json` contains `marcfargas/go-mapi.git`.
    - `jq -r '.repository.url' package.json` does NOT contain `anthropics`.
    - `jq -r '.scripts.test' package.json` contains `go test` AND `internal/mapi` AND `frontend`.
    - `jq -r '.scripts.test' package.json` contains the literal substring `frontend build && go test` (frontend build precedes go test so //go:embed all:frontend/dist resolves). Verifiable with: `grep -q "frontend build && go test" package.json`.
    - `jq -r '.scripts.check' package.json` contains `go vet` AND `svelte-check` OR the command invocation that runs svelte-check (e.g., `npm run -w @marcfargas/go-mapi-app-frontend check`).
    - `jq -r '.scripts.build' package.json` contains `build:interceptor` AND `wails build`.
    - `jq -r '.author' package.json` is non-empty.
    - `jq -r '.license' package.json` returns `LGPL-3.0-or-later`.
    - `jq '.workspaces' package.json` returns `["src/app"]`.
    - `jq '.devDependencies | keys' package.json` returns `["@changesets/cli"]` only.
    - `npm run check` exits 0.
    - `npm run test` exits 0.
    - `npm install` (after edits) exits 0 with minimal/no lockfile diff.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '(.description | contains("Wails")) and (.repository.url | contains("marcfargas/go-mapi")) and (.repository.url | contains("anthropics") | not) and (.scripts.test | contains("frontend build && go test")) and (.scripts.test | contains("test:run")) and (.scripts.check | contains("go vet")) and (.scripts.build | contains("wails build")) and (.workspaces == ["src/app"]) and ((.devDependencies | keys) == ["@changesets/cli"])' package.json</automated>
  </verify>
  <done>package.json finalized: accurate description, correct repo URL, Wails-era aggregator scripts, author field set, all other fields audited; npm run check + npm run test both green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Must-hold invariant + commit</name>
  <files></files>
  <read_first>
    - CONTEXT §Specifics "must-hold invariant".
  </read_first>
  <action>
    Run the four must-hold-invariant commands:

    1. `npm run build:interceptor` — C++ DLL builds (unchanged from Plan 05).
    2. `go build ./src/app/... ./internal/mapi/...` — Go builds.
    3. `go test ./src/app/... ./internal/mapi/...` — Go tests.
    4. `npm run -w @marcfargas/go-mapi-app-frontend build` — Svelte build.

    All four must exit 0.

    Additionally verify the new aggregators:
    - `npm run check` — exits 0.
    - `npm run test` (the new one) — exits 0.

    Commit with message: `chore(08.1): finalize root package.json for Wails-era monorepo`.

    Stage:
    - `package.json`
    - `package-lock.json` (if changed; usually unchanged by this plan's edits since no new deps)
  </action>
  <acceptance_criteria>
    - All four must-hold-invariant commands exit 0.
    - `npm run check` exits 0.
    - `npm run test` exits 0.
    - Commit touches only `package.json` (and possibly `package-lock.json`).
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run test</automated>
  </verify>
  <done>Root package.json is in its final Wails-era shape; every aggregator runs locally green; must-hold invariant holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No runtime boundaries introduced. Pure metadata + script aggregation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-08-01 | S (spoofing — wrong repo URL) | `repository.url` | mitigate | Explicitly correct from `anthropics/go-mapi` (the template default or accidental value) to `marcfargas/go-mapi`. Misleading repository URL in npm metadata is a minor spoofing risk if the package ever gets published; even as a private monorepo it affects `npm info` output. |
| T-08.1-08-02 | I (leaky author metadata) | `author` field | accept | GitHub profile URL used instead of email per privacy preference. Low risk — profile is public anyway. |
| T-08.1-08-03 | T (supply chain via scripts) | `npm run build` now invokes `wails` | accept | Wails CLI invocation is already covered by CI (build.yml Task 1 of Plan 07 pins `wails@latest`); local dev installs separately via `scripts/dev-wails.ps1` (untouched). No new supply-chain surface. |
</threat_model>

<verification>
- `package.json` has the target Wails-era shape per PATTERNS.md.
- Repository URL corrected.
- Description reflects v3.0 reality.
- Aggregator scripts (`test`, `check`, `build`) work locally.
- Must-hold invariant upheld.
- No extension/native-host/e2e/installer residue detectable via any `grep` against `package.json`.
</verification>

<success_criteria>
- The root monorepo manifest is coherent end-to-end with v3.0: description, repo URL, workspaces, devDeps, scripts all match the cleaned-up codebase.
- Developer ergonomics preserved: `npm run check` and `npm run test` are one-liner dev-loop reproductions of the CI gates from Plan 07.
- Plan 09 can rewrite CLAUDE.md + README.md with confidence that `package.json` references it cites are final/accurate.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-08-SUMMARY.md`.
</output>
