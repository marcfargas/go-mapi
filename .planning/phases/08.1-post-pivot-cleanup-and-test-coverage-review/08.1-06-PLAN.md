---
phase: 08.1-post-pivot-cleanup-and-test-coverage-review
plan: 06
type: execute
wave: 4
depends_on: ["05"]
files_modified:
  - tests/e2e
  - tests/fixtures
  - .github/workflows/e2e.yml
  - .github/workflows/release.yml
  - scripts/install.ps1
  - scripts/package-extension.ps1
  - docs/chrome-webstore-privacy-justifications.md
  - docs/chrome-webstore-testing-instructions.md
  - docs/webstore-description.md
  - docs/webstore-description.txt
  - TODO.txt
  - AGENTS.md
  - PLANNING_AGENT.md
  - CONTRIBUTING.md
  - package.json
  - package-lock.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "tests/e2e/ does not exist"
    - "tests/fixtures/ does not exist if no Wails test references it (confirmed orphan per PATTERNS.md since only src/native-host/ tests consumed it, and those were deleted in Plan 04)"
    - ".github/workflows/e2e.yml does not exist"
    - ".github/workflows/release.yml does not exist"
    - "scripts/install.ps1 does not exist (v2.x MAPI install script)"
    - "scripts/package-extension.ps1 does not exist"
    - "Four extension-era docs/chrome-webstore-* / docs/webstore-description.* files do not exist"
    - "TODO.txt (v1.0 Phase 1 log) does not exist"
    - "AGENTS.md + PLANNING_AGENT.md + CONTRIBUTING.md have been inspected and either updated (removing v2.x references) or deleted"
    - "docs/json-schema.json, docs/testing/, docs/measurements/ preserved unchanged (Wails-era artifacts per PATTERNS.md)"
    - "tests/protocol-fixtures/ + tests/sandbox/ preserved (consumed by internal/mapi and interceptor respectively)"
    - "root package.json no longer contains scripts: test:e2e, test:coverage, install:mapi, uninstall:mapi"
    - "root package.json devDependencies no longer contains @playwright/test; only @changesets/cli remains"
    - "Must-hold invariant all green after this plan"
  artifacts:
    - path: "package.json"
      provides: "Final devDependencies state for Plan 08 pickup: { '@changesets/cli': '^2.30.0' } only"
  key_links:
    - from: "package.json"
      to: "only @changesets/cli devDep"
      via: "devDependencies field"
      pattern: "devDependencies.*@changesets/cli"
---

<objective>
Execute cleanup waves 4+5+6 per CONTEXT D-02: delete `tests/e2e/` + `tests/fixtures/` (orphan after Plan 04), the Playwright + release CI workflows, v2.x PowerShell install scripts, extension-era docs, `TODO.txt`, and inspect-and-decide on `AGENTS.md` / `PLANNING_AGENT.md` / `CONTRIBUTING.md`. Drop `@playwright/test` devDep and `test:e2e` / `test:coverage` / `install:mapi` / `uninstall:mapi` scripts.

Purpose: With `src/extension/` and `src/native-host/` gone, the Playwright E2E tests cannot run (they test the extension + native messaging host), so the e2e directory + workflow + devDep are dead weight per D-13/D-14/D-16. `release.yml` (v2.x release pipeline) is owned by Phase 11 REL-02 per D-18. `scripts/install.ps1` was v2.x dev-install for the native host; nothing consumes it after Plan 04. Extension-era docs describe a retiring store listing per D-01 (no archive). Three repo-root docs (AGENTS.md, PLANNING_AGENT.md, CONTRIBUTING.md) need per-content decisions (Claude's Discretion in CONTEXT).

Output:
- `tests/e2e/`, `tests/fixtures/` gone (fixtures confirmed orphan in Task 1)
- `.github/workflows/e2e.yml`, `release.yml` gone
- `scripts/install.ps1`, `scripts/package-extension.ps1` gone
- 4 extension-era docs + `TODO.txt` gone
- `AGENTS.md`, `PLANNING_AGENT.md`, `CONTRIBUTING.md` — updated or deleted per content inspection
- `package.json`: `@playwright/test` devDep dropped; `test:e2e`, `test:coverage`, `install:mapi`, `uninstall:mapi` scripts dropped
- Must-hold invariant still green
</objective>

<execution_context>
@C:/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-CONTEXT.md
@.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-PATTERNS.md
@package.json
@AGENTS.md
@PLANNING_AGENT.md
@CONTRIBUTING.md

<interfaces>
<!-- Current-state references. Confirm at execution time. -->

Deletion targets this plan:
- Directories: `tests/e2e/`, `tests/fixtures/` (after orphan verification)
- Workflow files: `.github/workflows/e2e.yml`, `.github/workflows/release.yml`
- Scripts: `scripts/install.ps1`, `scripts/package-extension.ps1`
- Docs: `docs/chrome-webstore-privacy-justifications.md`, `docs/chrome-webstore-testing-instructions.md`, `docs/webstore-description.md`, `docs/webstore-description.txt`
- Root: `TODO.txt`

Preserve:
- `tests/protocol-fixtures/` — consumed by `internal/mapi/protocol_integration_test.go` via `internal/mapi/testutil/FixturePath` (PATTERNS.md confirmed)
- `tests/sandbox/` — MAPI DLL sandbox tests; no native-host dependency (PATTERNS.md grep-verified)
- `docs/json-schema.json`, `docs/testing/oauth2-testing-plan.md`, `docs/measurements/phase-07-ram-gate.*` (Wails-era per PATTERNS.md)
- `scripts/azure-ram-gate.ps1`, `scripts/azure-ram-gate.README.md`, `scripts/dev-wails.ps1`, `scripts/measure-ram.ps1`, `scripts/test-drop-email.ps1` (Wails-era)

Inspect-and-decide (CONTEXT Claude's Discretion):
- `AGENTS.md` — if it describes v2.x three-tier architecture, either rewrite for Wails or delete (Plan 09 rewrites CLAUDE.md; AGENTS.md may follow the same pattern).
- `PLANNING_AGENT.md` — likely a legacy agent definition; check for v2.x references.
- `CONTRIBUTING.md` — currently references `generate-icons.js` at line 77 (grep-confirmed); update to remove v2.x paths or delete if wholly v2.x.

Root package.json trims this plan:
- Scripts to remove: `test:e2e`, `test:coverage`, `install:mapi`, `uninstall:mapi`
- devDependencies to remove: `@playwright/test`
- devDependencies remaining after this plan: `@changesets/cli` ONLY
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight — verify tests/fixtures is orphan and no Go test references remain</name>
  <files></files>
  <read_first>
    - `ls tests/fixtures/` — five JSON fixture files per PATTERNS.md.
    - PATTERNS.md line 43: "tests/fixtures/ — contains malformed.json, multiple-recipients.json, simple-email.json, unicode-content.json, with-attachment.json — consumed by mime golden tests in src/native-host/. If planner ports mime_golden_test.go → internal/mapi/, it must update the fixture path. Otherwise tests/fixtures/ becomes orphaned (delete candidate)."
    - `internal/mapi/mime_golden_test.go` (Plan 01 output) — confirm it does NOT reference `tests/fixtures/` (it uses `testdata/mime/*.golden` fixtures, a different directory — already copied to `internal/mapi/testdata/mime/` in Plan 01).
  </read_first>
  <action>
    1. Grep across all remaining Go + TS + PS + JS files for references to `tests/fixtures` or `tests\\fixtures`:
       ```
       grep -r --include='*.go' --include='*.ts' --include='*.js' --include='*.mjs' --include='*.ps1' --include='*.svelte' 'tests/fixtures\|tests\\\\fixtures' .
       ```
       (Exclude node_modules, .git.) Expected result: zero matches.

    2. Grep for references to `tests/e2e`:
       ```
       grep -r --include='*.yml' --include='*.yaml' --include='*.json' 'tests/e2e' .
       ```
       Expected: only in `package.json` (script `test:e2e`) and `.github/workflows/e2e.yml`. Both will be deleted in this plan's subsequent tasks.

    3. Confirm `tests/protocol-fixtures/` IS still referenced (it must be preserved):
       ```
       grep -r --include='*.go' 'protocol-fixtures' .
       ```
       Expected: at least one match inside `internal/mapi/testutil/` or `internal/mapi/protocol_integration_test.go`. If zero matches, escalate — protocol-fixtures may now also be an orphan, but PATTERNS.md says it's consumed.

    4. If `tests/fixtures/` has any remaining references, STOP and surface. Otherwise it's safe to delete.

    5. Baseline must-hold invariant BEFORE mutations — all four commands exit 0.
  </action>
  <acceptance_criteria>
    - `grep -r --include='*.go' --include='*.ts' --include='*.ps1' 'tests/fixtures' . | wc -l` returns 0 (ignoring .git and node_modules — use appropriate `--exclude-dir`).
    - `grep -r 'protocol-fixtures' internal/mapi/ | wc -l` returns at least 1 (still consumed).
    - Must-hold invariant baseline passes.
  </acceptance_criteria>
  <verify>
    <automated>bash -c "cd C:/dev/go-mapi && ! grep -rq --include='*.go' --include='*.ts' --include='*.ps1' --exclude-dir='.git' --exclude-dir='node_modules' 'tests/fixtures' . && grep -rq --include='*.go' 'protocol-fixtures' internal/mapi/"</automated>
  </verify>
  <done>Orphan status of `tests/fixtures/` confirmed; `tests/protocol-fixtures/` confirmed still in use; baseline green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete tests/e2e, tests/fixtures, e2e.yml, release.yml, install scripts, extension docs, TODO.txt</name>
  <files>
    tests/e2e
    tests/fixtures
    .github/workflows/e2e.yml
    .github/workflows/release.yml
    scripts/install.ps1
    scripts/package-extension.ps1
    docs/chrome-webstore-privacy-justifications.md
    docs/chrome-webstore-testing-instructions.md
    docs/webstore-description.md
    docs/webstore-description.txt
    TODO.txt
  </files>
  <read_first>
    - `ls tests/e2e/`, `ls tests/fixtures/` — confirm targets exist.
    - `ls .github/workflows/` — confirm `e2e.yml` and `release.yml` exist.
    - `ls scripts/install.ps1 scripts/package-extension.ps1` — confirm.
    - `ls docs/chrome-webstore-*.md docs/webstore-description.*` — confirm.
    - `ls TODO.txt` — confirm at repo root.
    - PATTERNS.md §Wave 1-6 Deletion table — this task executes waves 4+5+6 per D-02.
  </read_first>
  <action>
    Delete all of these targets in one sweep (v2.1.x git tag is archive, D-01):

    Bash:
    ```
    rm -rf tests/e2e tests/fixtures
    rm -f .github/workflows/e2e.yml .github/workflows/release.yml
    rm -f scripts/install.ps1 scripts/package-extension.ps1
    rm -f docs/chrome-webstore-privacy-justifications.md docs/chrome-webstore-testing-instructions.md
    rm -f docs/webstore-description.md docs/webstore-description.txt
    rm -f TODO.txt
    ```

    PowerShell:
    ```
    Remove-Item -Recurse -Force tests/e2e, tests/fixtures
    Remove-Item -Force .github/workflows/e2e.yml, .github/workflows/release.yml
    Remove-Item -Force scripts/install.ps1, scripts/package-extension.ps1
    Remove-Item -Force docs/chrome-webstore-privacy-justifications.md, docs/chrome-webstore-testing-instructions.md
    Remove-Item -Force docs/webstore-description.md, docs/webstore-description.txt
    Remove-Item -Force TODO.txt
    ```

    Do NOT delete:
    - `tests/protocol-fixtures/`, `tests/sandbox/`
    - `.github/workflows/build.yml`, `go-race-nightly.yml`, `version-packages.yml`
    - `docs/json-schema.json`, `docs/testing/`, `docs/measurements/`
    - `scripts/azure-ram-gate.*`, `scripts/dev-wails.ps1`, `scripts/measure-ram.ps1`, `scripts/test-drop-email.ps1`

    After deletion, sanity-check remaining dir structure:
    - `ls tests/` — expect `protocol-fixtures/` and `sandbox/` only.
    - `ls .github/workflows/` — expect 3 files: `build.yml`, `go-race-nightly.yml`, `version-packages.yml`.
    - `ls scripts/` — expect 5 items: `azure-ram-gate.ps1`, `azure-ram-gate.README.md`, `dev-wails.ps1`, `measure-ram.ps1`, `test-drop-email.ps1`.
    - `ls docs/` — expect 3 items: `json-schema.json`, `measurements/`, `testing/`.
  </action>
  <acceptance_criteria>
    - `test ! -d tests/e2e`.
    - `test ! -d tests/fixtures`.
    - `test -d tests/protocol-fixtures && test -d tests/sandbox`.
    - `test ! -f .github/workflows/e2e.yml && test ! -f .github/workflows/release.yml`.
    - `test -f .github/workflows/build.yml && test -f .github/workflows/go-race-nightly.yml && test -f .github/workflows/version-packages.yml`.
    - `test ! -f scripts/install.ps1 && test ! -f scripts/package-extension.ps1`.
    - `test -f scripts/dev-wails.ps1 && test -f scripts/azure-ram-gate.ps1`.
    - `test ! -f docs/chrome-webstore-privacy-justifications.md && test ! -f docs/chrome-webstore-testing-instructions.md && test ! -f docs/webstore-description.md && test ! -f docs/webstore-description.txt`.
    - `test -f docs/json-schema.json`.
    - `test ! -f TODO.txt`.
  </acceptance_criteria>
  <verify>
    <automated>test ! -d tests/e2e && test ! -d tests/fixtures && test -d tests/protocol-fixtures && test ! -f .github/workflows/e2e.yml && test ! -f .github/workflows/release.yml && test -f .github/workflows/build.yml && test ! -f TODO.txt && test -f docs/json-schema.json</automated>
  </verify>
  <done>All 11 targets removed; all preserved assets still in place.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Inspect and decide on AGENTS.md, PLANNING_AGENT.md, CONTRIBUTING.md</name>
  <files>
    AGENTS.md
    PLANNING_AGENT.md
    CONTRIBUTING.md
  </files>
  <read_first>
    - Full `AGENTS.md` at repo root.
    - Full `PLANNING_AGENT.md` at repo root.
    - Full `CONTRIBUTING.md` at repo root (known reference: line 77 mentions `generate-icons.js` — already deleted in Plan 05).
    - CONTEXT §Claude's Discretion — "Fate of AGENTS.md, PLANNING_AGENT.md, CONTRIBUTING.md at repo root — inspect for v2.x references; update or delete per content. Leave LICENSE, COPYING, .gitignore, .editorconfig (not scoped)."
  </read_first>
  <action>
    For EACH of the three files:

    1. Read the full file.

    2. Count v2.x-specific references (any of: `src/native-host`, `src/extension`, `browser extension`, `chrome extension`, `native host`, `Chrome Web Store`, `Edge Add-ons`, `generate-icons`, `Inno Setup`, `eslint`, `Playwright`, `React`, `chrome.identity`, `native messaging`).

    3. Apply this decision rule:
       - **> 50% of the file's substantive content is v2.x-specific** → DELETE the file. Commit reasoning in the commit message body ("deleted: N% of content was v2.x-specific references replaced by Wails pivot; v2.1.x git tag preserves the original").
       - **≤ 50% v2.x-specific but file still has Wails-era relevance** → UPDATE in place: remove the v2.x-specific paragraphs/sections/lines and replace with a short note referring to `.planning/` or CLAUDE.md for current architecture. Keep the file's top-level purpose intact.
       - **Zero or near-zero v2.x references** → Leave untouched.

    4. **For `CONTRIBUTING.md` specifically:** line 77 references `generate-icons.js` (now deleted). AT MINIMUM, remove that line + any surrounding section describing the extension-asset pipeline. Check the full file for other stale references (`npm run build:extension`, `npm run test:extension`, any Chrome/Edge dev-setup steps) and trim.

    5. **For `AGENTS.md`:** If it is an old-style "agent index" (describing GSD or project agents), cross-reference with `.claude/skills/` and `.claude/agents/`. If it's superseded by those, DELETE. If it describes a custom agent protocol unique to this project, UPDATE the v2.x paths inside it.

    6. **For `PLANNING_AGENT.md`:** Likely legacy. If it describes a planning protocol NOT superseded by `.claude/` skills or CLAUDE.md, UPDATE. Otherwise DELETE.

    7. Do NOT touch:
       - `LICENSE`, `COPYING` (LGPL text)
       - `.gitignore`, `.editorconfig`
       - `CLAUDE.md`, `README.md` (Plan 09 rewrites both)
       - Anything under `.planning/`

    8. Commit this task's output as part of the plan's atomic commit at Task 5. Do NOT commit separately.

    9. Record decisions in the commit message body: "AGENTS.md: {deleted|updated}. PLANNING_AGENT.md: {deleted|updated}. CONTRIBUTING.md: {updated}."
  </action>
  <acceptance_criteria>
    - After this task, `grep -l 'src/native-host\|src/extension\|Chrome Web Store\|Edge Add-ons' AGENTS.md PLANNING_AGENT.md CONTRIBUTING.md 2>/dev/null` returns no files (all three either deleted or cleaned of those terms).
    - `grep -l 'generate-icons' CONTRIBUTING.md 2>/dev/null` returns nothing (if kept, that reference is gone).
    - `test -f LICENSE && test -f COPYING && test -f .gitignore` (untouched).
    - Each decision (deleted vs updated per file) is documented in the eventual commit message.
  </acceptance_criteria>
  <verify>
    <automated>bash -c "for f in AGENTS.md PLANNING_AGENT.md CONTRIBUTING.md; do if [ -f $f ] && grep -Eq 'src/native-host|src/extension|generate-icons|Chrome Web Store|Edge Add-ons' $f; then echo FAIL: $f still has v2.x references; exit 1; fi; done; echo OK"</automated>
  </verify>
  <done>Three root docs either deleted or cleaned of v2.x references; LICENSE/COPYING/.gitignore untouched; decision rationale recorded for commit message.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final root package.json trim — drop @playwright/test + test:e2e / test:coverage / install:mapi / uninstall:mapi scripts</name>
  <files>
    package.json
    package-lock.json
  </files>
  <read_first>
    - Current root `package.json` — after Plan 05, devDeps should be `{"@changesets/cli": "...", "@playwright/test": "..."}`; remove the second.
    - Scripts still present: `test:e2e` (line 24 originally), `test:coverage` (line 25), `install:mapi` (line 30), `uninstall:mapi` (line 31).
    - PATTERNS.md §`package.json (MODIFY)` — final target state.
  </read_first>
  <action>
    Edit `package.json`:

    1. **Remove scripts** (exact keys):
       - `test:e2e`
       - `test:coverage`
       - `install:mapi`
       - `uninstall:mapi`

    2. **Drop devDependencies:**
       - `@playwright/test`

    3. **Preserve:**
       - devDependency: `@changesets/cli`
       - All interceptor scripts + `test:dll:sandbox`

    4. After this task, the final expected `scripts` set should be approximately:
       ```json
       "scripts": {
         "build": "npm run build:interceptor",
         "build:interceptor": "powershell -ExecutionPolicy Bypass -File src/interceptor/build.ps1 -Config Release",
         "build:interceptor:debug": "powershell -ExecutionPolicy Bypass -File src/interceptor/build.ps1 -Config Debug -Tests",
         "clean": "npm run clean:interceptor",
         "clean:interceptor": "powershell -ExecutionPolicy Bypass -Command \"Remove-Item -Recurse -Force src/interceptor/build -ErrorAction SilentlyContinue\"",
         "test:interceptor": "powershell -ExecutionPolicy Bypass -Command \"src/interceptor/build/bin/go-mapi-test-harness.exe src/interceptor/build/bin/go-mapi.dll\"",
         "test:dll:sandbox": "powershell -ExecutionPolicy Bypass -File tests/sandbox/run-sandbox-test.ps1"
       }
       ```
       (Plan 08 may add a convenience `test` aggregator tying Go + frontend Vitest; that's D-22 domain.)

    5. After edit, run `npm install` from repo root. Playwright's large browser-install sub-deps will be removed from `package-lock.json`. Stage both files together per lockfile-discipline.

    6. Sanity verify: `jq '.devDependencies | keys' package.json` returns `["@changesets/cli"]` only.
  </action>
  <acceptance_criteria>
    - `jq '.scripts["test:e2e"] // "ABSENT"' package.json` returns `"ABSENT"`.
    - `jq '.scripts["test:coverage"] // "ABSENT"' package.json` returns `"ABSENT"`.
    - `jq '.scripts["install:mapi"] // "ABSENT"' package.json` returns `"ABSENT"`.
    - `jq '.scripts["uninstall:mapi"] // "ABSENT"' package.json` returns `"ABSENT"`.
    - `jq -r '.devDependencies | keys | join(",")' package.json` returns exactly `@changesets/cli`.
    - `jq '.devDependencies["@playwright/test"] // "ABSENT"' package.json` returns `"ABSENT"`.
    - `npm install` exits 0.
    - `grep -c 'playwright' package-lock.json` drops significantly (all Playwright transitive deps gone).
  </acceptance_criteria>
  <verify>
    <automated>jq -e '(.devDependencies | keys) == ["@changesets/cli"] and (.scripts["test:e2e"] == null) and (.scripts["test:coverage"] == null) and (.scripts["install:mapi"] == null) and (.scripts["uninstall:mapi"] == null)' package.json</automated>
  </verify>
  <done>package.json has only `@changesets/cli` in devDeps; four scripts removed; package-lock.json regenerated and staged alongside.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Must-hold invariant verification + atomic commit</name>
  <files></files>
  <read_first>
    - CONTEXT §Specifics "must-hold invariant".
  </read_first>
  <action>
    Run the four must-hold-invariant commands:

    1. `npm run build:interceptor` — C++ DLL builds.
    2. `go build ./src/app/... ./internal/mapi/...` — Go builds.
    3. `go test ./src/app/... ./internal/mapi/...` — Go tests pass.
    4. `npm run -w @marcfargas/go-mapi-app-frontend build` — Svelte frontend builds.

    All four must exit 0.

    Commit with a message summarizing all deletions:
    ```
    chore(08.1): drop tests/e2e, v2.x scripts + docs, release workflow, and @playwright/test devDep

    - delete tests/e2e/ and orphan tests/fixtures/
    - delete .github/workflows/e2e.yml and release.yml (Phase 11 REL-02 owns v3.0 pipeline)
    - delete scripts/install.ps1 and scripts/package-extension.ps1 (v2.x)
    - delete docs/chrome-webstore-* and docs/webstore-description.* (extension-store artifacts)
    - delete TODO.txt (v1.0 Phase 1 log)
    - AGENTS.md: {updated|deleted}
    - PLANNING_AGENT.md: {updated|deleted}
    - CONTRIBUTING.md: updated to drop generate-icons + v2.x extension dev steps
    - package.json: drop @playwright/test, test:e2e, test:coverage, install:mapi, uninstall:mapi
    ```

    Stage:
    - All deleted files (implicit via `git add -u` or explicit paths)
    - Edited files: `package.json`, `package-lock.json`
    - Whichever of `AGENTS.md` / `PLANNING_AGENT.md` / `CONTRIBUTING.md` survived (updated) or became deletions

    Do NOT stage coverage output files or any other test byproducts.
  </action>
  <acceptance_criteria>
    - All four must-hold invariant commands exit 0.
    - `git log -1 --stat` shows the expected file deletions + edits in one commit.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:interceptor && go build ./src/app/... ./internal/mapi/... && go test ./src/app/... ./internal/mapi/... && npm run -w @marcfargas/go-mapi-app-frontend build</automated>
  </verify>
  <done>Wave 4+5+6 cleanup committed atomically; repo build + test still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan deletes test scaffolding, v2.x release workflow, and extension-era docs. The deleted `release.yml` referenced a v2.x release pipeline that would not run against v3.0 artifacts correctly — removing it closes a DENIAL-OF-SERVICE class risk where a misconfigured workflow could fail noisily on main and block unrelated merges.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-06-01 | D (dangling CI) | `.github/workflows/e2e.yml` | mitigate | Deleted in same commit as `tests/e2e/`. No orphan workflow dispatch can fail. |
| T-08.1-06-02 | D (dangling release pipeline) | `.github/workflows/release.yml` | mitigate | Deleted outright. Phase 11 REL-02 writes the v3.0 replacement (NSIS + SignPath) from scratch — no stub left behind per D-18. |
| T-08.1-06-03 | I (info leak through stale docs) | `docs/chrome-webstore-*` files | mitigate | Deleted. These contained user-facing copy for the v2.x extension store listing — unpublished on v3.0 ship per REL-01. |
| T-08.1-06-04 | T (stale contributor guidance) | `CONTRIBUTING.md` | mitigate | Task 3 removes v2.x references; whatever contributor instructions remain must match the current Wails codebase (Plan 09's CLAUDE.md rewrite is the authoritative agent-facing doc; CONTRIBUTING.md is human-facing). |
| T-08.1-06-05 | I (orphan test fixture disclosure) | `tests/fixtures/*.json` | accept | Task 1 grep-confirmed no consumers. Synthetic test data only (no real PII). |
</threat_model>

<verification>
- All 11 deletion targets removed.
- Three inspect-and-decide files resolved (updated or deleted).
- `package.json` final state: only `@changesets/cli` in devDeps; no extension/e2e/install:mapi scripts.
- Must-hold invariant all green.
- Plan 07 (`build.yml` rewrite) has a clean CI workflow slate (only `build.yml`, `go-race-nightly.yml`, `version-packages.yml` remain).
</verification>

<success_criteria>
- Every v2.x Playwright + extension + Inno installer artifact is gone from the repository outside the v2.1.x git tag.
- Repository root contains only live artifacts (LICENSE, COPYING, README.md, CLAUDE.md, package.json, go.work + sums, package-lock.json, .changeset/, .planning/, src/, internal/, tests/, docs/, scripts/, .github/) — no legacy `TODO.txt` or dead docs.
- `package.json` is as lean as it can be before Plan 08 does the final shape (devDeps = just changesets).
- Plan 07 can rewrite `build.yml` freely: no references to deleted v2.x jobs, no ambient workflow files referencing deleted sources.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-06-SUMMARY.md`.
</output>
