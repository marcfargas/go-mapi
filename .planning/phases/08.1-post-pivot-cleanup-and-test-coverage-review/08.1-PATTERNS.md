# Phase 8.1: Post-pivot cleanup and test coverage review - Pattern Map

**Mapped:** 2026-04-18
**Files analyzed:** ~40 across deletion, modification, and new-test-scaffold categories
**Analogs found:** all modification/new-scaffold files have at least a role-match analog; deletions don't need analogs

---

## File Classification

### Wave 1-6: Deletion waves (no new code; no analogs needed)

| Path to delete | Wave | Category | Notes for planner |
|----------------|------|----------|-------------------|
| `src/native-host/` (whole dir) | 1 | cleanup-delete | Contains `main.go`, `gmail_test.go`, `mime_golden_test.go`, `protocol_test.go`, `protocol_integration_test.go`, `nativemessaging.go`, `version.go`, `manifests/`, `testdata/`, `build/`, `go.mod`, `go.sum`, `package.json`. **Landmine:** planner MUST diff `gmail_test.go` + `mime_golden_test.go` against `internal/mapi/` before delete — if they are NOT already ported there, port first (see "New test scaffolds" §Go below). |
| `src/native-host/` go.work `use` entry | 1 | config-trim | go.work currently lists `./src/native-host` on line 6 |
| root `package.json` workspace `"src/native-host"` | 1 | config-trim | Currently line 7 of package.json |
| root `package.json` scripts: `build:native-host`, `build:native-host:debug`, `test:native-host`, `clean:native-host` | 1 | config-trim | Lines 14–15, 21, 28 |
| root `package.json` `build` + `test` aggregator script entries referencing native-host | 1 | config-trim | Line 11 (`build`) chains `build:native-host`; line 20 (`test`) chains `test:native-host`; line 26 (`clean`) chains `clean:native-host` |
| `.changeset/config.json` `$workspaces` entry `"src/native-host"` | 1 | config-trim | Currently line 17 |
| `src/extension/` (whole dir) | 2 | cleanup-delete | React + Vitest + vitest.config.ts, chrome mocks, popup, dist/, etc. |
| `eslint.config.mjs` at repo root | 2 | cleanup-delete | Flat config — only used by extension |
| root `package.json` workspace `"src/extension"` | 2 | config-trim | Currently line 6 |
| root `package.json` scripts: `build:extension`, `dev:extension`, `test:extension`, `clean:extension`, `package:extension` | 2 | config-trim | Lines 16–19, 24, 29 |
| `.changeset/config.json` `$workspaces` entry `"src/extension"` | 2 | config-trim | Currently line 16 |
| `src/installer/` (whole dir — `go-mapi.iss`, `tests/installer.Tests.ps1`, `dist/`, `README.md`) | 3 | cleanup-delete | Inno Setup lives here |
| `.github/workflows/installer-release.yml` | 3 | cleanup-delete | Inno Setup release pipeline |
| `.github/workflows/installer-smoke.yml` | 3 | cleanup-delete | Pester smoke test for Inno |
| `tests/e2e/` (whole dir — all `.spec.ts`, `mock-gmail/`, `mock-host/`, `fixtures-v2.ts`, `fixtures.ts`, `playwright-report/`, `playwright.config.ts`) | 4 | cleanup-delete | — |
| `.github/workflows/e2e.yml` | 4 | cleanup-delete | — |
| root `package.json` devDep `@playwright/test` | 4 | config-trim | Currently line 53 |
| root `package.json` script `test:e2e` | 4 | config-trim | Currently line 24 |
| `scripts/install.ps1` | 5 | cleanup-delete | v2.x install script — invoked by `install:mapi` / `uninstall:mapi` npm scripts |
| `scripts/package-extension.ps1` | 5 | cleanup-delete | Extension packaging — no longer needed |
| `docs/chrome-webstore-privacy-justifications.md`, `docs/chrome-webstore-testing-instructions.md`, `docs/webstore-description.md`, `docs/webstore-description.txt` | 5 | cleanup-delete | Extension-store artifacts |
| `TODO.txt` | 5 | cleanup-delete | v1.0 Phase 1 log |
| root `package.json` scripts: `install:mapi`, `uninstall:mapi` | 5 | config-trim | Lines 30–31 |
| `.github/workflows/release.yml` | 6 | cleanup-delete | Phase 11 REL-02 owns v3.0 replacement |

**Inspection checks during waves (from CONTEXT §Claude's Discretion + §Potential Landmines):**
- `test-drop-email.ps1`, `tests/sandbox/*.ps1`: grep confirmed NO references to `src/native-host` or `src/extension`. Safe to keep.
- `tests/protocol-fixtures/*.json`: already consumed by `internal/mapi/protocol_integration_test.go` via `internal/mapi/testutil/FixturePath`. Keep.
- `tests/fixtures/`: contains `malformed.json`, `multiple-recipients.json`, `simple-email.json`, `unicode-content.json`, `with-attachment.json` — consumed by mime golden tests in `src/native-host/`. If planner ports `mime_golden_test.go` → `internal/mapi/`, it must update the fixture path. Otherwise `tests/fixtures/` becomes orphaned (delete candidate).
- `docs/json-schema.json`, `docs/testing/oauth2-testing-plan.md`, `docs/measurements/phase-07-ram-gate.{csv,md}`: Wails-era — keep all (Phase 7 artifacts + Phase 8 plan).
- `AGENTS.md`, `PLANNING_AGENT.md`, `CONTRIBUTING.md`: inspect for v2.x references — planner decides update-or-delete per content.

---

### Wave 7: CI workflow rewrite

| File | Role | Data Flow | Closest Analog | Match Quality |
|------|------|-----------|----------------|---------------|
| `.github/workflows/build.yml` (rewrite) | CI workflow | event-driven | `.github/workflows/go-race-nightly.yml` (for Go steps) + existing `build-interceptor` job (preserved) | role-match |

---

### Wave 8: Documentation rewrites

| File | Role | Data Flow | Closest Analog | Match Quality |
|------|------|-----------|----------------|---------------|
| `CLAUDE.md` (rewrite) | docs-rewrite | — | current `CLAUDE.md` (restructure, not template) | no pure analog |
| `README.md` (rewrite) | docs-rewrite | — | current `README.md` (restructure) | no pure analog |

---

### Waves 9-10: Config trims (covered in wave tables above) — no new files.

---

### Coverage additions: new Go tests

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `internal/mapi/gmail_test.go` (port from `src/native-host/gmail_test.go` if absent) | Go test | request-response (httptest) | `src/native-host/gmail_test.go` | exact |
| `internal/mapi/mime_golden_test.go` (port from `src/native-host/` if absent) | Go test | file-I/O + golden-file | `src/native-host/mime_golden_test.go` | exact |
| `src/app/auth_test.go` (EXTEND — already exists) | Go test | request-response (httptest) + keyring | `src/app/auth_test.go` (self, extended) | exact |
| `src/app/auth_keyring_windows_test.go` (NEW — if D-11 refactor introduces `KeyringStore` interface) | Go test (Windows-only integration) | file-I/O (real Credential Manager) | `src/app/sessionend_test.go` (uses `//go:build windows` + real Win32 API) | role-match |
| `src/app/app_test.go` (NEW — covers `NewApp`, `GetQueue` stub, `setVisible`/`isVisible`, `requestQuit`) | Go test | in-memory state | `src/app/watcher_bridge_test.go` (pattern for App-struct tests without full Wails runtime) | role-match |
| `src/app/paths_test.go` (NEW — covers `watcherDir` env precedence) | Go test | env var | `src/app/singleinstance_test.go` (pattern for pure-function + constant tests) | role-match |
| `src/app/logging_test.go` (NEW — optional per planner judgement; low risk) | Go test | file-I/O | `src/app/watcher_bridge_test.go` (goroutine + temp dir pattern) | role-match |
| `src/app/credentials_check_test.go` (NEW — covers non-bindings guard when ldflags unset) | Go test | env check / `os.Exit` proxy | `src/app/auth_test.go` TestAuthStatusZeroValueOmitsEmptyFields (short pure-logic style) | role-match |
| `src/app/watcher_bridge_test.go` (EXTEND if gaps — already exists) | Go test | goroutine + channel | itself | exact |

---

### Coverage additions: new frontend test scaffold (Vitest + @testing-library/svelte + jsdom)

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `src/app/frontend/vitest.config.ts` (NEW — OR fold into existing `vite.config.ts` as single-file config) | config | — | `src/extension/vitest.config.ts` (v2.x) | role-match (framework-swap React→Svelte) |
| `src/app/frontend/src/test/setup.ts` (NEW) | test scaffold | — | `src/extension/src/test/setup.ts` | role-match |
| `src/app/frontend/src/lib/auth.test.ts` (NEW — tests pre-auth localStorage + fetchAuthStatus/signIn/signOut/subscribeAuth via vi.mock of wailsjs) | TS unit test | mocked IPC | NONE inside `src/app/frontend/` — use Vitest docs + `src/extension/src/lib/*.test.ts` (v2.x) as reference if present | no analog (planner may scan `src/extension/src/` pre-deletion for similar vi.mock patterns and copy before wave 2 deletes it) |
| `src/app/frontend/src/lib/queue.test.ts` (NEW — tests `fetchQueue` null-guard, `subscribeQueue` error path) | TS unit test | mocked IPC | Same as auth.test.ts | no analog |
| `src/app/frontend/src/lib/components/SignInScreen.test.ts` (NEW) | Svelte component test | render + event | **@testing-library/svelte docs** (no repo analog for Svelte 5 runes mode) | no analog |
| `src/app/frontend/src/lib/components/PreAuthModal.test.ts` (NEW) | Svelte component test | render + event | same | no analog |
| `src/app/frontend/src/lib/components/ReAuthBanner.test.ts` (NEW) | Svelte component test | render + event | same | no analog |
| `src/app/frontend/src/lib/components/SignedInHeader.test.ts` (NEW) | Svelte component test | render + event + $derived | same | no analog |
| `src/app/frontend/src/App.test.ts` (NEW — smoke-level render+mount only per CONTEXT D-09) | Svelte component test | render + wailsjs mock | same | no analog |
| `src/app/frontend/package.json` (MODIFY — add `test`/`test:run` scripts + devDeps `vitest`, `@testing-library/svelte`, `jsdom`, optional `@testing-library/jest-dom`) | package config | — | `src/extension/package.json` scripts/devDeps block | role-match (version pins differ per Svelte-5 compat) |

---

## Pattern Assignments

### `.github/workflows/build.yml` (CI workflow, rewrite — wave 7)

**Analog for `build-wails-app` job:** `.github/workflows/go-race-nightly.yml` (Go test + race pattern) combined with existing `build-interceptor` job retained unchanged from current `build.yml`.

**Preserved block — `build-interceptor` job** (current `build.yml` lines 12–52, keep verbatim):
```yaml
build-interceptor:
  name: Build Interceptor (C++)
  runs-on: windows-latest
  strategy:
    matrix:
      config: [Debug, Release]
  steps:
    - uses: actions/checkout@v4
    - name: Install MinGW and Ninja
      run: |
        choco install mingw ninja cmake --no-progress -y
        echo "C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\bin" >> $env:GITHUB_PATH
    - name: Build Interceptor and Test Harness
      working-directory: src/interceptor
      run: .\build.ps1 -Config ${{ matrix.config }} -Tests -Clean
    # ...ctest + artifact upload unchanged
```

**Deleted blocks** (current `build.yml` lines 54–112 — remove entirely):
- `build-native-host` job (lines 54–81) — drops Go 1.21 setup, `cd src/native-host && go test -v ./...`, `go build` ldflags wiring, native-host artifact upload
- `build-extension` job (lines 83–112) — drops Node 20 setup, `npm run -w go-mapi-extension typecheck`, `npm run -w go-mapi-extension test:run`, `npm run build:extension`, extension artifact upload

**New job — `build-wails-app`** (structure derived from D-17):

Pattern for `setup-go` + `CGO_ENABLED=1` + `go test -race` — copy from `go-race-nightly.yml`:
```yaml
# Source: .github/workflows/go-race-nightly.yml lines 19-33
runs-on: windows-latest
env:
  CGO_ENABLED: '1'
steps:
  - uses: actions/checkout@v4
  - name: Setup Go
    uses: actions/setup-go@v5
    with:
      go-version: '1.25'   # UPGRADED from '1.21' — go.work requires 1.25.0
  - name: go test -race ./...
    run: go test -race -v ./...
```

**Key deviations from the nightly pattern for the per-PR job:**
1. **Module scope:** `./internal/mapi/... ./src/app/...` (both modules, per D-07) — NOT `working-directory: src/native-host` (deleted).
2. **Coverage flag:** add `-coverprofile=coverage.out` (D-08).
3. **Coverage artifact upload per module** (D-08 + CONTEXT §Specifics): `coverage-internal-mapi.out` + `coverage-src-app.out`.
4. **Frontend steps added after Go:**
   - `npm ci` (install workspace deps)
   - `npm run -w @marcfargas/go-mapi-app-frontend check` (svelte-check, blocking per D-10)
   - `npm run -w @marcfargas/go-mapi-app-frontend test:run` (Vitest, blocking)
5. **Wails build sanity check:** `wails build` (no signing, just confirms assets embed + link).

**Artifact pattern** (preserved from current `build.yml` upload blocks):
```yaml
- name: Upload coverage artifacts
  uses: actions/upload-artifact@v4
  with:
    name: go-coverage-${{ matrix.module }}
    path: ${{ matrix.coverage-path }}
    if-no-files-found: warn
```

**Deleted workflows in this wave's sibling files:** `e2e.yml` (wave 4), `installer-release.yml` + `installer-smoke.yml` (wave 3), `release.yml` (wave 6) — confirmed stale per CONTEXT D-03/D-14/D-18.

---

### `CLAUDE.md` (docs-rewrite — wave 8)

**Structural analog:** the current file itself. Planner keeps the `<!-- GSD:section-start source:X -->` HTML-comment markers (see current lines 1/19/20/135/etc.) but rewrites every block body.

**Sections to REPLACE (current → rewritten):**

| Current block (source) | What it says today (wrong) | What it must say |
|------------------------|----------------------------|------------------|
| `<!-- GSD:project-start -->` (lines 1–18) | "three-tier bridge (C++ MAPI DLL → Go native host → Chrome/Edge extension)" | "Two-component Wails desktop app (C++ MAPI DLL + Go/Svelte WebView2 app) that routes Windows 'Send to Mail recipient' calls to Gmail as drafts" |
| `<!-- GSD:stack-start -->` §Languages | Lists Go 1.21, TypeScript 5.3, React 18 | Go 1.25, TypeScript 5.x, Svelte 5, C++17 (DLL only) |
| §Frameworks | Lists React, React-Bootstrap, Vite 5, Vitest 2.1, Playwright 1.58, ESLint 9, @testing-library/react | Wails v2.12, Svelte 5, Vite 6, Vitest (added in 8.1), @testing-library/svelte (added in 8.1), svelte-check, jsdom, go test, fyne.io/systray (NO Playwright, NO ESLint, NO React) |
| §Key Dependencies | `sharp`, `react`, `react-dom`, `vitest-chrome` | `golang.org/x/oauth2`, `github.com/zalando/go-keyring`, `github.com/fsnotify/fsnotify`, `github.com/wailsapp/wails/v2`, `fyne.io/systray`, `github.com/pkg/browser` |
| §Configuration | `src/extension/public/manifest.json` OAuth, Chrome/Edge native messaging manifests, `eslint.config.mjs`, `playwright.config.ts` | Wails `src/app/wails.json`; OAuth via `-ldflags` injection into `src/app/auth_credentials.go`; svelte + vitest configs under `src/app/frontend/` |
| §Build Pipeline | `npm run build:extension`, `build:native-host` | `wails build` (single step, embeds frontend via `go:embed`); per-module `go test`; frontend via workspace scripts |
| §Directory and File Location | `src/native-host/*.go` | `src/app/*.go` (+ `internal/mapi/*.go` for shared code) |
| Full layout section | Describes 3-tier IPC (DLL → Go → Chrome) | Describes 2-component (DLL writes `%TEMP%\go-mapi\*.json`; Wails app watches same dir, renders Svelte UI, calls Gmail API) |

**Voice/length guidance (CONTEXT §Specifics):** technical, pragmatic, no marketing language. English. Agent-facing (more terse than README).

---

### `README.md` (docs-rewrite — wave 8)

**Structural analog:** current `README.md` (current lines 1–80 checked). Same Overview → Status → Architecture → Installation → Development structure, but every Status row + every Architecture line changes.

**Sections to REPLACE:**

| Current (wrong) | Rewritten |
|-----------------|-----------|
| Status table rows "Native messaging bridge", "Browser extension (popup + notifications)", "Chrome / Edge / Chromium / Brave / Vivaldi", "Single-click Windows installer (Inno Setup 6)" | `MAPI interception (ANSI + Unicode)` ✅ stable • `Wails desktop app (system tray + WebView2)` 🚧 v3.0 Phase 7 PASS • `OAuth desktop flow (PKCE loopback + Credential Manager)` 🚧 v3.0 Phase 8 • `NSIS installer` 📅 Phase 10 • `v3.0 release pipeline + SignPath` 📅 Phase 11 |
| Architecture ASCII diagram (3-tier: Windows App → DLL → Native Host → Chrome Extension → Gmail) | 2-component: Windows App → DLL → `%TEMP%\go-mapi\*.json` → Wails app (watches dir, Svelte UI + Go auth/Gmail) → Gmail API |
| Components table: Interceptor DLL / Native Host / Browser Extension | Interceptor DLL (unchanged C++) / Wails App (Go + Svelte 5 + WebView2, replaces both native host AND extension) |
| Why This Architecture (enterprise-friendly / Chrome Identity API / cross-browser) | Why Wails (desktop OAuth with no browser dependency / system tray for automode / native toasts / ≤80MB RAM gate PASS) |
| Install section (Windows host + browser extension + OAuth + test) | Install `go-mapi-setup.exe` (Phase 10+) / Dev: `npm install && wails dev` / Test: `go test ./... && npm run -w @marcfargas/go-mapi-app-frontend test:run` |

**Voice (CONTEXT §Specifics):** solo-maintainer-facing + future-contributor-facing, not marketing. English. Phase 11 REL-07 will polish with screenshots + final install URL — 8.1 provides accurate baseline only.

**Bottom of file — preserve the LGPL-3.0 block.** Check `COPYING` / `LICENSE` — both still apply.

---

### `internal/mapi/gmail_test.go` (port target, Go test, request-response + httptest)

**Analog (source file to port from):** `src/native-host/gmail_test.go` (lines 1–60 shown; full file stays behind in wave 1 deletion).

**Landmine check first:** planner runs `diff src/native-host/gmail_test.go internal/mapi/gmail_test.go` (if the target exists). If target is missing or older, port BEFORE wave 1 runs.

**Pattern to copy — httptest stub + case-table** (lines 36–80 of `src/native-host/gmail_test.go`):
```go
func TestGmailClient_CreateDraft(t *testing.T) {
    type stubHandler struct {
        status int
        body   string
    }
    cases := []struct {
        name         string
        stub         stubHandler
        closeServer  bool
        wantID       string
        wantErrSub   string
        expectCalled bool
    }{
        {name: "happy path returns draft id",
         stub: stubHandler{status: 200, body: `{"id":"draft_abc123"}`},
         wantID: "draft_abc123", expectCalled: true},
        {name: "401 unauthorized surfaces token expired",
         stub: stubHandler{status: 401, body: `{"error":"unauthorized"}`},
         wantErrSub: "token expired", expectCalled: true},
        // ...5xx, network failure, malformed body
    }
    // loop: spin up httptest.Server, inject via NewGmailClientWithBase(srv.URL)
}
```

**Import adjustment when porting:**
```go
// Before (in src/native-host — uses internal types directly)
"github.com/marcfargas/go-mapi/internal/mapi"
// Usage: mapi.MailMessage{...}

// After (file NOW LIVES in internal/mapi — use unqualified names)
package mapi
// Usage: MailMessage{...}, Recipient{...}, Recipients{...}
```

**Coverage-profile hook required** (per D-08 CI integration): tests stay `package mapi` (internal) so `go test -coverprofile=coverage-internal-mapi.out ./internal/mapi/...` captures them.

---

### `internal/mapi/mime_golden_test.go` (port target, Go test, file-I/O + golden-file)

**Analog:** `src/native-host/mime_golden_test.go` (lines 1–60 shown).

**Pattern — golden-file + `-update` flag + PID normalization** (lines 25–45):
```go
var updateGoldens = flag.Bool("update", false, "regenerate MIME golden files under testdata/mime")
var mimeBoundaryRE = regexp.MustCompile(`go_mapi_\d+`)
func normalizeMIMEBoundary(b []byte) []byte {
    return mimeBoundaryRE.ReplaceAll(b, []byte("go_mapi_PID"))
}
func writeAttachment(t *testing.T, dir, name string, data []byte) string {
    t.Helper()
    p := filepath.Join(dir, name)
    if err := os.WriteFile(p, data, 0o644); err != nil {
        t.Fatalf("write attachment: %v", err)
    }
    return p
}
```

**Golden fixture path move:** if `src/native-host/testdata/mime/*.golden` exists, planner moves to `internal/mapi/testdata/mime/`. Update `os.ReadFile(filepath.Join("testdata", "mime", caseName + ".golden"))` references. Preserve on-disk bytes exactly — regenerate ONLY if an encoding change is intentional.

---

### `src/app/auth_test.go` (EXTEND — existing file, Go test, request-response + httptest + keyring)

**Analog:** itself (lines 1–585). Already has all Phase 8 scenarios. 8.1 extends for gap fills per D-05 risk prioritization.

**Pattern 1 — httptest stub for OAuth token endpoint** (lines 295–323):
```go
func TestRefreshInvalidGrantClears(t *testing.T) {
    srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
        w.WriteHeader(400)
        _, _ = w.Write([]byte(`{"error":"invalid_grant"}`))
    }))
    defer srv.Close()
    tokenEndpointOverride = srv.URL
    t.Cleanup(func() { tokenEndpointOverride = "" })

    _ = keyring.Delete(keyringService, keyringUser)
    am := NewAuthManager()
    am.tokens = &OAuthTokens{AccessToken: "old", RefreshToken: "dead", Expiry: time.Now().Add(-time.Minute)}
    _ = am.saveToKeyringLocked()

    err := am.refreshIfNeededLocked(context.Background())
    if !errors.Is(err, ErrInvalidGrant) { t.Fatalf(...) }
    if am.tokens != nil { t.Fatal("expected tokens cleared") }
    _, getErr := keyring.Get(keyringService, keyringUser)
    if !errors.Is(getErr, keyring.ErrNotFound) { t.Fatalf("expected keyring cleared, got %v", getErr) }
}
```

**Pattern 2 — real keyring round-trip with cleanup** (lines 54–79):
```go
func TestAuthManagerKeyringRoundTrip(t *testing.T) {
    // Sequential (no t.Parallel) — shared service/user key.
    am := NewAuthManager()
    am.tokens = &OAuthTokens{AccessToken: "a", RefreshToken: "r", TokenType: "Bearer",
        Expiry: time.Date(2026, 4, 15, 0, 0, 0, 0, time.UTC)}
    if err := am.SaveToKeyring(); err != nil { t.Fatalf("save: %v", err) }
    t.Cleanup(func() { _ = am.ClearTokens() })

    am2 := NewAuthManager()
    if err := am2.LoadFromKeyring(); err != nil { t.Fatalf("load: %v", err) }
    if *am2.tokens != *am.tokens { t.Fatalf("round-trip mismatch") }
}
```

**D-11 decision — KeyringStore interface:**
The current `auth.go` calls `keyring.Get/Set/Delete` as **package-level functions** (lines 113, 142, 165, 505, 677) — there is **no existing interface** to extend. Planner must **introduce** a `KeyringStore` interface if D-11 is to be honored:
```go
// PROPOSED in src/app/auth.go — extracted in 8.1 refactor.
type KeyringStore interface {
    Get(service, user string) (string, error)
    Set(service, user, secret string) error
    Delete(service, user string) error
}
// realKeyringStore wraps zalando/go-keyring package-level funcs.
// fakeKeyringStore (in *_test.go) uses a sync.Mutex + map[string]string.
```
Cross-platform unit tests inject `fakeKeyringStore`; `src/app/auth_keyring_windows_test.go` (new, `//go:build windows`) runs round-trip against `realKeyringStore`.

**New test scenarios to add in 8.1 (gap-fill per D-05 risk):**
- Refresh: `400 invalid_client` (auth.go line 499 already handles it — test missing)
- Refresh: rotated refresh_token in response body (auth.go line 532 handles; test missing)
- Refresh: `200 OK` with empty `access_token` (auth.go line 523 error case; test missing)
- `MakeAuthenticatedGmailCall`: first-call 200 happy path (no refresh, no retry)
- `MakeAuthenticatedGmailCall`: non-401 error from `fn` bubbles up verbatim (line 611)
- `bootstrapAuth`: transient error path (line 716–720) — tokens retained, signed-in status kept, warning logged
- `bootstrapAuth`: `keyring.Get` returning non-ErrNotFound error (line 694–699) — tray flips to "credential store unavailable"
- `fetchUserInfoLocked`: happy path email+name set; 401 swallowed (am.email unchanged)

---

### `src/app/auth_keyring_windows_test.go` (NEW — Go Windows-only integration test)

**Analog:** `src/app/sessionend_test.go` lines 1–10 — the `//go:build windows` pattern.

**Pattern — build tag + real-API round-trip:**
```go
//go:build windows

package main

import (
    "testing"
    "github.com/zalando/go-keyring"
)

// TestRealKeyring_WindowsRoundTrip runs ONLY on windows-latest CI.
// Exercises the real Windows Credential Manager via zalando/go-keyring.
func TestRealKeyring_WindowsRoundTrip(t *testing.T) {
    // Ensure clean state.
    _ = keyring.Delete(keyringService, keyringUser)
    t.Cleanup(func() { _ = keyring.Delete(keyringService, keyringUser) })

    store := &realKeyringStore{}
    if err := store.Set(keyringService, keyringUser, `{"access_token":"a"}`); err != nil {
        t.Fatalf("set: %v", err)
    }
    got, err := store.Get(keyringService, keyringUser)
    if err != nil { t.Fatalf("get: %v", err) }
    if got != `{"access_token":"a"}` { t.Fatalf("got %q", got) }
}
```

---

### `src/app/app_test.go` (NEW — covers `NewApp`, visibility, `GetQueue`, `requestQuit`)

**Analog:** `src/app/watcher_bridge_test.go` (pattern for testing `*App`-style structs with goroutines + mutex + injectable contexts).

**Pattern — constructor + in-memory state** (watcher_bridge_test.go lines 22–38):
```go
// Test 1: NewApp constructs an App with auth set up.
func TestNewAppInitializesAuth(t *testing.T) {
    app := NewApp()
    if app.auth == nil { t.Fatal("NewApp should initialize auth") }
    if app.GetQueue() != nil { t.Fatal("GetQueue with nil watcher should return nil") }
}

// Test 2: setVisible / isVisible round-trip.
func TestVisibilityToggle(t *testing.T) {
    app := NewApp()
    app.setVisible(true)
    if !app.isVisible() { t.Fatal("expected visible after setVisible(true)") }
    app.setVisible(false)
    if app.isVisible() { t.Fatal("expected hidden after setVisible(false)") }
}
```

**Concurrency pattern** (watcher_bridge_test.go lines 163–183):
```go
// Test 3: concurrent visibility reads/writes don't race.
func TestVisibilityConcurrent(t *testing.T) {
    app := NewApp()
    var wg sync.WaitGroup
    for i := 0; i < 50; i++ {
        wg.Add(2)
        go func() { defer wg.Done(); app.setVisible(true) }()
        go func() { defer wg.Done(); _ = app.isVisible() }()
    }
    wg.Wait()
    // Pass if -race didn't complain.
}
```

**Note:** `requestQuit` calls `wruntime.Quit(a.ctx)` — not testable without a live Wails runtime. Cover `intentionalQuit.Store(true)` side effect only (read `a.intentionalQuit.Load()` after).

---

### `src/app/paths_test.go` (NEW — covers `watcherDir` env-var precedence)

**Analog:** `src/app/singleinstance_test.go` lines 13–38 (pattern for pure constant/prefix assertions).

**Pattern — table-driven env-var test:**
```go
func TestWatcherDir_EnvPrecedence(t *testing.T) {
    tests := []struct {
        name     string
        setEnv   map[string]string
        unsetEnv []string
        want     string
    }{
        {"GOMAPI_WATCH_DIR wins",
         map[string]string{"GOMAPI_WATCH_DIR": `C:\override`},
         nil, `C:\override`},
        {"falls back to TEMP",
         map[string]string{"TEMP": `C:\Temp`},
         []string{"GOMAPI_WATCH_DIR"},
         filepath.Join(`C:\Temp`, "go-mapi")},
        // ...TMP fallback, os.TempDir fallback
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            for k, v := range tt.setEnv { t.Setenv(k, v) }
            // ...
            if got := watcherDir(); got != tt.want {
                t.Errorf("watcherDir() = %q, want %q", got, tt.want)
            }
        })
    }
}
```

---

### `src/app/credentials_check_test.go` (NEW — covers guard)

**Analog:** `src/app/auth_test.go` TestAuthCodeURLHasPKCE (lines 132–166) — shows `oauthClientID`/`oauthClientSecret` are exported (lowercase) ldflags-injected package-level vars that tests can set/reset.

**Pattern — ldflags var mutation + cleanup:**
```go
func TestCheckOAuthCredentials_Panics_OnEmpty(t *testing.T) {
    // NOTE: checkOAuthCredentials calls os.Exit(1) — can't call directly.
    // Refactor to return error for testability, keep os.Exit(1) at caller.
    // OR: use a test-only build tag that swaps os.Exit for a panic-style hook.
}
```
Planner may opt to refactor `checkOAuthCredentials` to return an error (tested) and `main.go` calls `os.Exit(1)` on non-nil. This keeps the test simple and preserves D-10 behavior.

---

### `src/app/frontend/vitest.config.ts` (NEW — or fold into `vite.config.ts`)

**Analog:** `src/extension/vitest.config.ts` (lines 1–18).

**Pattern — separate `vitest.config.ts` for clarity** (Svelte-adjusted):
```typescript
// Source pattern: src/extension/vitest.config.ts
import { defineConfig } from 'vitest/config';
import { svelte } from '@sveltejs/vite-plugin-svelte';

export default defineConfig({
  plugins: [svelte({ hot: !process.env.VITEST })],  // disable HMR during tests
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
    include: ['src/**/*.test.{ts,svelte}'],
    exclude: ['node_modules', 'dist', 'wailsjs'],
  },
});
```

**Key differences from the extension's React-era config:**
- `@sveltejs/vite-plugin-svelte` (already a devDep, line 13 of `src/app/frontend/package.json`) instead of `@vitejs/plugin-react`
- Exclude `wailsjs/` — auto-generated bindings, don't test
- Drop `coverage` block (D-08 governs coverage for Go only; frontend coverage deliberately NOT enforced)

**Svelte 5 compat note (CONTEXT §Specifics):** verify `@testing-library/svelte@^5` or later supports runes mode (`$state`, `$props`, `$derived`) — the existing components use runes (see `SignedInHeader.svelte` line 3 `$derived`, `App.svelte` line 20 `$state`).

---

### `src/app/frontend/src/test/setup.ts` (NEW)

**Analog:** `src/extension/src/test/setup.ts` (content shown in bash output).

**Pattern — adapted for Wails (no Chrome API mocking):**
```typescript
// Source pattern: src/extension/src/test/setup.ts
import '@testing-library/jest-dom';
import { vi, beforeEach, afterEach } from 'vitest';

// Stub the auto-generated wailsjs modules — tests mock per-suite via vi.mock.
// No Chrome API stub (Svelte app does not use chrome.* APIs).
// No fetch stub by default — auth.ts and queue.ts call wailsjs bindings,
// not fetch; tests that do need fetch install it with vi.stubGlobal locally.

beforeEach(() => {
  // Clear localStorage — hasSeenPreAuthExplainer reads from it.
  localStorage.clear();
});

afterEach(() => {
  vi.clearAllMocks();
});
```

---

### `src/app/frontend/src/lib/auth.test.ts` (NEW)

**No direct in-repo analog** (`src/extension/` has the closest but uses Chrome APIs, not wailsjs). Use the **@testing-library approach** + mocking `../../wailsjs/go/main/App` + `../../wailsjs/runtime/runtime`.

**Pattern — vi.mock wailsjs bindings:**
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// Mock the auto-generated wailsjs modules BEFORE importing auth.ts.
vi.mock('../../wailsjs/go/main/App', () => ({
  GetAuthStatus: vi.fn(),
  SignIn: vi.fn(),
  SignOut: vi.fn(),
}));
vi.mock('../../wailsjs/runtime/runtime', () => ({
  EventsOn: vi.fn(() => () => {}),  // returns unsubscribe
}));

import { fetchAuthStatus, signIn, signOut, subscribeAuth,
         hasSeenPreAuthExplainer, markPreAuthExplainerSeen } from './auth';
import { GetAuthStatus, SignIn, SignOut } from '../../wailsjs/go/main/App';
import { EventsOn } from '../../wailsjs/runtime/runtime';

describe('auth module', () => {
  it('hasSeenPreAuthExplainer returns false on fresh state', () => {
    expect(hasSeenPreAuthExplainer()).toBe(false);
  });

  it('markPreAuthExplainerSeen persists, hasSeen reads it back', () => {
    markPreAuthExplainerSeen();
    expect(hasSeenPreAuthExplainer()).toBe(true);
  });

  it('fetchAuthStatus calls GetAuthStatus binding', async () => {
    (GetAuthStatus as any).mockResolvedValue({ authenticated: true, email: 'x@y.com' });
    await expect(fetchAuthStatus()).resolves.toEqual({ authenticated: true, email: 'x@y.com' });
  });

  it('subscribeAuth wires EventsOn("auth-changed", cb) and returns unsubscribe', () => {
    const unsub = vi.fn();
    (EventsOn as any).mockReturnValue(unsub);
    const cb = vi.fn();
    const ret = subscribeAuth(cb);
    expect(EventsOn).toHaveBeenCalledWith('auth-changed', expect.any(Function));
    expect(ret).toBe(unsub);
  });
});
```

---

### `src/app/frontend/src/lib/queue.test.ts` (NEW)

**Pattern — same vi.mock approach, plus error-path coverage:**
```typescript
vi.mock('../../wailsjs/go/main/App', () => ({ GetQueue: vi.fn() }));
vi.mock('../../wailsjs/runtime/runtime', () => ({ EventsOn: vi.fn(() => () => {}) }));

describe('queue module', () => {
  it('fetchQueue returns [] when binding returns null (WR-03 regression)', async () => {
    (GetQueue as any).mockResolvedValue(null);
    await expect(fetchQueue()).resolves.toEqual([]);
  });

  it('subscribeQueue calls onError when fetchQueue rejects', async () => {
    const onChange = vi.fn();
    const onError = vi.fn();
    let eventHandler: () => void;
    (EventsOn as any).mockImplementation((_: string, h: () => void) => {
      eventHandler = h; return () => {};
    });
    (GetQueue as any).mockRejectedValue(new Error('ipc fail'));

    subscribeQueue(onChange, onError);
    eventHandler!();  // simulate emit
    await new Promise(r => setTimeout(r, 0));  // flush microtask
    expect(onError).toHaveBeenCalledWith(expect.any(Error));
  });
});
```

Covers WR-03 regression locked in `08-REVIEW-FIX.md` (see queue.ts lines 22–32).

---

### `src/app/frontend/src/lib/components/SignInScreen.test.ts` (NEW, Svelte component)

**No repo analog.** Use @testing-library/svelte (external docs).

**Pattern for props + event:**
```typescript
import { render, fireEvent } from '@testing-library/svelte';
import { vi } from 'vitest';
import SignInScreen from './SignInScreen.svelte';

it('renders sign-in button and calls onSignIn when clicked', async () => {
  const onSignIn = vi.fn();
  const { getByRole } = render(SignInScreen, { props: { onSignIn } });
  const btn = getByRole('button', { name: /sign in with google/i });
  await fireEvent.click(btn);
  expect(onSignIn).toHaveBeenCalledOnce();
});
```

**Repo reference for component props shape:** `src/app/frontend/src/lib/components/SignInScreen.svelte` line 2 uses Svelte 5 `$props()` destructuring — @testing-library/svelte `render` passes plain object to `props`.

**Equivalent tests for sibling components:**
- `PreAuthModal.test.ts`: test onContinue / onCancel callbacks, presence of "Advanced" + "Go to go-mapi (unsafe)" copy
- `ReAuthBanner.test.ts`: test onRestore callback, role="alert"
- `SignedInHeader.test.ts`: test email preferred over name in display (`$derived` logic — see line 3), onSignOut callback

---

### `src/app/frontend/src/App.test.ts` (NEW — smoke-level per D-09)

**Scope per CONTEXT D-09:** "at least a render-and-mount smoke test."

**Pattern:**
```typescript
vi.mock('../wailsjs/go/main/App', () => ({
  GetAuthStatus: vi.fn().mockResolvedValue({ authenticated: false }),
  GetQueue: vi.fn().mockResolvedValue([]),
  SignIn: vi.fn(), SignOut: vi.fn(),
}));
vi.mock('../wailsjs/runtime/runtime', () => ({ EventsOn: vi.fn(() => () => {}) }));

it('mounts without throwing and renders sign-in on unauthenticated state', async () => {
  const { findByText } = render(App);
  expect(await findByText(/Sign in with Google/i)).toBeInTheDocument();
});
```

Do NOT over-cover `App.svelte`'s logic here — that's Phase 9 work (per CONTEXT "UI component tests beyond what exists today → Phase 9").

---

### `src/app/frontend/package.json` (MODIFY)

**Analog:** `src/extension/package.json` (lines 11–35) — scripts + devDeps block.

**Current `src/app/frontend/package.json`** (lines 6–20):
```json
"scripts": {
  "dev": "vite", "build": "vite build", "preview": "vite preview",
  "check": "svelte-check --tsconfig ./tsconfig.json"
},
"devDependencies": {
  "@sveltejs/vite-plugin-svelte": "^5.0.0",
  "@tsconfig/svelte": "^5.0.0",
  "svelte": "^5.0.0", "svelte-check": "^4.0.0",
  "tslib": "^2.6.0", "typescript": "^5.0.0",
  "vite": "^6.0.0"
}
```

**Additions (derived from `src/extension/package.json` pattern lines 13–15 + 24–34):**
```json
"scripts": {
  // ...existing + add:
  "test": "vitest",
  "test:run": "vitest run"
},
"devDependencies": {
  // ...existing + add:
  "vitest": "^2.1.0",                    // Vitest 2.x for Vite 6 compat — verify
  "@testing-library/svelte": "^5.0.0",   // Svelte 5 runes support — VERIFY
  "@testing-library/jest-dom": "^6.4.0", // matcher utilities
  "jsdom": "^24.0.0"                     // DOM in Node
}
```

**Lockfile discipline (user global rule `lockfile-discipline.md`):** planner commits `package-lock.json` with `package.json` in the same commit.

**No `@vitest/coverage-v8`:** frontend coverage deliberately not enforced (D-08 covers Go only).
**No `vitest-chrome`:** Svelte app has no Chrome API; extension-era dep irrelevant.

---

### `.changeset/config.json` (MODIFY — wave 10)

**Analog:** itself (lines 1–20).

**Pattern — minimal edit, preserve all other fields:**
```diff
-  "$workspaces": [
-    "src/extension",
-    "src/native-host",
-    "src/app"
-  ]
+  "$workspaces": [
+    "src/app"
+  ]
```

**Landmine check** (from CONTEXT §Potential Landmines): before this edit, planner runs `ls .changeset/*.md` to verify no pending changesets reference `go-mapi-extension` or `go-mapi-native-host` (stale changesets fail `changeset version`).

---

### `go.work` (MODIFY — wave 1)

**Analog:** itself (lines 1–7).

**Pattern — drop `./src/native-host` from use block:**
```diff
 go 1.25.0

 use (
   ./internal/mapi
   ./src/app
-  ./src/native-host
 )
```

**Cross-reference D-21:** "Final `go.work`: `go 1.25.0` + `use (./internal/mapi ./src/app)`."

**Landmine cross-check (CONTEXT §Potential Landmines):** `go.work.sum` may need `go work sync` after the use-block edit. Planner runs `go work sync` before committing wave 1.

---

### `package.json` (MODIFY — waves 1, 2, 4, 5, 9)

**Cumulative result after wave 10:**
```json
{
  "name": "go-mapi",
  "description": "MAPI to Gmail bridge for Windows — Wails desktop app",
  "private": true,
  "workspaces": ["src/app"],            // or ["src/app/frontend"] per D-22 planner check
  "scripts": {
    "build:interceptor":        "powershell ... build.ps1 -Config Release",
    "build:interceptor:debug":  "powershell ... build.ps1 -Config Debug -Tests",
    "clean:interceptor":        "powershell ... Remove-Item ...",
    "test:interceptor":         "powershell ... go-mapi-test-harness.exe ...",
    "test:dll:sandbox":         "powershell ... tests/sandbox/run-sandbox-test.ps1"
    // Dropped: build:*, dev:*, test:extension, test:native-host, test:e2e,
    //          test:coverage, clean:extension, clean:native-host,
    //          package:extension, install:mapi, uninstall:mapi, lint,
    //          aggregated build + test + clean scripts (no longer chain native-host/extension)
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
    // Dropped: @eslint/js, @playwright/test, sharp*, eslint, typescript-eslint
  }
}
```

***sharp dep verification (D-16):*** `scripts/generate-icons.js` line 3 imports `sharp`, line 9 targets `src/extension/public/icons/` — which is deleted in wave 2. The icon-generation script itself becomes orphan. Planner options: (a) delete `generate-icons.js` (extension-era only) and drop sharp; (b) rewrite `generate-icons.js` to target Wails tray assets (`src/app/assets/tray/`) — but the tray icons are already committed as `.ico` files (see `src/app/tray.go` line 12). Conclusion: **delete `scripts/generate-icons.js` + drop `sharp` devDep**. Flag to planner for explicit confirmation.

**New aggregate commands (optional convenience, not required):**
```json
"build":    "npm run build:interceptor && wails build",
"test":     "go test ./internal/mapi/... ./src/app/... && npm run -w @marcfargas/go-mapi-app-frontend test:run"
```

---

## Shared Patterns

### Pattern A — Go test table-driven with `t.Parallel()`

**Source:** `src/app/auth_test.go` lines 18–37 (simple JSON round-trip) and `internal/mapi/protocol_test.go` lines 176–215 (table-driven normalizeAddress).

**Apply to:** all new Go unit tests except: (a) tests that mutate shared package-level vars like `oauthClientID` / `tokenEndpointOverride` / `keyringService` entries (must be sequential, see auth_test.go line 56 comment), (b) Windows-specific tests under `//go:build windows` that touch real kernel resources.

```go
// Source: internal/mapi/protocol_test.go line 176
func TestNormalizeAddress(t *testing.T) {
    tests := []struct {
        input    string
        expected string
    }{
        {"SMTP:user@example.com", "user@example.com"},
        // ...
    }
    for _, tt := range tests {
        result := normalizeAddress(tt.input)
        if result != tt.expected {
            t.Errorf("normalizeAddress(%q) = %q, want %q", tt.input, result, tt.expected)
        }
    }
}
```

### Pattern B — `httptest.Server` + endpoint override var

**Source:** `src/app/auth.go` lines 438–457 (the overrideable endpoints) + `src/app/auth_test.go` lines 295–323 (usage).

**Apply to:** any new test that needs to intercept HTTP traffic the production code would normally send to `oauth2.googleapis.com` or `gmail.googleapis.com`. Keep the `*Override` variable pattern, don't introduce a second mechanism (interface injection, flags, etc.) — consistency matters.

```go
// Source: src/app/auth.go
var (
    tokenEndpointOverride  = ""
    revokeEndpointOverride = ""
)
func tokenEndpoint() string {
    if tokenEndpointOverride != "" { return tokenEndpointOverride }
    return "https://oauth2.googleapis.com/token"
}

// Source: src/app/auth_test.go line 301
srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
    w.WriteHeader(400)
    _, _ = w.Write([]byte(`{"error":"invalid_grant"}`))
}))
defer srv.Close()
tokenEndpointOverride = srv.URL
t.Cleanup(func() { tokenEndpointOverride = "" })
```

### Pattern C — Goroutine lifecycle with `chan struct{}` + `sync.Once`

**Source:** `src/app/watcher_bridge.go` lines 15–45 (bridge struct) + `src/app/watcher_bridge_test.go` lines 122–183 (idempotent Close + concurrent callers).

**Apply to:** any new test exercising a goroutine-owned resource (watcher, listener, dispatcher).

```go
// Source: src/app/watcher_bridge_test.go lines 123–135
func TestBridgeCloseIdempotent(t *testing.T) {
    ctx := context.Background()
    b := newWatcherBridgeWithEmitter(ctx, nil, noopEmitter)
    defer func() {
        if r := recover(); r != nil {
            t.Errorf("Close() panicked on second call: %v", r)
        }
    }()
    b.Close()
    b.Close() // Must not panic.
}
```

### Pattern D — Logging contract (never log tokens/body content)

**Source:** `src/app/logging.go` lines 60–68 + `src/app/auth.go` lines 336, 398, 501, 724 (INFO/ERROR usage).

**Apply to:** any log line in new test scaffolds or helpers. **Never log** token values, email bodies, subjects, attachment contents. **Safe to log:** counts, error types, refresh lifecycle timestamps.

```go
// Source: src/app/auth.go line 336 (good)
logInfo("oauth: signed in as %s", am.email)        // email is low-sensitivity (userinfo)

// Source: src/app/auth.go line 540 (good)
logInfo("oauth: access token refreshed, new expiry %s", am.tokens.Expiry.Format(time.RFC3339))

// NEVER:
// logInfo("oauth: token = %s", am.tokens.AccessToken)   // leaks token
// logInfo("email: subject = %s", mail.Subject)          // leaks body content
```

### Pattern E — Svelte 5 runes in components + `$props()` destructuring

**Source:** `src/app/frontend/src/lib/components/SignInScreen.svelte` line 2, `SignedInHeader.svelte` lines 2–3 (uses both `$props()` and `$derived`).

**Apply to:** every new `*.test.ts` for `.svelte` components. When mocking props, pass plain JS object; when a component uses `$derived`, assert the rendered text, not the derived expression directly.

```svelte
<!-- Source: src/app/frontend/src/lib/components/SignedInHeader.svelte lines 2-3 -->
<script lang="ts">
  let { email, name, onSignOut }: { email: string; name: string; onSignOut: () => void } = $props();
  const displayName = $derived(email || name || 'your Google account');
</script>
```

```typescript
// In test:
render(SignedInHeader, { props: { email: 'a@b', name: 'A', onSignOut: vi.fn() } });
// Then assert rendered text contains 'a@b' (email wins over name per $derived).
```

### Pattern F — Windows build tag for platform-specific tests

**Source:** `src/app/sessionend_test.go` line 1 + `src/app/singleinstance_test.go` line 1.

**Apply to:** `src/app/auth_keyring_windows_test.go` (new, D-11 integration) and any future test that touches Win32 APIs, Credential Manager, or kernel mutexes.

```go
//go:build windows

package main

import ( ... )
```

**CI runtime:** `build-wails-app` job runs on `windows-latest` (D-17) so these execute on every PR. No special workflow needed.

---

## No Analog Found

Files with no close in-repo match — planner relies on external references:

| File | Role | Reason |
|------|------|--------|
| `src/app/frontend/src/lib/*.test.ts` and `src/app/frontend/src/lib/components/*.test.ts` | Svelte 5 test scaffold | No Svelte tests exist in the repo today. Nearest analog is `src/extension/src/test/*` (React + Chrome mocks), which is deleted in wave 2. Use @testing-library/svelte docs + Vitest docs. |
| `src/app/frontend/vitest.config.ts` | Svelte-flavor Vitest config | Structure inherits from `src/extension/vitest.config.ts` (deleted in wave 2), but plugin swap React → Svelte is non-trivial. |
| `KeyringStore` interface in `src/app/auth.go` (introduced in 8.1 if D-11 adopted) | new Go interface | No existing `KeyringStore`-like abstraction; current code calls `zalando/go-keyring` package-level funcs directly. Planner introduces a new shape. |

---

## Metadata

**Analog search scope:**
- `src/app/` — all Go source + tests, assets, wails.json, package.json
- `src/app/frontend/` — Svelte sources, components, vite.config.ts, tsconfig.json, package.json
- `internal/mapi/` — all Go source + tests + testutil
- `src/native-host/` — delete-target, but `gmail_test.go` + `mime_golden_test.go` inspected as port sources
- `src/extension/` — delete-target, but `vitest.config.ts` + `test/setup.ts` inspected as Vitest config analogs
- `.github/workflows/` — all 7 workflows inspected (preserve: `build.yml` §build-interceptor, `go-race-nightly.yml`, `version-packages.yml`; delete/rewrite rest)
- Root-level: `package.json`, `.changeset/config.json`, `go.work`, `CLAUDE.md`, `README.md`
- `scripts/`, `tests/`, `docs/` — inspected per CONTEXT §Claude's Discretion

**Files scanned:** ~50 (incl. deletion-targets inspected for port candidates)
**Pattern extraction date:** 2026-04-18
**Reference to upstream CONTEXT:** `.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-CONTEXT.md` D-01..D-22
