---
phase: 08.1-post-pivot-cleanup-and-test-coverage-review
plan: 05
type: execute
wave: 3
depends_on: ["04"]
files_modified:
  - src/extension
  - src/installer
  - eslint.config.mjs
  - .github/workflows/installer-release.yml
  - .github/workflows/installer-smoke.yml
  - package.json
  - package-lock.json
  - .changeset/config.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/extension/ directory does not exist"
    - "src/installer/ directory does not exist"
    - "eslint.config.mjs at repo root does not exist"
    - ".github/workflows/installer-release.yml does not exist"
    - ".github/workflows/installer-smoke.yml does not exist"
    - "root package.json workspaces array is ['src/app'] — neither extension nor native-host"
    - "root package.json devDependencies contains @changesets/cli only (sharp removed because generate-icons.js — its sole consumer — was deleted)"
    - "root package.json does not contain scripts: build:extension, dev:extension, test:extension, clean:extension, package:extension, lint"
    - "root package.json aggregator scripts (build, test, clean) reference only interceptor (no extension, no native-host)"
    - ".changeset/config.json $workspaces array is ['src/app']"
    - "Must-hold invariant all green after this plan"
  artifacts:
    - path: "package.json"
      provides: "Cleaned root monorepo config — only src/app workspace, only @changesets/cli devDep (plus sharp if kept)"
  key_links:
    - from: "package.json"
      to: "src/app only"
      via: "workspaces field"
      pattern: "\"workspaces\":\\s*\\[\"src/app\"\\]"
---

<objective>
Execute cleanup wave 2 per CONTEXT D-02: hard-delete `src/extension/` (React browser extension), `src/installer/` (Inno Setup), the root `eslint.config.mjs` (extension-only linter), and the two installer CI workflows. Also drop all ESLint devDeps and the `lint` / `package:extension` scripts. Honors D-13, D-14 (delete e2e/playwright workflow partially — e2e.yml + tests/e2e go in Plan 06), D-16, D-19. Also resolves D-16's `sharp` decision: PATTERNS.md confirmed `scripts/generate-icons.js` only targets the deleted `src/extension/public/icons/` — delete the script + drop `sharp`.

Purpose: The browser extension + Inno Setup installer are v2.x artifacts retired by the Wails pivot (D-13, D-03). `eslint.config.mjs` only lints `src/extension/src/`; with the extension gone, the config is dead weight (D-19: frontend linting delegated to svelte-check + strict tsc). The installer workflows reference `src/installer/go-mapi.iss` which is being deleted — they must go in the same commit to keep CI green.

Output:
- `src/extension/` gone
- `src/installer/` gone
- `eslint.config.mjs` gone
- Two installer workflow files gone
- `scripts/generate-icons.js` gone
- Root `package.json` reduced to `{workspaces: ["src/app"], devDependencies: {"@changesets/cli": "..."}}` + interceptor scripts + DLL-sandbox + changeset convenience scripts
- `.changeset/config.json` `$workspaces` reduced to `["src/app"]`
- Must-hold invariant still green
</objective>

<execution_context>
@C:/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-CONTEXT.md
@.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-PATTERNS.md
@package.json
@.changeset/config.json

<interfaces>
<!-- Current-state references. -->

Expected removals this plan:
- `src/extension/` (React popup, vitest.config.ts, chrome mocks, dist, manifests, service-worker, everything)
- `src/installer/` (go-mapi.iss, tests/installer.Tests.ps1, dist/, README.md)
- `eslint.config.mjs` (root)
- `.github/workflows/installer-release.yml`
- `.github/workflows/installer-smoke.yml`
- `scripts/generate-icons.js` (orphan — targets deleted `src/extension/public/icons/`)

Root package.json trims this plan:
- workspaces array: remove `"src/extension"` → becomes `["src/app"]`
- scripts to remove: `build:extension`, `dev:extension`, `test:extension`, `package:extension`, `clean:extension`, `lint`
- aggregator scripts: `build` drops `build:extension`; `test` drops `test:extension`; `clean` drops `clean:extension`
- devDependencies to remove: `@eslint/js`, `eslint`, `typescript-eslint`, `sharp`
- devDependencies to keep: `@changesets/cli`
- devDependencies NOT touched here: `@playwright/test` (Plan 06 owns it)

.changeset/config.json trim this plan:
- `$workspaces`: `["src/extension","src/app"]` → `["src/app"]`
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete src/extension, src/installer, eslint.config.mjs, scripts/generate-icons.js, installer workflows</name>
  <files>
    src/extension
    src/installer
    eslint.config.mjs
    scripts/generate-icons.js
    .github/workflows/installer-release.yml
    .github/workflows/installer-smoke.yml
  </files>
  <read_first>
    - `ls src/extension/`, `ls src/installer/` — confirm directories exist.
    - `ls .github/workflows/installer-*.yml` — confirm two files exist.
    - `ls eslint.config.mjs scripts/generate-icons.js` — confirm files exist.
    - CONTEXT D-03 (installer delete), D-13 (extension delete), D-16 (sharp/generate-icons decision), D-19 (eslint config delete).
    - PATTERNS.md §sharp-dep-verification line confirming `scripts/generate-icons.js` targets deleted `src/extension/public/icons/`.
  </read_first>
  <action>
    Delete all of these in one sweep (no archival copies — v2.1.x git tag is the archive per D-01):

    1. **Directories:**
       - `src/extension/` (recursive, force)
       - `src/installer/` (recursive, force)

    2. **Root-level files:**
       - `eslint.config.mjs`
       - `scripts/generate-icons.js`

    3. **CI workflow files:**
       - `.github/workflows/installer-release.yml`
       - `.github/workflows/installer-smoke.yml`

    Bash: `rm -rf src/extension src/installer eslint.config.mjs scripts/generate-icons.js .github/workflows/installer-release.yml .github/workflows/installer-smoke.yml`
    PowerShell: `Remove-Item -Recurse -Force src/extension, src/installer, eslint.config.mjs, scripts/generate-icons.js, .github/workflows/installer-release.yml, .github/workflows/installer-smoke.yml`

    4. Do NOT delete `.github/workflows/build.yml`, `e2e.yml`, `release.yml`, `go-race-nightly.yml`, `version-packages.yml` — Plans 06 and 07 own those.

    5. Do NOT delete `scripts/install.ps1`, `scripts/package-extension.ps1` — Plan 06 owns those (they belong with the e2e/installer-era cleanup).

    6. Do NOT delete `scripts/azure-ram-gate.ps1`, `scripts/dev-wails.ps1`, `scripts/measure-ram.ps1`, `scripts/test-drop-email.ps1`, `scripts/azure-ram-gate.README.md` — these are Wails-era, preserved.

    7. After deletion, quick sanity:
       ```
       ls src/ | sort
       # Expect: app, interceptor   (only two dirs left)
       ```
  </action>
  <acceptance_criteria>
    - `test ! -d src/extension`.
    - `test ! -d src/installer`.
    - `test ! -f eslint.config.mjs`.
    - `test ! -f scripts/generate-icons.js`.
    - `test ! -f .github/workflows/installer-release.yml`.
    - `test ! -f .github/workflows/installer-smoke.yml`.
    - `ls src/ | sort | tr '\n' ','` returns exactly `app,interceptor,` (or `app,interceptor` without trailing comma — just confirm only those two directories remain).
    - All still-needed Wails-era files present: `scripts/dev-wails.ps1`, `scripts/measure-ram.ps1`, `.github/workflows/go-race-nightly.yml`, etc.
  </acceptance_criteria>
  <verify>
    <automated>test ! -d src/extension && test ! -d src/installer && test ! -f eslint.config.mjs && test ! -f scripts/generate-icons.js && test ! -f .github/workflows/installer-release.yml && test ! -f .github/workflows/installer-smoke.yml && test -f scripts/dev-wails.ps1 && test -f .github/workflows/go-race-nightly.yml</automated>
  </verify>
  <done>All six targets removed; Wails-era assets preserved; src/ contains only app/ and interceptor/.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trim root package.json — drop extension workspace, extension scripts, lint script, ESLint devDeps, sharp devDep</name>
  <files>
    package.json
    package-lock.json
  </files>
  <read_first>
    - Current root `package.json` in full. The extension scripts are at lines 16-19, 22, 29 (per original listing; Plan 04 may have shifted line numbers).
    - `.planning/codebase/STACK.md` line 68 — the v2.x STACK doc references `sharp` for "extension icon handling"; Plan 09 will refresh CLAUDE.md but not this file.
  </read_first>
  <action>
    Edit `package.json`:

    1. **workspaces array** — remove `"src/extension"`. Final value: `["src/app"]`.

    2. **Remove scripts** (exact keys):
       - `build:extension`
       - `dev:extension`
       - `test:extension`
       - `package:extension`
       - `clean:extension`
       - `lint`

    3. **Edit aggregator scripts:**
       - `"build": "npm run build:interceptor && npm run build:extension"` (from Plan 04) → `"build": "npm run build:interceptor"` (interceptor only for now; Plan 08 may add a `wails build` convenience).
       - `"test": "npm run test:extension"` (from Plan 04) → DELETE entirely (Plan 08 replaces with the new Wails-era aggregator; having a stale single-step in the interim is worse than no `test` script for one wave).
       - `"clean": "npm run clean:interceptor && npm run clean:extension"` (from Plan 04) → `"clean": "npm run clean:interceptor"`.

    4. **Preserve these scripts** (untouched):
       - `build:interceptor`
       - `build:interceptor:debug`
       - `clean:interceptor`
       - `test:interceptor`
       - `test:dll:sandbox`
       - `install:mapi` (Plan 06 removes)
       - `uninstall:mapi` (Plan 06 removes)
       - `test:e2e` (Plan 06 removes)
       - `test:coverage` (Plan 06 removes)

    5. **Drop devDependencies:**
       - `@eslint/js`
       - `eslint`
       - `typescript-eslint`
       - `sharp` (PATTERNS.md confirmed — `scripts/generate-icons.js` just deleted was its sole consumer)

    6. **Preserve devDependencies:**
       - `@changesets/cli`
       - `@playwright/test` (Plan 06 removes)

    7. Run `npm install` from repo root. `package-lock.json` will shrink significantly (sharp has large native-binary sub-deps).

    8. Stage both `package.json` AND `package-lock.json` in the commit.

    9. Verify no unresolved references:
       ```
       grep -r 'eslint\|typescript-eslint' --include '*.json' --include '*.js' --include '*.mjs' --include '*.ts' .   # excluding node_modules
       ```
       (Except where documentation files reference eslint historically — those get fixed in Plan 09's CLAUDE.md rewrite.)
  </action>
  <acceptance_criteria>
    - `jq -r '.workspaces | join(",")' package.json` returns exactly `src/app`.
    - `jq '.scripts | keys | map(select(. == "build:extension" or . == "dev:extension" or . == "test:extension" or . == "package:extension" or . == "clean:extension" or . == "lint")) | length' package.json` returns 0.
    - `jq -r '.scripts.build' package.json` returns exactly `npm run build:interceptor`.
    - `jq -r '.scripts.test // "ABSENT"' package.json` returns `ABSENT` OR the aggregator was removed (Plan 08 rebuilds it).
    - `jq -r '.scripts.clean' package.json` returns exactly `npm run clean:interceptor`.
    - `jq '.devDependencies | keys | map(select(. == "eslint" or . == "@eslint/js" or . == "typescript-eslint" or . == "sharp")) | length' package.json` returns 0.
    - `jq -r '.devDependencies["@changesets/cli"]' package.json` is non-null.
    - `npm install` exits 0.
    - `grep -c '"sharp"' package-lock.json` drops substantially compared to pre-plan (sharp and all `@img/sharp-*` sub-deps removed).
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.workspaces == ["src/app"] and ([.scripts | keys[] | select(. == "build:extension" or . == "dev:extension" or . == "test:extension" or . == "package:extension" or . == "clean:extension" or . == "lint")] | length == 0) and ([.devDependencies | keys[] | select(. == "eslint" or . == "@eslint/js" or . == "typescript-eslint" or . == "sharp")] | length == 0) and (.devDependencies["@changesets/cli"] != null)' package.json</automated>
  </verify>
  <done>package.json reduced to workspaces=['src/app'], no extension/lint/ESLint traces, sharp dropped; package-lock.json regenerated; both staged together.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Trim .changeset/config.json — $workspaces to ["src/app"]</name>
  <files>
    .changeset/config.json
  </files>
  <read_first>
    - Current `.changeset/config.json` — `$workspaces` should contain `["src/extension","src/app"]` after Plan 04.
    - CONTEXT D-20 — final state `["src/app"]`.
  </read_first>
  <action>
    Edit `.changeset/config.json`:

    1. Change `$workspaces` from `["src/extension","src/app"]` to `["src/app"]`.

    2. Do NOT touch other fields (`changelog`, `commit`, `baseBranch`, `updateInternalDependencies`, `privatePackages`, etc.).
  </action>
  <acceptance_criteria>
    - `jq -r '.["$workspaces"] | join(",")' .changeset/config.json` returns exactly `src/app`.
    - `jq -r '.baseBranch' .changeset/config.json` still returns `main`.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.["$workspaces"] == ["src/app"]' .changeset/config.json</automated>
  </verify>
  <done>.changeset/config.json $workspaces is `["src/app"]` — matches D-20 final state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Must-hold invariant verification + commit</name>
  <files></files>
  <read_first>
    - CONTEXT §Specifics "must-hold invariant".
  </read_first>
  <action>
    Run all four must-hold-invariant commands after mutations:

    1. `npm run build:interceptor` — C++ DLL builds.
    2. `go build ./src/app/... ./internal/mapi/...` — Go builds.
    3. `go test ./src/app/... ./internal/mapi/...` — Go tests pass.
    4. `npm run -w @marcfargas/go-mapi-app-frontend build` — Svelte frontend builds.

    All four must exit 0.

    Commit with message `chore(08.1): drop src/extension, src/installer, ESLint config and installer workflows`.

    Stage:
    - `src/extension/` (deletion)
    - `src/installer/` (deletion)
    - `eslint.config.mjs` (deletion)
    - `scripts/generate-icons.js` (deletion)
    - `.github/workflows/installer-release.yml` (deletion)
    - `.github/workflows/installer-smoke.yml` (deletion)
    - `package.json` + `package-lock.json` (edits)
    - `.changeset/config.json` (edit)

    Do NOT stage anything else.
  </action>
  <acceptance_criteria>
    - All four must-hold invariant commands exit 0.
    - Commit is atomic — one commit contains all deletions + edits.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:interceptor && go build ./src/app/... ./internal/mapi/... && go test ./src/app/... ./internal/mapi/... && npm run -w @marcfargas/go-mapi-app-frontend build</automated>
  </verify>
  <done>Wave 2 cleanup committed atomically; repo still builds + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan deletes code and config; the CI workflows being deleted reference source trees also being deleted in the same commit, so no dangling CI jobs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-05-01 | D (denial-of-service via broken CI) | two installer workflow files | mitigate | Deleted in same commit as `src/installer/` (their sole source input). No orphan workflow references. |
| T-08.1-05-02 | T (tampering) | scripts/generate-icons.js removal | accept | PATTERNS.md verified the script's only output target (`src/extension/public/icons/`) is deleted in this same commit. Nothing else consumes the script. |
| T-08.1-05-03 | I (lost coverage on ESLint removal) | extension linting | accept | Extension is deleted — there is no TypeScript to lint in `src/extension/`. Svelte frontend relies on svelte-check + strict tsc per D-19; Plan 07 wires svelte-check into CI as a blocking gate. |
| T-08.1-05-04 | D (transitive security deps from sharp) | `@img/sharp-*` native binaries dropped from lockfile | accept | Positive side effect — fewer transitive deps, smaller `npm ci` surface, less attack surface for the dev environment. |
</threat_model>

<verification>
- Six targets removed (two dirs, four files).
- `package.json` trimmed per D-19 + sharp drop; only `@changesets/cli` + `@playwright/test` remain in devDeps (Plan 06 takes `@playwright/test`).
- `.changeset/config.json` `$workspaces` is `["src/app"]` (final per D-20).
- Must-hold invariant holds after the commit.
- Plan 06 can safely proceed with e2e + remaining docs/scripts cleanup.
</verification>

<success_criteria>
- Repository no longer contains the React browser extension, Inno Setup installer, or their CI workflows.
- `eslint.config.mjs` gone; linting delegated to svelte-check + strict tsc per D-19.
- `sharp` + all ESLint devDeps dropped — lighter dev-install footprint.
- `src/` contains exactly two subdirectories: `app` and `interceptor`.
- Must-hold invariant upheld; Plan 06 has a clean baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-05-SUMMARY.md`.
</output>
