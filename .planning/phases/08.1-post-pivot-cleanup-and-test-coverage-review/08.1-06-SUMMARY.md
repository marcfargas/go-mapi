---
phase: 08.1-post-pivot-cleanup-and-test-coverage-review
plan: 06
subsystem: infra
tags: [cleanup, ci, package-json, playwright, extension-era, v2-removal]

requires:
  - phase: 08.1
    provides: "Plans 01-05 already removed src/native-host, src/extension, src/installer, generate-icons, sharp, eslint, ESLint flat config; left tests/e2e + Playwright + extension docs + v2.x install scripts as the wave-4-6 cleanup pickup."
provides:
  - "Repo free of all v2.x Playwright + browser-extension docs + Inno-era PowerShell install scripts"
  - "package.json devDependencies trimmed to a single entry: @changesets/cli (final shape before Plan 08)"
  - "Three CI workflows remaining: build.yml, go-race-nightly.yml, version-packages.yml (release.yml gone — Phase 11 REL-02 owns v3.0 replacement)"
  - "AGENTS.md and CONTRIBUTING.md deleted (both >85% v2.x-specific; Plan 09 rewrites CLAUDE.md as the agent-facing source-of-truth)"
affects: [08.1-07-build-yml-rewrite, 08.1-08-final-trim, 08.1-09-claude-md-readme-rewrite, Phase-11-REL-02-release-pipeline]

tech-stack:
  added: []
  patterns: ["Final-shape root package.json: only @changesets/cli devDep, only build:interceptor / clean:interceptor / test:interceptor / test:dll:sandbox scripts (Plan 08 may add a test aggregator)"]

key-files:
  created:
    - ".planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-06-SUMMARY.md"
  modified:
    - "package.json"
    - "package-lock.json"

key-decisions:
  - "AGENTS.md and CONTRIBUTING.md were deleted outright (not updated). Both files were dominantly v2.x: AGENTS.md described the three-tier architecture / React extension / native-host protocol verbatim; CONTRIBUTING.md walked contributors through `npm run build:extension` / `npm run build:native-host`, generate-icons.js, Chrome/Edge dev-load steps, and Playwright test:e2e. Per the plan's >50% threshold rule, both crossed it. CLAUDE.md (rewritten in Plan 09) plus the user-global agent rules cover the AI-agent role; a future contributing rewrite can land when v3.0 ships (Phase 11 REL-07 covers public-facing docs)."
  - "TODO.txt and PLANNING_AGENT.md were not present at execution time (already absent in the worktree base). Plan listed them as candidates; status surfaced in this summary so the plan checker has explicit evidence rather than silent skip."
  - "scripts/install.ps1 deleted — was the v2.x dev-install script (`install:mapi` / `uninstall:mapi` npm scripts removed in same edit)."
  - "release.yml deleted — Phase 11 REL-02 writes v3.0 release pipeline (NSIS + SignPath) from scratch per CONTEXT D-18. No stub left behind."

patterns-established:
  - "Wave-4-6 cleanup commit pattern: single atomic commit removes Playwright + extension docs + extension-era PowerShell + release.yml + repo-root v2.x docs + drops devDep, regenerates package-lock in same commit, must-hold invariant green before commit."

requirements-completed: []

duration: 4min
completed: 2026-04-18
---

# Phase 08.1 Plan 06: Drop tests/e2e + Playwright + v2.x install scripts + extension-era docs + release workflow Summary

**Removed every v2.x Playwright/install/extension-store leftover from the repo root, CI workflows, scripts/, docs/, and devDependencies — leaving root package.json with `@changesets/cli` as the sole devDep and three CI workflows (build.yml, go-race-nightly.yml, version-packages.yml).**

## Performance

- **Duration:** ~4 min (execution; excludes ~1 min `npm install` warmup needed because the fresh worktree had no node_modules and no frontend dist)
- **Started:** 2026-04-18T21:14:45Z
- **Completed:** 2026-04-18T21:18:25Z
- **Tasks:** 5 (all auto, no checkpoints)
- **Files removed:** 22 (whole `tests/e2e/` tree, `tests/fixtures/` tree, 4 webstore docs, 2 PowerShell scripts, 2 CI workflows, AGENTS.md, CONTRIBUTING.md)
- **Files modified:** 2 (`package.json`, `package-lock.json`)

## Accomplishments

- Deleted `tests/e2e/` (all `.spec.ts`, `mock-gmail/`, `mock-host/`, `playwright.config.ts`, `fixtures.ts`, `fixtures-v2.ts`) and orphan `tests/fixtures/` (5 JSON fixtures consumed only by `src/native-host/` mime golden tests, removed in Plan 04)
- Deleted `.github/workflows/e2e.yml` and `.github/workflows/release.yml` (extension Playwright pipeline + v2.x release pipeline; Phase 11 REL-02 owns v3.0 replacement)
- Deleted `scripts/install.ps1` (v2.x dev-install) and `scripts/package-extension.ps1` (extension packaging)
- Deleted 4 extension-era docs: `docs/chrome-webstore-privacy-justifications.md`, `docs/chrome-webstore-testing-instructions.md`, `docs/webstore-description.md`, `docs/webstore-description.txt`
- Deleted `AGENTS.md` and `CONTRIBUTING.md` (both >85% v2.x-specific per plan's threshold rule)
- Trimmed root `package.json`: removed scripts `test:e2e`, `install:mapi`, `uninstall:mapi`; removed devDep `@playwright/test`. Final devDeps: `{"@changesets/cli": "^2.30.0"}` only.
- Regenerated `package-lock.json` (3 packages removed)
- Must-hold invariant green: `go build ./src/app/... ./internal/mapi/...` exits 0, `go test ./src/app/... ./internal/mapi/...` exits 0, frontend Vite build exits 0

## Task Commits

All five tasks landed in a single atomic commit per the plan's Task 5 instruction:

1. **Task 1 (Pre-flight verification):** No commit — verification-only task. Confirmed `tests/fixtures/` is orphan (zero refs in Go/TS/PS source outside `.planning/` planning docs and `tests/e2e/happy-path.spec.ts` which is being deleted) and `tests/protocol-fixtures/` is still consumed by `internal/mapi/testutil/fixtures.go`.
2. **Task 2 (delete 11 targets):** Part of atomic commit (Task 5).
3. **Task 3 (inspect-and-decide AGENTS.md / PLANNING_AGENT.md / CONTRIBUTING.md):** Part of atomic commit (Task 5). PLANNING_AGENT.md was already absent. AGENTS.md and CONTRIBUTING.md both exceeded the >50% v2.x threshold and were deleted.
4. **Task 4 (package.json + package-lock.json trim):** Part of atomic commit (Task 5).
5. **Task 5 (must-hold invariant + atomic commit):** The single deletions+config commit. Hash: `02b4a32` — `chore(08.1): drop tests/e2e, v2.x scripts + docs, release workflow, and @playwright/test devDep` (29 files changed, 2 insertions, 3729 deletions).

**Plan metadata commit:** Separate commit added `08.1-06-SUMMARY.md` per orchestrator instruction.

## Files Created/Modified

**Created:**
- `.planning/phases/08.1-post-pivot-cleanup-and-test-coverage-review/08.1-06-SUMMARY.md` — this file

**Modified:**
- `package.json` — dropped `test:e2e` / `install:mapi` / `uninstall:mapi` scripts and `@playwright/test` devDep; final devDeps = `{"@changesets/cli": "^2.30.0"}`; final scripts retain only interceptor and sandbox commands
- `package-lock.json` — regenerated via `npm install` after `@playwright/test` drop; 3 packages removed (`@playwright/test` + transitive)

**Deleted (22 paths):**
- `tests/e2e/` (whole tree: 8 files including `extension.spec.ts`, `happy-path.spec.ts`, `install-ux.spec.ts`, `fixtures.ts`, `fixtures-v2.ts`, `playwright.config.ts`, `mock-gmail/`, `mock-host/`)
- `tests/fixtures/` (5 JSON fixtures: malformed, multiple-recipients, simple-email, unicode-content, with-attachment)
- `.github/workflows/e2e.yml`
- `.github/workflows/release.yml`
- `scripts/install.ps1`
- `scripts/package-extension.ps1`
- `docs/chrome-webstore-privacy-justifications.md`
- `docs/chrome-webstore-testing-instructions.md`
- `docs/webstore-description.md`
- `docs/webstore-description.txt`
- `AGENTS.md`
- `CONTRIBUTING.md`

**Preserved (verified per plan §interfaces):**
- `tests/protocol-fixtures/` (consumed by `internal/mapi/testutil/fixtures.go`)
- `tests/sandbox/` (MAPI DLL sandbox harness)
- `.github/workflows/build.yml`, `go-race-nightly.yml`, `version-packages.yml`
- `scripts/azure-ram-gate.ps1`, `azure-ram-gate.README.md`, `dev-wails.ps1`, `measure-ram.ps1`, `test-drop-email.ps1`
- `docs/json-schema.json`, `docs/measurements/`, `docs/testing/`
- `LICENSE`, `COPYING`, `.gitignore`, `.editorconfig` (untouched per plan)
- `CLAUDE.md`, `README.md` (Plan 09 rewrites both)

## Decisions Made

- **AGENTS.md → DELETE.** File explicitly described "Stack: C++ (MinGW), Go, TypeScript + React" and the three-tier architecture diagram (Windows App → C++ DLL → JSON files → Go Native Host → Browser Extension → Gmail API), full sections on each component (`Interceptor`, `Native Host (Go)` with native messaging protocol JSON examples, `Browser Extension (React)` with React 18 / React Bootstrap / Vite / TypeScript dev steps), persona system bound to v2.x components, Common Tasks like "Adding extension settings", and ADRs for "React Bootstrap over Tailwind". Effectively 100% v2.x. The role this file played for AI agents is taken over by CLAUDE.md (Plan 09 rewrites for Wails) plus the user-global rules in `~/.claude/`.
- **CONTRIBUTING.md → DELETE.** Project Structure section enumerated `src/native-host/`, `src/extension/`, `tests/e2e/`, `scripts/install.ps1`, `scripts/generate-icons.js` (all gone). Build commands section taught contributors `npm run build:native-host`, `npm run build:extension`, `npm run dev:extension`, `npm run test:e2e`. "Browser Extension (React)" section walked through Chrome/Edge developer-mode loading. Effectively 95% v2.x. A focused contributing-rewrite can land in a future doc pass when the v3.0 install story is locked (Phase 11 REL-07). Keeping a stale CONTRIBUTING that misleads contributors is worse than no CONTRIBUTING.
- **PLANNING_AGENT.md → not present.** Documented for plan-checker traceability; not a plan deviation.
- **TODO.txt → not present.** Same: documented for traceability, not a deviation.
- Did NOT touch `LICENSE`, `COPYING`, `.gitignore`, `.editorconfig`, `CLAUDE.md`, `README.md`, anything under `.planning/` — per plan's explicit "do not touch" list.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Frontend dist had to be built before baseline could pass**
- **Found during:** Task 1 baseline must-hold-invariant
- **Issue:** `go build ./src/app/...` failed with `pattern all:frontend/dist: no matching files found` because the fresh worktree had no `node_modules/` and no `src/app/frontend/dist/`. The baseline must-hold check required these to exist for the embed directive in `src/app/main.go`.
- **Fix:** Ran `npm install` then `npm run -w @marcfargas/go-mapi-app-frontend build` once before the baseline. This is a one-time worktree warmup, not a code change. No files committed.
- **Files modified:** None (only `node_modules/` and `src/app/frontend/dist/`, both gitignored)
- **Verification:** `go build ./src/app/... ./internal/mapi/...` exited 0; `go test ./src/app/... ./internal/mapi/...` exited 0
- **Committed in:** N/A — no source files changed.

**2. [Scope-discipline] Did NOT clean a stale reference in `tests/sandbox/README.md`**
- **Found during:** Task 1 grep for `tests/e2e`
- **Issue:** `tests/sandbox/README.md` line 120 has a comparison-table row referencing `Playwright tests/e2e/*.spec.ts` and the deleted `e2e.yml`. After my deletions this row points at nothing. There is also a pre-existing stale row at line 119 referencing `src/installer/tests/installer.Tests.ps1` and `installer-smoke.yml` (both gone in Plan 04).
- **Decision:** Did NOT auto-fix. The stale row at line 119 was already broken before Plan 06; my changes only widen the rot, not introduce it. Per `<analysis_paralysis_guard>` and `<scope_discipline>` rules, this is a pre-existing documentation-staleness pattern in a file outside this plan's `files_modified` list. Recommend a future targeted pass on `tests/sandbox/README.md` to rewrite the "Relation to other test surfaces" section for the v3.0 reality (or fold it into Plan 09's CLAUDE.md/README.md rewrite scope check).
- **Tracked for follow-up:** Logged in this summary so the verifier and Plan 09 planner can pick it up.

**3. [Plan deviation note] PLANNING_AGENT.md and TODO.txt listed in plan but not present**
- **Found during:** Task 2 / Task 3 file-existence verification
- **Issue:** Plan §interfaces and §files_modified listed `TODO.txt`, `PLANNING_AGENT.md` as deletion targets. Both were already absent at the worktree base (`78b567eb338e5620505d8030f3a9987d50478bd0`).
- **Fix:** Skipped silently in the deletion sweep (no error). Documented here so the plan-checker has explicit evidence rather than wondering why these don't appear in the commit's deletion stat.
- **Files modified:** None.

---

**Total deviations:** 3 documentation-only items (1 worktree warmup, 1 explicit non-fix with rationale, 1 absent-file note)
**Impact on plan:** Zero scope creep; no source code changes beyond the plan's targets. Must-hold invariant green throughout.

## Issues Encountered

- Fresh worktree required `npm install` + frontend `vite build` before baseline could even compile — pre-existing environmental setup, not a phase issue. Resolved in ~30 sec total.
- C++ interceptor build (`npm run build:interceptor`) was not run as part of the must-hold invariant in this commit. Rationale: no C++ source was touched, the interceptor build requires a Windows-native MinGW toolchain present in the runner, and prior plans 04 and 05 in this phase followed the same pragmatic pattern (Go + frontend gates only). The success-criteria spec only enumerates `go build`/`go test`/frontend; the interceptor was not explicitly listed.

## User Setup Required

None — no external service configuration changed by this plan.

## Next Phase Readiness

- **Plan 07 (build.yml rewrite)** has a clean CI workflow slate. Only three workflow files remain: `build.yml`, `go-race-nightly.yml`, `version-packages.yml`. Plan 07 can replace `build-native-host` + `build-extension` jobs with `build-wails-app` per CONTEXT D-17 without contending with stale workflow files.
- **Plan 08 (final root package.json shape)** picks up a `package.json` with only `@changesets/cli` in devDeps and 7 scripts (build, build:interceptor, build:interceptor:debug, test:interceptor, clean, clean:interceptor, test:dll:sandbox). Per CONTEXT D-22, Plan 08 may add a `test` aggregator and verify the final workspaces shape (`["src/app", "src/app/frontend"]` currently).
- **Plan 09 (CLAUDE.md + README.md rewrite)** will note the absence of AGENTS.md and CONTRIBUTING.md. CLAUDE.md becomes the sole agent-facing canonical doc.
- **No blockers** introduced by this plan.

## Threat Flags

None. Per the plan's `<threat_model>` Task 5 dispositions (T-08.1-06-01..05), all five threats were `mitigate` (handled by the deletions in this commit) or `accept` (orphan fixtures had no PII). No new trust-boundary surface introduced — this plan only removes dead code and config.

## Self-Check: PASSED

Verified after commit `02b4a32` landed:

- All 12 deletion targets gone: `tests/e2e`, `tests/fixtures`, `.github/workflows/e2e.yml`, `.github/workflows/release.yml`, `scripts/install.ps1`, `scripts/package-extension.ps1`, `docs/chrome-webstore-privacy-justifications.md`, `docs/chrome-webstore-testing-instructions.md`, `docs/webstore-description.md`, `docs/webstore-description.txt`, `AGENTS.md`, `CONTRIBUTING.md`.
- All 10 preserved targets still in place: `tests/protocol-fixtures`, `tests/sandbox`, `.github/workflows/build.yml`, `go-race-nightly.yml`, `version-packages.yml`, `docs/json-schema.json`, `LICENSE`, `COPYING`, `package.json`, `package-lock.json`.
- Commit `02b4a32` present in git log.
- `package.json` final shape verified: `jq -e '(.devDependencies | keys) == ["@changesets/cli"] and (.scripts["test:e2e"] == null) and (.scripts["install:mapi"] == null) and (.scripts["uninstall:mapi"] == null)'` returned `true`.

---
*Phase: 08.1-post-pivot-cleanup-and-test-coverage-review*
*Completed: 2026-04-18*
