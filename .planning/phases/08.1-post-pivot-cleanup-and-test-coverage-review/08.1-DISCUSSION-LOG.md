# Phase 8.1: Post-pivot cleanup and test coverage review - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-18
**Phase:** 08.1-post-pivot-cleanup-and-test-coverage-review
**Areas discussed:** Cleanup scope, Coverage methodology, Test scope breadth, Old Playwright E2E fate

---

## Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Cleanup scope | Delete-vs-archive, commit shape, installer + CI workflow fate, doc rewrites | ✓ |
| Coverage methodology | Risk-based vs numeric vs hybrid, QUAL-04 stance, coverage tooling | ✓ |
| Test scope breadth | Go-only vs include Svelte frontend | ✓ |
| Old Playwright E2E fate | Delete, shelve, or rewrite for Wails | ✓ |

**User's choice:** All four areas.

---

## Cleanup scope

### Q1: Archive mode — archive before delete, or hard delete outright?

| Option | Description | Selected |
|--------|-------------|----------|
| Hard delete | git history / v2.1.x tag is the archive | ✓ |
| Archive to docs/legacy/ first | Preserve in-repo reference before deleting | |
| Leave v2.1.x tag as the archive | Same as hard delete + pointer in README | |

**User's choice:** Hard delete (Recommended).

### Q2: Commit shape — how to stage cleanup in git?

| Option | Description | Selected |
|--------|-------------|----------|
| Per-domain commits | Separate commits per cleanup domain, each buildable in isolation | ✓ |
| One big cleanup commit | Single "chore: purge v2.x residue" commit | |
| Delete in plan waves | GSD plan waves own domains | |

**User's choice:** Per-domain commits (Recommended).

### Q3: src/installer/ (Inno Setup) + installer-smoke.yml + installer-release.yml fate?

| Option | Description | Selected |
|--------|-------------|----------|
| Delete now | Phase 10 INST-01 writes NSIS fresh | ✓ |
| Keep until Phase 10 NSIS replacement lands | Avoid installer CI gap | |
| Delete CI gates, keep files | Worst of both | |

**User's choice:** Delete now (Recommended).

### Q4: README.md + CLAUDE.md rewrite timing?

| Option | Description | Selected |
|--------|-------------|----------|
| CLAUDE.md now, README defer to REL-07 | Only fix agent-facing doc now | |
| Both now | Rewrite both in 8.1 | ✓ |
| Neither now, both in Phase 11 | Leave both stale until REL-07 | |

**User's choice:** Both now. **Note:** user diverged from recommendation, signaling preference for aggressive docs cleanup even knowing REL-07 will re-polish README.

### Q5: build.yml v2.x jobs — what happens?

| Option | Description | Selected |
|--------|-------------|----------|
| Replace with build-wails-app job now | Delete build-native-host + build-extension, add build-wails-app | ✓ |
| Delete v2.x jobs, defer Wails CI to Phase 10 | Keep only build-interceptor + Go tests | |
| Keep everything, fix in Phase 10 | Accept broken CI | |

**User's choice:** Replace with build-wails-app job now (Recommended).

### Q6: release.yml fate?

| Option | Description | Selected |
|--------|-------------|----------|
| Delete now | Phase 11 REL-02 writes v3.0 pipeline from scratch | ✓ |
| Stub it out (workflow_dispatch only) | Preserve shape for Phase 11 | |
| Rewrite for Wails binary now | Premature without signing + installer | |

**User's choice:** Delete now (Recommended).

### Q7: eslint.config.mjs fate?

| Option | Description | Selected |
|--------|-------------|----------|
| Delete now | Frontend uses svelte-check + tsc only | ✓ |
| Rewrite for Svelte + TS | Add eslint-plugin-svelte | |
| Keep current config, rescope ignores | Fragile (rules were React-specific) | |

**User's choice:** Delete now (Recommended).

### Q8: Changesets workspaces — trim or retire?

| Option | Description | Selected |
|--------|-------------|----------|
| Trim to just src/app | Keep changesets + version-packages.yml | ✓ |
| Retire changesets entirely | Delete .changeset/ + version-packages.yml | |
| Trim + promote src/app version to root package.json | Loses Version Packages PR automation | |

**User's choice:** Trim to just src/app (Recommended).

### Notes — Cleanup scope

User consistently picked "delete now" + aggressive cleanup over "defer / stub / archive" options across the area. Explicit divergence from recommendation on README rewrite timing (chose "Both now" over "defer README"). Signal: user wants a clean repo at the end of 8.1 with no v2.x residue anywhere, even in docs.

---

## Coverage methodology

### Q1: Coverage philosophy?

| Option | Description | Selected |
|--------|-------------|----------|
| Risk-based gap filling | Carry PROJECT.md v2.x decision forward | ✓ |
| Numeric floor + risk-based (hybrid) | Minimum per-package coverage as safety net | |
| Numeric floor across the board | Flat ≥60% everywhere | |

**User's choice:** Risk-based gap filling (Recommended).

### Q2: Rubric — tier definitions?

| Option | Description | Selected |
|--------|-------------|----------|
| Three tiers: Critical / Standard / Glue | Explicit rubric guides planner | |
| Two tiers: Critical / Everything else | Simpler | |
| No explicit rubric — planner decides | Planner judgement per file | ✓ |

**User's choice:** No explicit rubric — planner decides. **Note:** diverged from recommended three-tier rubric; prefers planner judgement over prescriptive categories.

### Q3: QUAL-04 (`go test -race`) stance?

| Option | Description | Selected |
|--------|-------------|----------|
| Promote -race to per-PR gate | Add -race to build-wails-app; keep nightly; close QUAL-04 | ✓ |
| Keep nightly-only, mark QUAL-04 done | Lightest change | |
| Both: nightly full + per-PR targeted | Path-filtered per-PR race | |

**User's choice:** Promote -race to per-PR gate (Recommended).

### Q4: Coverage measurement tooling?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, report but don't gate | coverprofile artifact, no threshold | ✓ |
| No, skip coverage tooling | Risk-based doesn't need numbers | |
| Yes, with per-package thresholds | Gate per-package minimums | |

**User's choice:** Yes, report but don't gate (Recommended).

### Notes — Coverage methodology

User combined risk-based philosophy with planner-judgement rubric (no explicit tier labels) and coverage-reporting-without-gating. Consistent thread: trust planner + review, measure for signal not enforcement.

---

## Test scope breadth

### Q1: Frontend inclusion?

| Option | Description | Selected |
|--------|-------------|----------|
| Include frontend: lib/ only | Vitest for pure logic modules | |
| Include frontend: lib + components | Vitest + @testing-library/svelte, component DOM tests | ✓ |
| Go-only; defer frontend to Phase 9 | Frontend tested when queue UI lands | |

**User's choice:** Include frontend: lib + components. **Note:** user went broader than recommended — wants component DOM tests in 8.1, not just pure logic.

### Q2: svelte-check CI wire-up?

| Option | Description | Selected |
|--------|-------------|----------|
| Add to build-wails-app CI | Blocking gate on svelte-check | ✓ |
| Keep local-only | Developer discipline | |
| Nightly-only | Slower feedback | |

**User's choice:** Add to build-wails-app CI (Recommended).

### Q3: Keyring test strategy?

| Option | Description | Selected |
|--------|-------------|----------|
| Interface + in-memory fake for tests, real on Windows CI | Fast unit tests + Windows integration test | ✓ |
| Real keyring only, Windows-CI only | No fake, simpler | |
| Whatever Phase 8 already set up | Extend existing pattern | |

**User's choice:** Interface + in-memory fake for tests, real on Windows CI (Recommended).

### Q4: OAuth HTTP test strategy?

| Option | Description | Selected |
|--------|-------------|----------|
| httptest.Server | Stdlib local server emulating Google endpoints | ✓ |
| Interface-based transport mock | http.Client with RoundTripper mock | |
| Whatever Phase 8 already uses | Extend existing pattern | |

**User's choice:** httptest.Server (Recommended).

---

## Old Playwright E2E fate

### Q1: tests/e2e fate?

| Option | Description | Selected |
|--------|-------------|----------|
| Delete everything now | Remove specs + mocks + fixtures + workflow | ✓ |
| Shelve as skip-until-rewritten | Preserve bootstrap + disable tests | |
| Rewrite for Wails now | Attach Playwright to Wails dev-server | |

**User's choice:** Delete everything now (Recommended).

### Q2: e2e.yml CI workflow?

| Option | Description | Selected |
|--------|-------------|----------|
| Delete in the same commit | Together with tests/e2e | ✓ |
| Keep workflow, stub it out | Preserve slot + badge | |
| Rewrite for Wails smoke now | Minimal Wails-build check | |

**User's choice:** Delete in the same commit (Recommended).

### Q3: Future Wails e2e strategy?

| Option | Description | Selected |
|--------|-------------|----------|
| Deferred Ideas note + pointers to Phase 10/11 | CONTEXT.md references INST-07 + REL-06 | ✓ |
| Plant a seed in the backlog | /gsd-plant-seed forward-looking trigger | |
| New phase 8.2 or follow-up todo | Adds scope | |

**User's choice:** Deferred Ideas note + pointers to Phase 10/11 (Recommended).

### Q4: Root devDeps cleanup?

| Option | Description | Selected |
|--------|-------------|----------|
| Drop @playwright/test + sharp + eslint pkgs | Root-level cleanup | ✓ |
| Drop Playwright + eslint only, keep sharp | Preserve icon pipeline potential | |
| Let planner audit root package.json holistically | Most thorough | |

**User's choice:** Drop @playwright/test + sharp + eslint pkgs (Recommended). **Note:** CONTEXT.md D-16 caveats sharp as "verify during planning" in case Wails icon pipeline still uses it.

---

## Claude's Discretion

Areas where the user deferred implementation detail to Claude/planner (see CONTEXT.md §Claude's Discretion for full list):

- Exact wording of CLAUDE.md + README.md rewrites
- Whether `sharp` devDep stays (depends on Wails icon pipeline)
- Fate of AGENTS.md, PLANNING_AGENT.md, CONTRIBUTING.md at repo root
- Fate of docs/json-schema.json, docs/testing/, docs/measurements/
- Fate of `.planning/codebase/*.md` maps (stale — flag or refresh post-8.1)
- Frontend test file layout (co-located vs __tests__/)
- Depth of component tests
- Whether to add a Wails App struct full-lifecycle integration test

## Deferred Ideas

See CONTEXT.md §deferred for the canonical list. Key entries:

- Wails end-to-end suite → Phase 10 INST-07 + Phase 11 REL-06
- `.planning/codebase/*.md` refresh → post-8.1 `/gsd-map-codebase` run
- `docs/legacy/v2/` migration guide → REL-07 (Phase 11) if needed
- Changesets retirement → reassess at v3.0 GA or Phase 11 release work
- `eslint-plugin-svelte` reintroduction → future phase if needed
