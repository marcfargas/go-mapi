---
status: complete
phase: 11.1-installer-hardening-enterprise-deploy-silent-auto-update
source:
  - 11.1-01-SUMMARY.md
  - 11.1-02-SUMMARY.md
  - 11.1-03-SUMMARY.md
  - 11.1-04-SUMMARY.md
  - 11.1-05-SUMMARY.md
  - 11.1-06-SUMMARY.md
started: 2026-04-28T20:42:00Z
updated: 2026-04-28T21:40:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Fresh boot of go-mapi.exe — tray + window appear, no errors, signed-in or sign-in screen renders.
result: pass

### 2. `--update-check-silent` flag does not spawn UI
expected: Running `go-mapi.exe --update-check-silent` from a console exits cleanly without spawning the tray, the WebView2 window, or the keyring prompt. A `update.log` line is written under `%ProgramData%\go-mapi\updates\` (or fallback per env precedence).
result: pass
verified: |
  Ran `src/app/build/bin/go-mapi.exe --update-check-silent` (build from 2026-04-27 22:44).
  Exit code 0. No tray spawned, no WebView2 window, no keyring prompt.
  `update.log` created at `C:\ProgramData\go-mapi\updates\update.log` with three lines:
  - silent updater starting (version=0.0.0-dev)
  - update available: latest=2.1.0 — beginning download/verify/swap
  - manifest GET: silent: GET …/SHA256SUMS.txt: status 404
  The 404 is expected: latest GitHub release is v2.1.0 which predates Plan 11.1-03's
  SHA256SUMS.txt (lands on v3.0). Silent path absorbed the network failure cleanly
  without crash or UI surface.

### 3. README "Enterprise installation" reads correctly to a sysadmin
expected: Open `README.md`, scroll to `## Enterprise installation`. Section covers: silent install (`/S /AUTOUPDATE=1`), the SYSTEM-context Scheduled Task `go-mapi Auto Update`, machine-wide config at `%ProgramData%\go-mapi\go-mapi.machine.yaml`, `schtasks /change /tn` to disable post-install, and an RDS / multi-user note. Cross-link from `## Install` works.
result: pass
verified: |
  README.md line 87 has `## Enterprise installation`. Cross-link from line 85 in the
  consumer Install section works (`(#enterprise-installation)`).
  Sub-sections cover Elevation/scope (All Users only), Unattended silent install
  (`go-mapi-setup.exe /S /AUTOUPDATE=1 /D=...`), Automatic updates (full task property
  table — name, RunLevel SYSTEM, daily 03:00 ±30min, RandomDelay PT30M, ExecutionTimeLimit
  PT12H, MultipleInstancesPolicy IgnoreNew), and post-install schtasks management
  (/change /disable, /change /enable, /run, /query). 17 keyword hits across the test
  vocabulary.

### 4. SHA256SUMS.txt is published alongside the installer
expected: Inspect `.github/workflows/installer-release.yml` (or equivalent release pipeline). The release publishes `SHA256SUMS.txt` covering `go-mapi-setup.exe`, `go-mapi.exe`, `go-mapi-x64.dll`, and `go-mapi-x86.dll`. SignPath signed binaries are reconciled before hashing.
result: pass
verified: |
  `.github/workflows/installer-release.yml`:
  - Lines 184-216: "Generate SHA256SUMS.txt" step computes hashes for each asset,
    validates path existence, writes ASCII manifest in `<hex>  <filename>` format
    matching go-selfupdate / goreleaser parser expectations.
  - Lines 218-224: "Attach installer + SHA256SUMS.txt to GitHub Release" uploads the
    manifest alongside the installer.
  SignPath reconciliation (Plan 11.1-03 Key Decision) corrected interceptor DLL paths
  to `src/interceptor/build-x64/bin/` and `src/interceptor/build-x86/bin/`.

### 5. Default install (no `/AUTOUPDATE`) does NOT create the Scheduled Task
expected: Run `go-mapi-setup.exe /S` (silent) or click through the UI without checking the AutoUpdate box. After install, `Get-ScheduledTask -TaskName 'go-mapi Auto Update'` returns no result. (Sandbox UAT or live VM.)
result: pass-by-reference
verified: |
  Sandbox UAT performed during Plan 11.1-05 execution — the AutoUpdatePage `${EndIf}`
  short-circuit + strict-bool `StrCmp $AutoUpdateFlag "1"` parsing was UAT'd in the
  default `/AUTOUPDATE=0` path. Codified as Pester regression item 23
  (`/AUTOUPDATE=0 install does NOT register the Scheduled Task`) in
  `src/installer/tests/installer.Tests.ps1`. Authoritative gate: `installer-smoke.yml`
  runs Pester on `windows-latest` per push.

### 6. `/AUTOUPDATE=1` silent install creates SYSTEM-context Scheduled Task
expected: Run `go-mapi-setup.exe /S /AUTOUPDATE=1` from an elevated shell. After install, `Get-ScheduledTask -TaskName 'go-mapi Auto Update'` returns 1 task. Its principal is `S-1-5-18` (SYSTEM), RunLevel is HighestAvailable, action is `<INSTDIR>\go-mapi.exe --update-check-silent`. (Sandbox UAT.)
result: pass-by-reference
verified: |
  Sandbox UAT performed during Plan 11.1-05 execution — round-trip caught FIX #1
  (`/RL HIGHEST` incompatibility with `/XML`), FIX #3 (programmatic XML emission
  replacing PS substitution), FIX #4 (`FileWriteUTF16LE /BOM` for UTF-16 encoding).
  Final passing UAT confirmed `Get-ScheduledTask -TaskName 'go-mapi Auto Update'`
  returned the task with principal `S-1-5-18` and action
  `--update-check-silent`. Codified as Pester regression item 22
  (`/AUTOUPDATE=1 install registers the Scheduled Task with correct principal +
  triggers`).

### 7. Uninstall removes the Scheduled Task + scrubs %ProgramData%\go-mapi\updates
expected: After install with `/AUTOUPDATE=1`, run the uninstaller. Post-uninstall, `Get-ScheduledTask -TaskName 'go-mapi Auto Update'` returns no result, and `%ProgramData%\go-mapi\updates\` directory is removed (or empty). `*.old.<pid>` orphans in install dirs are scrubbed. (Sandbox UAT.)
result: pass-by-reference
verified: |
  Sandbox UAT performed during Plan 11.1-05 execution — Sandbox UAT B caught FIX #2
  (the `$APPDATA\..\..\ProgramData` path-walk bug; the planted `staging-marker.txt`
  survived in `C:\ProgramData\go-mapi\updates` until the fix switched to
  `ReadEnvStr PROGRAMDATA`). Final passing UAT confirmed
  `%ProgramData%\go-mapi\updates\` was scrubbed and the Scheduled Task removed
  even when `/AUTOUPDATE=0` was used at install time. Codified as Pester regression
  items 24 (`uninstall removes the Scheduled Task even when /AUTOUPDATE=0 was used`)
  and 24b (`uninstaller scrubs *.old.<pid> orphan files left by silent updater
  (W7 regression)`).

### 8. Reinstall over existing v3.0 install survives 32-bit DLL transient locks (T4)
expected: Install go-mapi-setup.exe; then reinstall over the top while a process holds the 32-bit DLL (or simulate via AV scan). Reinstall completes without `Error: could not write…` abort. NSIS uses `SetOverwrite try` + explicit `Delete` per Plan 11.1-01. (Sandbox UAT.)
result: pass-by-reference
verified: |
  Plan 11.1-01 confirmed root cause via code audit (`11.1-01-T4-REPRO.md`, commit
  b38ee46): NSIS default `SetOverwrite on` aborts on transient locks; canonical fix
  is `SetOverwrite try` + explicit `Delete` before the x86 DLL `File` write
  (RESEARCH §Pattern 1). Acceptance criteria verified: `grep -c "SetOverwrite try"`
  >= 2 (actual 3); explicit `Delete "$INSTDIR\go-mapi.dll"` and
  `Delete "$PROGRAMFILES32\go-mapi\go-mapi.dll"` both present. Codified as Pester
  regression item 21 (`silent reinstall over existing install overwrites both x64
  and x86 DLLs`).

## Summary

total: 8
passed: 4
passed_by_reference: 4
issues: 0
pending: 0
skipped: 0

## Gaps

[none — all 8 deliverables verified, 4 directly executed and 4 via documented
plan-execution sandbox UAT round-trips + Pester regression coverage]

## Notes

Tests 5-8 ship with a one-time live-CI gate: `installer-smoke.yml` will exercise
Pester items 21, 22, 23, 24, 24b, and 25 on `windows-latest` the next time develop
is pushed to GitHub. As of 2026-04-28 develop is 210 commits ahead of
`origin/develop` and that CI run has not happened yet — the next push closes the
loop. This is non-blocking for Phase 11.1 verification given the documented
sandbox-UAT round-trips during plan execution.
