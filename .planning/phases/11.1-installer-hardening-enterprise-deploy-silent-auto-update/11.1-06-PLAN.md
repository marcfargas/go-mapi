---
phase: 11.1
plan: 06
type: execute
wave: 3
depends_on: [11.1-04, 11.1-05]
files_modified:
  - README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REL-07, REL-09]
must_haves:
  truths:
    - "README.md gains an 'Enterprise installation' section AFTER the existing consumer Install section, documenting: All Users elevation requirement, /AUTOUPDATE=1 silent-install parameter, Scheduled Task `go-mapi Auto Update` management, SHA-256 verification expectation, multi-user RDS caveat"
    - "Existing consumer install language stays intact (D-19) — no rewrites of pre-existing copy"
    - "ROADMAP §Phase 11.1 success criteria match the simplified scope: binary checkbox + Scheduled Task + SHA-256 manifest. Tri-state language and machine-config bullets removed (D-20)"
    - "REQUIREMENTS.md gains REL-09 with traceability mapping it to Phase 11.1; REL-03 + REL-04 entries note the silent-path extension"
    - "Phase 11.1 success-criteria block matches the actual delivered scope (so downstream agents reading ROADMAP get the truth, not the stale pre-CONTEXT text)"
  artifacts:
    - path: README.md
      provides: "## Enterprise installation section + cross-link from existing Install section"
    - path: .planning/ROADMAP.md
      provides: "Phase 11.1 success-criteria rewrite + plan list with summary references"
    - path: .planning/REQUIREMENTS.md
      provides: "REL-09 entry + traceability table update"
  key_links:
    - from: "README.md ## Install (existing)"
      to: "README.md ## Enterprise installation (new)"
      via: "cross-reference link / `See also` line"
      pattern: "Enterprise installation"
    - from: ".planning/REQUIREMENTS.md REL-09"
      to: ".planning/ROADMAP.md Phase 11.1 entry"
      via: "Phase column in traceability table"
      pattern: "REL-09\\s+\\|\\s+Phase 11\\.1"
---

<objective>
Land the documentation deliverables that close out Phase 11.1: README enterprise-install section, ROADMAP rewrite to match the simplified scope, REQUIREMENTS.md REL-09 insertion + traceability update. Phase 11.1 changes user-visible behavior (new install param, new Scheduled Task), so docs are not optional.

Purpose: Close the loop between the user-facing surface (D-19, D-20) and the requirements register. After this plan ships, downstream agents reading ROADMAP get the truth (currently stale per D-20), enterprise admins reading README know how to use the new feature, and the requirements traceability is complete.

Output: README enterprise-install H2, ROADMAP Phase 11.1 success-criteria rewrite, REQUIREMENTS.md REL-09 entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-CONTEXT.md
@.planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-RESEARCH.md
@.planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-PATTERNS.md

@README.md

<interfaces>
Existing README.md ## Install section (line 57 area per PATTERNS.md) — KEEP intact.

ROADMAP.md Phase 11.1 entry currently has:
- Goal (still mostly correct — admin can drop a config file is wrong but the rest stands).
- Success Criteria 1-7 — bullets 3 + 4 (machine-wide YAML config + .example files) are STALE per D-02 and must be removed. Bullet 5 (tri-state install) must collapse to "binary checkbox + Scheduled Task". Bullet 7 needs to drop the machine-config-presence verification.
- Plans: TBD — fill in with the 6 actual plans from this phase.

REQUIREMENTS.md §Release & Autoupdate currently has REL-01..REL-08. Add REL-09 with the canonical text:

REL-09: Silent auto-update: a SYSTEM-context Windows Scheduled Task downloads release assets from the stable URL, verifies SHA-256 against `SHA256SUMS.txt`, and atomically swaps `go-mapi.exe` (and the x64 + x86 `go-mapi.dll`) using MoveFileEx rename-while-running, without elevating the interactive user. Install-time opt-in via UI checkbox or `/AUTOUPDATE=1` parameter (default OFF). Uninstaller idempotently removes the Scheduled Task and the staging directory.

Phase: Phase 11.1 (per the new traceability row).

REL-03 + REL-04 entries: append a note "(extended in Phase 11.1: see REL-09)" — purely informational, no functional change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 'Enterprise installation' H2 section to README.md (D-19)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — must understand the current consumer-install language verbatim before adding a sibling section, and identify the right anchor point)
    - .planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-CONTEXT.md (D-19 — exact required content)
    - .planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-PATTERNS.md (§README.md — section ordering)
    - .planning/phases/10-installer-migration/10-CONTEXT.md (D-19 multi-user RDS limitation — the carry-forward caveat to document here)
  </read_first>
  <behavior>
    - Add H2 `## Enterprise installation` AFTER the existing `## Install` section, BEFORE any subsequent unrelated sections (License, Contributing, etc.).
    - Section content covers:
      1. All Users elevation requirement (no per-user path; UAC mandatory; D-01).
      2. `/AUTOUPDATE=1` silent-install parameter — what it does, default OFF.
      3. Scheduled Task `go-mapi Auto Update` — name, principal (SYSTEM), schedule (daily 03:00 + at boot, ±30min jitter), management commands (`schtasks /change /tn "go-mapi Auto Update" /disable|/enable`, Task Scheduler UI).
      4. SHA-256 integrity verification expectation (`SHA256SUMS.txt` published alongside release; admins can verify manually with `Get-FileHash`).
      5. Multi-user RDS caveat: uninstaller does NOT enumerate per-user profiles to scrub (carry-forward from Phase 10 D-19); admin who runs uninstaller sees only their own profile cleaned.
      6. Reference to the consumer Install section above for non-enterprise users.
    - Add a brief cross-link from the existing `## Install` section: a single line "For unattended/managed deployments, see [Enterprise installation](#enterprise-installation) below." — minimal, non-disruptive.
  </behavior>
  <action>
    Edit `README.md`. Locate the existing `## Install` section (around line 57). After the LAST line of the `## Install` section's content (i.e., right before the next H2), add the cross-link line + the new section.

    Cross-link in existing Install section (one line, after the last bullet/paragraph):
    ```
    > For unattended or managed deployments (RDS, MSI/SCCM/Intune fleet roll-outs), see [Enterprise installation](#enterprise-installation) below.
    ```

    New section to add immediately after the existing `## Install` section content ends:

    ```markdown
    ## Enterprise installation

    For administrators deploying go-mapi to a fleet (Windows Server / RDS hosts, MSI / SCCM / Intune push, etc.). For single-user installs, the consumer instructions above cover everything.

    ### Elevation and scope

    `go-mapi-setup.exe` is **All Users only**. The MAPI handler is registered under `HKLM\SOFTWARE\Clients\Mail\go-mapi`, which is inherently machine-wide; there is no per-user install path. The installer requires UAC elevation. Run as an administrator (or via a managed-deployment context that elevates) — there is no "Just for me" option.

    ### Unattended (silent) install

    Silent install with all defaults:

    ```
    go-mapi-setup.exe /S /D=C:\Program Files\go-mapi
    ```

    Add `/AUTOUPDATE=1` to register a Windows Scheduled Task that keeps go-mapi updated automatically (see [Automatic updates](#automatic-updates) below):

    ```
    go-mapi-setup.exe /S /AUTOUPDATE=1 /D=C:\Program Files\go-mapi
    ```

    Default is `/AUTOUPDATE=0` (no Scheduled Task; manual update notification only — same as the consumer install).

    ### Automatic updates

    When installed with `/AUTOUPDATE=1` (or with the "Enable automatic updates" checkbox ticked during interactive install), the installer registers a Windows Scheduled Task:

    | Property | Value |
    |---|---|
    | Task name | `go-mapi Auto Update` |
    | Path | `\go-mapi Auto Update` (root of Task Scheduler) |
    | Run as | `SYSTEM` (no per-user credential, no logon required) |
    | Schedule | Daily 03:00 with ±30 minute random delay |
    | Also runs | At system startup (5 minute delay) |
    | Network | `RunOnlyIfNetworkAvailable=true` (skips offline runs) |
    | Catch-up | `StartWhenAvailable=true` (runs after wake/reboot if missed) |
    | Concurrency | `MultipleInstancesPolicy=IgnoreNew` (no overlapping runs) |
    | Time limit | 12 hours per run (`ExecutionTimeLimit=PT12H`) |

    The task fires `go-mapi.exe --update-check-silent`, which:

    1. Fetches `SHA256SUMS.txt` from the stable Release URL.
    2. Downloads the new binary (or installer) into `%ProgramData%\go-mapi\updates\staging\`.
    3. Verifies the SHA-256 digest **before** writing the binary into the install path.
    4. Atomically swaps the running `go-mapi.exe` (and the x64 + x86 `go-mapi.dll`) using `MoveFileEx`'s rename-while-running pattern. The interactive go-mapi instance keeps running with its old in-memory file mapping until next launch.
    5. Logs to `%ProgramData%\go-mapi\updates\update.log` (admin-readable; no PII, no message content, no hex digests).

    The task does **not** restart the running interactive go-mapi.exe. The new binary takes effect on the next launch.

    #### Managing the Scheduled Task post-install

    Disable temporarily (e.g. during maintenance windows):

    ```
    schtasks /change /tn "go-mapi Auto Update" /disable
    ```

    Re-enable:

    ```
    schtasks /change /tn "go-mapi Auto Update" /enable
    ```

    Run the update check immediately (testing / forced refresh):

    ```
    schtasks /run /tn "go-mapi Auto Update"
    ```

    Inspect last run + status:

    ```
    schtasks /query /tn "go-mapi Auto Update" /v /fo LIST
    ```

    Or open Task Scheduler (`taskschd.msc`) and navigate to `\go-mapi Auto Update`.

    The task is removed automatically by the go-mapi uninstaller. To convert an existing notify-only install to silent-update, re-run `go-mapi-setup.exe /AUTOUPDATE=1` over the existing install — the installer is idempotent.

    ### Integrity verification

    Every release publishes `SHA256SUMS.txt` alongside the installer at:

    ```
    https://github.com/marcfargas/go-mapi/releases/latest/download/SHA256SUMS.txt
    ```

    Format follows the `sha256sum` convention (one line per asset, `<lowercase-hex>  <filename>`). The silent updater verifies downloads automatically; for manual verification:

    ```
    $expected = (Invoke-WebRequest 'https://github.com/marcfargas/go-mapi/releases/latest/download/SHA256SUMS.txt').Content
    $actual = (Get-FileHash -Algorithm SHA256 .\go-mapi-setup.exe).Hash.ToLower()
    Write-Host "Expected: $expected"
    Write-Host "Actual:   $actual  go-mapi-setup.exe"
    ```

    SignPath signing (when present on the release) is additive — verify both the SHA-256 digest AND the Authenticode signature for defense-in-depth.

    ### Multi-user RDS limitation

    The uninstaller scrubs the running admin's profile and machine-wide locations (`HKLM\SOFTWARE\Clients\Mail\go-mapi`, `%ProgramFiles%\go-mapi\`, `%ProgramData%\go-mapi\`, `\go-mapi Auto Update`). It does **not** enumerate every user profile on a multi-user / RDS host to scrub `%APPDATA%\go-mapi\` or per-user shortcuts left by older builds. On RDS hosts where many users have run go-mapi, residue may persist in user profiles after uninstall.

    This is a known carry-forward limitation from Phase 10 (the v3.0 install milestone). Workaround: an admin can run `Remove-Item -Recurse -Force "$Profile\..\..\..\*\AppData\Roaming\go-mapi"` per RDP session as needed, or wait for the v3.x roadmap entry that adds enumerate-all-profiles uninstall.

    ### Privacy posture

    go-mapi makes network calls only to:

    - `https://github.com/marcfargas/go-mapi/releases/latest/download/...` (update check + asset download).
    - Google OAuth + Gmail API (when the user signs in / drafts mail).

    No telemetry. No content retention. No hash-of-installed-binary reporting. Silent-update logs at `%ProgramData%\go-mapi\updates\update.log` record the version transition and download success/failure only — no message body, no recipient data, no SHA-256 digests of installed binaries.
    ```

    Verify the section anchor matches the cross-link target: GitHub renders `## Enterprise installation` as `#enterprise-installation` (lowercase, spaces→hyphens, no other transforms). The cross-link `[Enterprise installation](#enterprise-installation)` matches.

    Do NOT modify any other section. Existing copy stays intact.
  </action>
  <verify>
    <automated>grep -c "## Enterprise installation" README.md && grep -c "/AUTOUPDATE=1" README.md && grep -c "go-mapi Auto Update" README.md && grep -c "SHA256SUMS.txt" README.md && grep -c "Multi-user RDS" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Enterprise installation" README.md` returns exactly 1.
    - `grep -c "/AUTOUPDATE=1" README.md` returns at least 2 (intro + example block).
    - `grep -c "go-mapi Auto Update" README.md` returns at least 4 (table row + schtasks examples).
    - `grep -c "SHA256SUMS.txt" README.md` returns at least 2 (URL reference + manual-verify code).
    - `grep -c "schtasks /change /tn" README.md` returns at least 2 (disable + enable examples).
    - `grep -c "Multi-user RDS" README.md` returns at least 1 (limitation H3).
    - `grep -c "Enterprise installation\\](#enterprise-installation)" README.md` returns at least 1 (cross-link from existing Install section).
    - The existing `## Install` H2 still exists (`grep -cE "^## Install\\b" README.md` returns 1).
    - `grep -c "MoveFileEx\\|rename-while-running" README.md` returns at least 1 (technical accuracy for fleet admins).
  </acceptance_criteria>
  <done>Enterprise install section lands; consumer copy untouched; cross-link in place.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite ROADMAP.md Phase 11.1 success criteria + plan list (D-20)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file — must understand current Phase 11.1 entry verbatim, plus the milestone/phase formatting style for surrounding phases so the rewrite matches the document conventions)
    - .planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-CONTEXT.md (D-20 — exact requirements for the rewrite)
    - .planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-RESEARCH.md (§Phase Requirements table — final REL coverage list)
  </read_first>
  <behavior>
    - Locate the `### Phase 11.1` section in ROADMAP.md.
    - Rewrite the Success Criteria block to match the simplified scope:
      1. Installer is system-wide only (D-01); T2 + T4 fixes landed.
      2. Reinstall over existing install succeeds (T4 fix + Pester regression).
      3. `/AUTOUPDATE=1` silent-install parameter + UI checkbox; default OFF.
      4. Scheduled Task `go-mapi Auto Update` registered with SYSTEM/HIGHEST + daily 03:00 + at-boot + RandomDelay PT30M when enabled; absent when disabled.
      5. `go-mapi.exe --update-check-silent` downloads, verifies SHA-256 (via SHA256SUMS.txt + go-selfupdate ChecksumValidator), and atomically swaps the binary using MoveFileEx rename-while-running.
      6. Uninstaller idempotently removes the Scheduled Task + scrubs `%ProgramData%\go-mapi\updates\`.
      7. CI publishes `SHA256SUMS.txt` alongside release assets; Pester smoke harness covers the new flows.
      8. README has "Enterprise installation" section.
    - Drop the old bullets 3 + 4 (machine-wide YAML config, .example files) per D-02.
    - Drop tri-state language per D-07.
    - Update Plans: line from "TBD" to the 6 actual plans:
      - 11.1-01-PLAN.md — NSIS T2 + T4 fixes + Pester items 21+25 (D-03/D-04/D-05; INST-01, INST-04, INST-05, REL-06)
      - 11.1-02-PLAN.md — Silent updater scaffold + path resolver + flag parser + build-tag stub (D-10; REL-03, REL-09)
      - 11.1-03-PLAN.md — SHA256SUMS.txt release pipeline (D-17; REL-02, REL-09)
      - 11.1-04-PLAN.md — Silent updater download + ChecksumValidator + atomic swap (D-11/D-12/D-14; REL-03, REL-09)
      - 11.1-05-PLAN.md — Scheduled Task XML + NSIS register/remove + uninstaller scrub + Pester items 22-24 (D-07/D-08/D-09/D-16/D-18; INST-01, INST-04, INST-05, REL-06, REL-09)
      - 11.1-06-PLAN.md — README + ROADMAP + REQUIREMENTS docs (D-19/D-20; REL-07, REL-09)
    - Update **Requirements:** line to: `INST-01, INST-04, INST-05, REL-02, REL-03, REL-06, REL-07, REL-09`. (REL-09 is added by Plan 11.1-06 Task 3.)
    - Update the bottom Progress table row for Phase 11.1: `0/6` plans, status `Not started` until execute-phase runs. Adjust phase numbering only if the existing row was a stub.
  </behavior>
  <action>
    Edit `.planning/ROADMAP.md`. Find the section header `### Phase 11.1: Installer hardening + enterprise deploy + silent auto-update (INSERTED)`. Replace its entire body (Goal through Plans) with:

    ```markdown
    ### Phase 11.1: Installer hardening + enterprise deploy + silent auto-update (INSERTED)
    **Goal**: Ship v3.0 with a complete enterprise-deploy story — an admin can run `go-mapi-setup.exe /AUTOUPDATE=1` (or tick the install-time checkbox) to get a fleet-ready endpoint that installs system-wide and keeps itself updated unattended via a SYSTEM-context Scheduled Task. Single-user install is dropped (MAPI handler is inherently machine-wide); pre-existing T2 (Start Menu shortcut location) and T4 (32-bit DLL reinstall overwrite) bugs are root-caused and fixed.
    **Depends on**: Phase 11 (11-01/02/03/06 already shipped — updater backend + tray UI + main-window banner + E2E harness are prerequisites for the silent-update path; 11-04 Task 2+3 stays paused on `go-mapi-www` separately)
    **Requirements**: INST-01, INST-04, INST-05, REL-02, REL-03, REL-06, REL-07, REL-09 (REL-09 introduced by this phase — see REQUIREMENTS.md)
    **Success Criteria** (what must be TRUE):
      1. Installer is system-wide only: `RequestExecutionLevel admin` enforced, `SetShellVarContext all` wraps the Start Menu shortcut create/delete, no "Current User" / per-user fallback language anywhere in NSIS or docs (T2 + D-01 + D-03)
      2. In-place reinstall succeeds without a manual uninstall step — both x64 and x86 `go-mapi.dll` overwrite cleanly via `SetOverwrite try` + explicit `Delete`, with a Pester regression in the smoke harness (T4 + D-04 + D-05)
      3. Installer exposes a single boolean "Enable automatic updates" affordance (UI checkbox, default OFF; CLI parameter `/AUTOUPDATE=1` for silent installs); no tri-state, no machine-config gate (D-07)
      4. When enabled, installer registers a Windows Scheduled Task `go-mapi Auto Update` running as `SYSTEM` with `MultipleInstancesPolicy=IgnoreNew`, daily 03:00 + RandomDelay PT30M, at-startup catch-up, `RunOnlyIfNetworkAvailable=true`, `StartWhenAvailable=true`, `ExecutionTimeLimit=PT12H` (D-08 + D-09 + D-14)
      5. `go-mapi.exe --update-check-silent` runs without spawning the tray, window, or WebView2; downloads the new release asset(s) into `%ProgramData%\go-mapi\updates\staging\`, verifies SHA-256 via `SHA256SUMS.txt` + go-selfupdate `ChecksumValidator`, and atomically swaps the running binary using `MoveFileEx` rename-while-running. `MOVEFILE_DELAY_UNTIL_REBOOT` is explicitly NOT used (RDS targets do not reboot). Retry-with-backoff caps at 12 hours total elapsed (D-10 + D-11 + D-12 + D-13 + D-14)
      6. Uninstaller idempotently removes the Scheduled Task (`schtasks /delete /tn "go-mapi Auto Update" /f`, "task not found" exit code swallowed) BEFORE binary scrub, AND scrubs `%ProgramData%\go-mapi\updates\` and any `.old.<pid>` orphans (D-16 + D-18 case 6)
      7. CI publishes `SHA256SUMS.txt` alongside `go-mapi-setup.exe` at the stable Release URL; Pester smoke harness extends Phase 10's coverage with: 32-bit DLL reinstall regression (item 21), Start Menu shortcut location regression (item 25), `/AUTOUPDATE=1` task registration (item 22), `/AUTOUPDATE=0` task absence (item 23), uninstaller idempotent removal (item 24) (D-17 + D-18)
      8. README has an "Enterprise installation" section covering the All Users elevation requirement, `/AUTOUPDATE=1` parameter, Scheduled Task management commands, SHA-256 verification expectation, and the multi-user RDS limitation carry-forward; ROADMAP and REQUIREMENTS reflect the simplified scope (D-19 + D-20 + REL-09)
    **Plans**: 6 plans
    - [ ] 11.1-01-PLAN.md — NSIS T2 + T4 fixes + Pester items 21+25 regressions (INST-01, INST-04, INST-05, REL-06; D-01/D-03/D-04/D-05)
    - [ ] 11.1-02-PLAN.md — Silent updater scaffold: --update-check-silent flag + updatesStagingDir() + build-tag stub (REL-03, REL-09; D-10/D-15)
    - [ ] 11.1-03-PLAN.md — SHA256SUMS.txt release pipeline step + asset list extension (REL-02, REL-09; D-11/D-17)
    - [ ] 11.1-04-PLAN.md — Silent updater download + ChecksumValidator wiring + MoveFileEx atomic swap with retry (REL-03, REL-09; D-11/D-12/D-13/D-14)
    - [ ] 11.1-05-PLAN.md — Scheduled Task XML + NSIS /AUTOUPDATE parser + register/remove + Pester items 22-24 + uninstaller scrub (INST-01, INST-04, INST-05, REL-06, REL-09; D-07/D-08/D-09/D-16/D-18)
    - [ ] 11.1-06-PLAN.md — README "Enterprise installation" section + ROADMAP rewrite + REQUIREMENTS REL-09 insertion (REL-07, REL-09; D-19/D-20)
    ```

    Update the Progress table at the bottom of the file. Find the row for Phase 11.1:
    ```
    | 11.1. Installer hardening + enterprise deploy + silent auto-update (INSERTED) | v3.0 | 0/6 | Not started | - |
    ```
    (Adjust 0/6 to match plan count.)

    Update the trailing footer comment to reflect the 11.1 rewrite date:
    ```
    *Roadmap updated: 2026-04-25 — Phase 11.1 success criteria rewritten to simplified scope (binary auto-update checkbox + Scheduled Task + SHA-256 manifest); machine-config YAML deferred per D-02; tri-state install collapsed to binary per D-07. See .planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-CONTEXT.md for the locked decision rationale.*
    ```

    Do NOT touch other phases. Phase 11's paused 11-04 Task 2+3 entry stays as-is.
  </action>
  <verify>
    <automated>grep -c "Phase 11.1: Installer hardening" .planning/ROADMAP.md && grep -c "REL-09" .planning/ROADMAP.md && grep -c "11.1-01-PLAN.md" .planning/ROADMAP.md && grep -c "11.1-06-PLAN.md" .planning/ROADMAP.md && grep -cE "machine-wide.*config\\.yaml|Tri-state" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 11.1: Installer hardening" .planning/ROADMAP.md` returns 1.
    - `grep -c "11.1-01-PLAN.md" .planning/ROADMAP.md` through `grep -c "11.1-06-PLAN.md" .planning/ROADMAP.md` each return at least 1.
    - `grep -c "REL-09" .planning/ROADMAP.md` returns at least 1 (Requirements line).
    - `grep -c "REL-09 introduced by this phase" .planning/ROADMAP.md` returns 1.
    - `grep -cE "(machine-wide config|tri-state|None / Notify / Silent)" .planning/ROADMAP.md` returns 0 in the Phase 11.1 entry (those bullets are removed). Run a scoped grep: `awk '/^### Phase 11\\.1/,/^### Phase 12|^## Progress/' .planning/ROADMAP.md | grep -cE "(machine-wide config|tri-state|None / Notify / Silent)"` returns 0.
    - Phase 11.1 success criteria has exactly 8 numbered bullets (verifiable via `awk '/^### Phase 11\\.1/,/^### Phase 12|^## Progress/' .planning/ROADMAP.md | grep -cE "^\\s+[0-9]+\\."`) returns 8.
    - `grep -c "schtasks" .planning/ROADMAP.md` returns at least 1 (success criterion 6).
    - The Progress table row for Phase 11.1 reads `0/6 | Not started`.
    - Footer date updated to 2026-04-25 (current research date).
  </acceptance_criteria>
  <done>ROADMAP rewritten to match delivered scope; downstream agents now read the truth.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Insert REL-09 into REQUIREMENTS.md + update traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — must understand existing format conventions and traceability table structure)
    - .planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-RESEARCH.md (§Phase Requirements table — REL-09 canonical text)
    - .planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-CONTEXT.md (D-11 + scoping for REL-09)
  </read_first>
  <behavior>
    - In §Release & Autoupdate, add `REL-09` after the existing `REL-08` entry (preserves numerical ordering).
    - REL-09 text per the canonical formulation in RESEARCH.md.
    - Append a note to REL-03 + REL-04 in the same section: "(extended in Phase 11.1 — see REL-09 for the silent-update path)".
    - Add a row to the Traceability table mapping `REL-09` to `Phase 11.1` with status `Pending`.
    - Update the Coverage line at the bottom of the Traceability section: total v3.0 requirements becomes 45 (was 44); mapped to phases stays equal (no orphans).
    - Update the "Last updated" footer to reflect the new entry.
  </behavior>
  <action>
    Edit `.planning/REQUIREMENTS.md`:

    **Step 1 — In §Release & Autoupdate**, immediately after the existing `REL-08` line, add:
    ```markdown
    - [ ] **REL-09**: Silent auto-update — a SYSTEM-context Windows Scheduled Task downloads release assets from the stable URL, verifies SHA-256 against `SHA256SUMS.txt` (parsed via go-selfupdate `ChecksumValidator{UniqueFilename: "SHA256SUMS.txt"}`), and atomically swaps `go-mapi.exe` (and the x64 + x86 `go-mapi.dll`) using `MoveFileEx` rename-while-running, without elevating the interactive user or restarting it. Install-time opt-in via UI checkbox or `/AUTOUPDATE=1` parameter (default OFF). Uninstaller idempotently removes the Scheduled Task and the `%ProgramData%\go-mapi\updates\` staging directory.
    ```

    **Step 2 — Append annotations to REL-03 + REL-04** in the same section. Find these existing lines:
    ```markdown
    - [ ] **REL-03**: In-app version check uses `creativeprojects/go-selfupdate` to query GitHub Releases on startup and once per 24h
    - [ ] **REL-04**: When a newer version is available, app shows a tray notification with "Download" action that opens the release page in the browser — no in-process binary replacement
    ```
    Replace with:
    ```markdown
    - [ ] **REL-03**: In-app version check uses `creativeprojects/go-selfupdate` to query GitHub Releases on startup and once per 24h *(extended in Phase 11.1 — see REL-09 for the silent-update path)*
    - [ ] **REL-04**: When a newer version is available, app shows a tray notification with "Download" action that opens the release page in the browser — no in-process binary replacement *(Phase 11.1 adds an out-of-process SYSTEM Scheduled Task path alongside the notify-only baseline — REL-09)*
    ```

    **Step 3 — Add traceability row.** Find the Traceability table and locate the existing `REL-08` row. After it, add:
    ```markdown
    | REL-09 | Phase 11.1 | Pending |
    ```

    **Step 4 — Update Coverage block** at the bottom of the Traceability section. Find:
    ```markdown
    **Coverage:**
    - v3.0 requirements: 44 total (note: original count of 43 was off by one — all categories re-counted from requirements text)
    - Mapped to phases: 44
    - Unmapped: 0 ✓
    ```
    Replace with:
    ```markdown
    **Coverage:**
    - v3.0 requirements: 45 total (REL-09 added in Phase 11.1; original count of 43 was off by one; all categories re-counted from requirements text)
    - Mapped to phases: 45
    - Unmapped: 0 ✓
    ```

    **Step 5 — Update footer** at very bottom:
    ```markdown
    *Last updated: 2026-04-25 — REL-09 added (silent auto-update via SYSTEM Scheduled Task + SHA-256 verification) and mapped to Phase 11.1; REL-03 + REL-04 annotated to reference the silent-path extension.*
    ```
  </action>
  <verify>
    <automated>grep -c "REL-09" .planning/REQUIREMENTS.md && grep -c "Phase 11.1" .planning/REQUIREMENTS.md && grep -c "ChecksumValidator" .planning/REQUIREMENTS.md && grep -c "extended in Phase 11.1" .planning/REQUIREMENTS.md && grep -c "v3.0 requirements: 45" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \\[ \\] \\*\\*REL-09\\*\\*" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "REL-09" .planning/REQUIREMENTS.md` returns at least 4 (definition + traceability row + footer + any cross-reference).
    - `grep -c "Phase 11.1" .planning/REQUIREMENTS.md` returns at least 1 (traceability row).
    - `grep -c "ChecksumValidator" .planning/REQUIREMENTS.md` returns at least 1 (REL-09 definition).
    - `grep -c "extended in Phase 11.1" .planning/REQUIREMENTS.md` returns at least 2 (REL-03 + REL-04 annotations).
    - `grep -c "v3.0 requirements: 45" .planning/REQUIREMENTS.md` returns 1 (Coverage block updated).
    - `grep -cE "REL-09\\s+\\|\\s+Phase 11\\.1\\s+\\|\\s+Pending" .planning/REQUIREMENTS.md` returns 1 (traceability row).
    - Footer line includes "REL-09 added" + the date `2026-04-25` (or whatever current research date the orchestrator passed).
  </acceptance_criteria>
  <done>REL-09 lands in REQUIREMENTS.md with full traceability; existing REL-03/REL-04 entries cross-reference it.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files (README, ROADMAP, REQUIREMENTS) | Pure text — no executable surface. Trust boundary is git history (anyone with repo write access can edit). No security threats from this plan; documentation accuracy is the deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1.06-01 | Information Disclosure | README publishes the exact Scheduled Task name, principal, and trigger schedule — could help an attacker who has admin access to plan a malicious task swap | accept | Task properties are also visible to anyone running `taskschd.msc` on a go-mapi-installed machine. Hiding the docs would be security-by-obscurity. The actual mitigation (admin-only ACL on the task XML at `%SystemRoot%\System32\Tasks\`) lives in Plan 11.1-05. |
| T-11.1.06-02 | Tampering | Documentation drift between README and actual installer behavior could mislead admins into thinking they can rely on a feature that doesn't work | mitigate | Task 1 acceptance criteria check that all key feature names (`/AUTOUPDATE=1`, `go-mapi Auto Update`, `SHA256SUMS.txt`) appear in README, matching the actual implementation in earlier plans. Pester items 22-24 (Plan 11.1-05) are the ground-truth verification of the documented behavior. |

</threat_model>

<verification>
- All three tasks complete with green acceptance criteria.
- README.md ## Enterprise installation H2 lands without disrupting consumer copy.
- ROADMAP.md Phase 11.1 entry rewritten with 8 numbered success criteria + 6 plan list entries.
- REQUIREMENTS.md REL-09 entry + traceability row + coverage update lands; REL-03 + REL-04 annotated.
</verification>

<success_criteria>
- README has the documented enterprise install path; admins can read it and use `/AUTOUPDATE=1` correctly.
- ROADMAP downstream agents see the simplified scope (no machine-config language, no tri-state).
- REQUIREMENTS REL-09 is canonical and traceable to Phase 11.1.
- Phase 11.1 documentation deliverables fully closed.
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-06-SUMMARY.md`.
</output>
