# Phase 11.1 Plan 03: SHA256SUMS.txt Generation Summary

**Status:** Completed
**Phase:** 11.1
**Plan:** 03
**Subsystem:** Release CI
**Key Files:** `.github/workflows/installer-release.yml`, `.planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-03-AUDIT.md`

## substantive one-liner
Extended the release pipeline to generate and publish a `SHA256SUMS.txt` manifest alongside the installer, enabling integrity verification for silent auto-updates.

## Key Decisions
- **Option A (Full Manifest):** Included `go-mapi-setup.exe`, `go-mapi.exe`, and both DLL bitnesses (`go-mapi-x64.dll`, `go-mapi-x86.dll`) in the manifest. This provides maximum coverage for future per-file update strategies.
- **SignPath Reconciliation:** Fixed a pre-existing bug in the `installer-release.yml` staging/replacement logic where it used incorrect paths for the interceptor DLLs. Corrected to use `src/interceptor/build-x64/bin/` and `src/interceptor/build-x86/bin/`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed broken interceptor DLL paths in installer-release.yml**
- **Found during:** Task 1 audit.
- **Issue:** The workflow used `src/interceptor/build/bin/go-mapi.dll` for staging and replacement, but this path doesn't exist since the switch to dual-bitness builds.
- **Fix:** Updated paths to `src/interceptor/build-x64/bin/go-mapi.dll` and `src/interceptor/build-x86/bin/go-mapi.dll`. Renamed during staging to include architecture in the filename for SignPath (`go-mapi-x64.dll`, `go-mapi-x86.dll`).
- **Files modified:** `.github/workflows/installer-release.yml`
- **Commit:** `0685ae7`

## Self-Check: PASSED

**1. Check created files exist:**
- `FOUND: .planning/phases/11.1-installer-hardening-enterprise-deploy-silent-auto-update/11.1-03-AUDIT.md`

**2. Check commits exist:**
- `FOUND: 8eb3508` (Audit commit)
- `FOUND: 0685ae7` (Implementation commit)

## Metrics
- **Duration:** ~25 minutes
- **Tasks:** 2/2
- **Files:** 3 modified/created
