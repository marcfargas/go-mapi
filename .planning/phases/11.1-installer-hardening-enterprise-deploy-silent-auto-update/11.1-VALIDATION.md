---
phase: 11.1
slug: installer-hardening-enterprise-deploy-silent-auto-update
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-28
mode: reconstruct-from-artifacts
---

# Phase 11.1 — Validation Strategy

> Reconstructed retroactively from `11.1-0{1..6}-SUMMARY.md` and `11.1-UAT.md` (8/8 pass: 4 direct + 4 by-reference).

---

## Test Infrastructure

| Layer | Framework | Config | Quick command | Full command | CI gate |
|-------|-----------|--------|---------------|--------------|---------|
| Go unit | `go test` (stdlib) | `go.work`, `src/app/go.mod` | `go test ./src/app/... -run TestSilent` | `go test -race ./internal/mapi/... ./src/app/...` | `build.yml` (per-PR, `-race` on `windows-latest`) |
| NSIS regression | Pester 5 | `src/installer/tests/installer.Tests.ps1` | `Invoke-Pester -Path src/installer/tests` | (same) | `installer-smoke.yml` (per push to develop/main, path-filtered) |
| Release pipeline | GitHub Actions | `.github/workflows/installer-release.yml` | n/a — runs on tag push | (same) | tag push gate |
| Installer UAT | Windows Sandbox harness | `tests/sandbox/uat-11.1-05.ps1` | n/a — orchestrator-executed | (same) | manual / on-demand |

**Estimated quick runtime:** ~5s (Go silent-update tests in isolation). **Full Pester:** ~3-5 min on `windows-latest`.

---

## Sampling Rate

- **After every Go task commit (Plans 11.1-02, 11.1-04):** `go test ./src/app/...` (no race; arm64 dev hosts can't `-race`)
- **After every NSIS task commit (Plans 11.1-01, 11.1-05):** `Invoke-Pester -Path src/installer/tests` (Windows host required)
- **Before phase close-out:** Full Pester via CI + Go `-race` via CI + sandbox UAT round-trip
- **Max feedback latency:** ~30s local (Go), ~5min CI (Pester on windows-latest)

---

## Per-Task Verification Map

| Plan | Requirement / Decision | Test Type | Automated Command | Test ID | Status |
|------|------------------------|-----------|-------------------|---------|--------|
| 11.1-01 | T2 / D-03 — Start Menu shortcut at `%ProgramData%` | Pester | `Invoke-Pester -FullNameFilter "25."` | item 25 | ✅ COVERED |
| 11.1-01 | T4 / D-04, D-05 — 32-bit DLL reinstall overwrite | Pester | `Invoke-Pester -FullNameFilter "21."` | item 21 | ✅ COVERED |
| 11.1-02 | D-12 — `updatesStagingDir()` env precedence | Go unit | `go test -run TestUpdatesStagingDir_EnvPrecedence ./src/app/...` | `paths_test.go` (3 subtests) | ✅ COVERED |
| 11.1-02 | D-10 — `--update-check-silent` flag stub | Go unit | `go test -run TestRunSilentUpdate ./src/app/...` | `updates_silent_test.go` (4 tests) | ✅ COVERED |
| 11.1-02 | D-09 — bindings build-tag split | Build | `go build -tags bindings ./...` | CI (`build.yml`) | ✅ COVERED |
| 11.1-03 | D-17 — `SHA256SUMS.txt` published in release | Manual / GHA run | (workflow run on tag push) | n/a | ⚠️ MANUAL-ONLY |
| 11.1-04 | D-11 — SHA-256 verify-before-write | Go unit | `go test -run "TestSilentChecksum\|FullPipeline\|ChecksumMismatch" ./src/app/...` | 4 tests | ✅ COVERED |
| 11.1-04 | D-12 — MoveFileEx atomic swap | Go unit | `go test -run TestSilentSwapHappyPath ./src/app/...` | 1 test | ✅ COVERED |
| 11.1-04 | D-14 — Retry cap | Go unit | `go test -run TestSilentSwapWithRetry_GivesUpAtDeadline ./src/app/...` | 1 test | ✅ COVERED |
| 11.1-04 | W7 (Go) — `*.old.<pid>` orphan cleanup | Go unit | `go test -run TestSilentCleansOldOrphans ./src/app/...` | 1 test | ✅ COVERED |
| 11.1-04 | Pitfall 6 / T-11.1.04-06 — no hex digest in `update.log` | Go unit (hex-leak guard) | `go test -run TestRunSilentUpdate_UpdateAvailable_FullPipeline ./src/app/...` | embedded scan | ✅ COVERED |
| 11.1-04 | REL-09 / D-11 — canonical SHA256SUMS URL pinning | Go unit | `go test -run TestSilentChecksumsURLIsCanonicalStableURL ./src/app/...` | **added 2026-04-28** | ✅ COVERED (this audit) |
| 11.1-05 | D-08 / D-18#1 — `/AUTOUPDATE=1` registers task | Pester + Sandbox UAT | `Invoke-Pester -FullNameFilter "22."` | item 22 + UAT A | ✅ COVERED |
| 11.1-05 | D-07 / D-18#2 — `/AUTOUPDATE=0` no task | Pester + Sandbox UAT | `Invoke-Pester -FullNameFilter "23."` | item 23 + UAT C | ✅ COVERED |
| 11.1-05 | D-16 / D-18#5 — Idempotent uninstall removes task | Pester + Sandbox UAT | `Invoke-Pester -FullNameFilter "24\."` | item 24 + UAT D | ✅ COVERED |
| 11.1-05 | D-18#6 — Uninstall scrubs `%ProgramData%\go-mapi\updates` | Pester + Sandbox UAT | `Invoke-Pester -FullNameFilter "24\."` | item 24 + UAT B | ✅ COVERED |
| 11.1-05 | W7 (NSIS) — Uninstall scrubs `*.old.<pid>` orphans | Pester + Sandbox UAT | `Invoke-Pester -FullNameFilter "24b\."` | item 24b + UAT F | ✅ COVERED |
| 11.1-05 | D-15 — No Wails UI changes | n/a (negative req) | n/a | n/a | ✅ COVERED (no surface) |
| 11.1-06 | D-19 — README "Enterprise installation" section | grep AC | grep counts in `11.1-06-PLAN.md` | inspection | ✅ COVERED |
| 11.1-06 | D-20 — ROADMAP success-criteria rewrite | grep AC | grep counts in `11.1-06-PLAN.md` | inspection | ✅ COVERED |
| 11.1-06 | REL-09 entry in REQUIREMENTS.md | grep AC | grep counts in `11.1-06-PLAN.md` | inspection | ✅ COVERED |

*Status: ⬜ pending · ✅ COVERED · ⚠️ MANUAL-ONLY · ❌ MISSING*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new framework or fixture stubs needed — Phase 11.1 inherited Pester 5 (Phase 10), Go test (Phase 1+), and the `tests/sandbox/` harness (planted during Plan 11.1-05).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `installer-release.yml` actually publishes `SHA256SUMS.txt` on tag push | D-17 / REL-09 | GitHub Actions workflow can only be exercised end-to-end by an actual tag push. YAML inspection is the only static gate. | After v3.0 tag push, verify Release page contains `SHA256SUMS.txt` alongside `go-mapi-setup.exe`, `go-mapi.exe`, `go-mapi-x64.dll`, `go-mapi-x86.dll`. Manifest format: `<64-char hex>  <filename>`. |
| Sandbox UAT round-trip for installer changes | D-07, D-08, D-16, D-18 (#1, #2, #5, #6) | Real Windows install path required (NSIS `makensis` compile + Pester discovery don't exercise the install runtime). Pester items 22-24b codify the assertions for CI, but the round-trip itself is orchestrator-executed. | Run `tests/sandbox/uat-11.1-05.ps1` (or successor); verify `uat-11.1-05-results.json` reports `all_pass: true`. |

---

## Validation Sign-Off

- [x] All tasks have automated verification or are explicitly manual-only with rationale
- [x] Sampling continuity: every plan touched by code has at least one automated test
- [x] Wave 0 not needed — existing infrastructure covers all requirements
- [x] No watch-mode flags
- [x] Feedback latency: ~30s (Go local), ~5min (Pester CI)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-04-28

---

## Validation Audit 2026-04-28

| Metric | Count |
|--------|-------|
| Phase plans audited | 6 |
| Requirements/decisions tracked | 21 |
| COVERED | 20 |
| MANUAL-ONLY | 1 (D-17 release-pipeline run) |
| MISSING (pre-audit) | 1 (REL-09 URL pinning) |
| Resolved this audit | 1 (added `TestSilentChecksumsURLIsCanonicalStableURL`) |
| Escalated | 0 |

**Pending CI gate:** `installer-smoke.yml` has not yet exercised Pester items 21, 22, 23, 24, 24b, 25 on `windows-latest` since develop is 210+ commits ahead of `origin/develop`. Next push to develop closes the loop. Non-blocking for Phase 11.1 verification given documented sandbox-UAT round-trips at plan-execution time.
