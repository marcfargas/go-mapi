# Phase 1 Handoff — Resume at Phase 2

**Phase 1 status:** ✓ COMPLETE (2026-04-10)
**Next action:** `/clear` then `/gsd:autonomous --from 2`

## What shipped in Phase 1

All FOUND-* requirements + SIGN-01 draft. See `01-VERIFICATION.md` for details.

- FOUND-01 watcher race fix (1 line, verified with `-race`)
- FOUND-02 version.go + hostVersion READY field (additive)
- FOUND-03 GmailClient baseURL via `NewGmailClientWithBase`
- FOUND-04 GOMAPI_WATCH_DIR env + --watch-dir + --gmail-api-base flags
- FOUND-05 C++ message_converter OBJECT library extraction
- FOUND-06 manifest .tmpl files + install.ps1 refactor
- SIGN-01 draft at `SIGNPATH-APPLICATION.md` (filing deferred by Marc)
- License aligned to LGPL-3.0-or-later (LICENSE, COPYING, package.json, README.md)

## Deferred to Marc (NOT Phase 2 scope)

- **File SignPath Foundation application.** Draft is ready. See `01-08-SUMMARY.md` for the pre-filing checklist.
- **Publish Chrome Web Store listing.** Separate effort, out of roadmap.
- **Verify SignPath URL is still https://signpath.org/apply before filing.**

## Decisions that Phase 2 must honor

From `01-CONTEXT.md` and decisions accumulated in STATE.md:

1. **Keep the legacy `version` field** on OutgoingMessage alongside new `hostVersion` — Phase 2 EXT-03 consumes `hostVersion`, but v1 extensions still read the legacy field. No protocol bump.
2. **Phase 2 EXT-03** defines `MIN_SUPPORTED_HOST_VERSION` equal to the current host version so the `OUTDATED` branch ships as a dead branch (ready for v3.0.0 activation without wire-protocol change).
3. **All new state lives in the internal `HOST_STATE` extension broadcast** — no new native-messaging wire message types.
4. **Placeholder download URL** in `InstallPrompt.tsx` during Phase 2 — EXT-07 (real GitHub Releases URL swap) lives in Phase 3 because the real URL only exists after the installer is published.
5. **Scope discipline**: Phase 2 is EXT-01..06 only. No installer work (Phase 3), no extension test writing (Phase 4 TSTEST-*).

## Known rough edges / tech debt surfaced in Phase 1

- `src/native-host/gmail.go` has ~10 `staticcheck` suggestions (WriteString(Sprintf) → Fprintf, `interface{}` → `any`). Pre-existing, not from Phase 1 work. Out of scope unless Phase 4 chooses to address.
- `src/native-host/main.go` has `logInfo/logError` signatures using `interface{}` instead of `any`. Pre-existing.
- `src/native-host/watcher.go:352` has a stringcutprefix suggestion. Pre-existing.

## Context needed by Phase 2

Essential reads at Phase 2 kickoff:
- `.planning/ROADMAP.md` (Phase 2 section)
- `.planning/REQUIREMENTS.md` (EXT-01 through EXT-06)
- `.planning/phases/01-foundation-signpath-application/01-SUMMARY.md` files (what was actually built, esp. 01-01 for the hostVersion field)
- `src/extension/src/background/service-worker.ts` (current host connection logic)
- `src/extension/src/popup/App.tsx` (current popup root)
- `src/extension/src/types/messages.ts` (NativeReadyMessage with new `hostVersion?: string` field)
- `src/native-host/protocol.go` (READY message for reference)

## Verification commands for Phase 2 exit

When Phase 2 is done, these should all exit 0:

```bash
cd src/native-host && go build ./... && go vet ./... && go test ./...
cd src/extension && npx tsc --noEmit && npm run lint && npm run test:run
```

## Git state at handoff

- Branch: `develop`
- Commits ahead of origin/develop: ~34
- Working tree: clean
- Head: `ac1f573 docs: mark Phase 1 complete in STATE.md — advance to Phase 2`
