node_modules/
build/
bin/
obj/
# QUICK-260423-ntu T3a: arch-specific CMake build dirs (x86_64 / i686 clang)
build-x64/
build-x86/
# QUICK-260423-tk6: scratch CMake build dir used when running the C++
# test harness standalone with -DBUILD_TESTS=ON (see tk6-PLAN.md verification).
build-x64-test/
dist/
*.dll
*.obj
*.exe

# Vendored NSIS plugin binaries (Phase 10 installer) — tracked intentionally
!src/installer/plugins/**/*.dll
# Vendored WebView2 Evergreen bootstrapper (Phase 10 plan 10-02, D-05) — tracked intentionally
!src/installer/MicrosoftEdgeWebview2Setup.exe

.env
*.pdb
*.lib
*.exp
out/
.vs/
CMakeUserPresets.json
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/
.user
*.log
.claude/settings.local.json


PLANNING_AGENT.md
TODO.txt
TODO.md

# Test outputs
test-results/
playwright-report/
.claude/*
!.claude/skills/

# Pi agent config (local, not tracked)
.pi/

# Internal/dev-only
experiments/
TODO.txt
PLANNING_AGENT.md
/release/

# Phase 8: OAuth credentials — must never be committed
/.env.local

# Phase 11 smoke: sandbox evidence bundles (screenshots, logs, app.log copies)
# and locally-built RC installers. Ledger stays tracked via 11-SMOKE-EVIDENCE.md.
tests/sandbox/phase11/evidence-*/
tests/sandbox/phase11/installer/

# Phase 11 plan 06: Playwright e2e workspace artifacts
tests/e2e/node_modules/
tests/e2e/playwright-report/
tests/e2e/test-results/
tests/e2e/.cache/

# Local Crabbox failure bundles and run captures
.crabbox/

# Per-worktree temporary build and validation artifacts
.tmp/
